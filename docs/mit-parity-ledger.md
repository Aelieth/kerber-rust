# MIT 1.22.2 KDC parity ledger

Oracle: MIT Kerberos **1.22.2** source SHA `3243ffbc…af13`
(`https://github.com/krb5/krb5/tree/krb5-1.22.2-final`) plus the live
image `kerber-rust-mit-kdc:1.22.2`. Verified with `sha256sum -c` on
`working/logs/audit-polish-0902/w0d/mit-src-sha256.txt` (and the W0e
regeneration under `working/logs/audit-polish-0902/w0e/`).
Heimdal/Samba are regression, not the equality bar. Isolation: host
`/etc/krb5.conf` stays `TESTLABBY.LOCAL`.

This file is the W1-A sweep (`working/ledger-w1a-0903-1702.md`) with
Part 0 of `working/plan-audit-polish-w1a-f-0903-1330.md` applied from
the three verification reports
`working/logs/audit-polish-0902/w0c-audit/ledger-verify-a{1,2,3}-*.md`.
Overlap (PROCESS_TGS, GET_LOCAL_TGT, FIND_FAST, HANDLE_AUTHDATA,
AD-FX-ARMOR) is left in both sections on purpose — A1 owns TGS gather,
A2 owns AS/`kdc_util`, A3 owns FAST residue.

W0d G3 is in tree: FAST unwrap failures put the MIT status word
`FIND_FAST` on the wire `e_text` (`do_as_req.c:806`,
`do_tgs_req.c:205-206`) and the `k5_setmsg` text in the `kdc.issue`
`detail` field. Rows that were `deviation (e_text)` only for that
mismatch are `exact` here.

Schema: `MIT file:line | check | MIT status + wire code | Rust site | Rust e_text + code | verdict | proof`.
Verdict ∈ {exact, stricter-documented (`docs/security.md` row), absent,
deviation, deferred (reason + promotion oracle)}. Proof `none` only
with deferred. A named gate cell or `diffsend` case that does not exist
is `proposed`. The 110 live `diffsend` cases are `garbage-pdu`,
`unknown-cname`, `etype-nosupp`, `as-session-enctype`, `wrong-realm`, `pauser-no-preauth`,
`skewed-timestamp`, `as-needchange`, `as-invalid-opts`, `as-validate-before-preauth`,
`as-optimistic-encts-wrong-etype`, `unknown-sname`, `as-success`, `as-retransmit`,
`as-request-anonymous`, `tgs-success`, `tgs-not-a-tgt`, `tgt-expired`, `tgt-nyv`, `tgt-nyv-no-starttime`,
`fast-armor-no-subkey`, `armor-ap-req-as-pa-tgs-req`, `tgs-ad-fx-armor-authenticator`,
`as-bad-msg-type`, `as-bad-pvno`, `tgs-bad-msg-type`, `as-service-not-allowed`,
`tgs-ap-options`, `tgs-header-kvno-zero`, `as-hw-preauth`, `as-spake-round1`,
`u2u-2nd-ticket-unknown-server`, `u2u-2nd-ticket-bad-etype`, `u2u-2nd-ticket-corrupt`,
`tgs-pac-client-mismatch`, `tgs-pac-corrupt-before-sname`, `tgs-pac-request-false`,
`tgs-from-pacless-tgt`, `tgs-renew-service-ticket`, `tgs-proxy-krbtgt`,
`tgs-canonicalize-renew`, `tgs-expired-vs-unknown-sname`, `s4u2self-no-pac`,
`s4u2self-pac-client-mismatch`, `pa-s4u-x509-user-bad-checksum`, `pa-s4u-x509-user-nonce`,
`pa-for-user-only`, `pa-s4u-x509-user-empty`, `pa-for-user-undecodable`,
`pa-s4u-x509-user`, `s4u2proxy-no-2nd-tkt`, `s4u2proxy-not-forwardable`,
`s4u2proxy-u2u-combo`, `s4u2proxy-tgs-target`, `s4u2proxy-no-header-pac`,
`s4u2proxy-header-pac`, `s4u2proxy-no-stkt-pac`, `s4u2proxy-evidence-mismatch`,
`s4u2proxy-local-stkt-pac`, `u2u-no-2nd-tkt`, `u2u-2nd-ticket-not-tgs`,
`u2u-2nd-ticket-mismatch`, `u2u-2nd-ticket-bad-pac`, `u2u-bad-etype`,
`u2u-success`, `tgs-addr-mismatch`, `tgs-forwarded-addresses`,
`u2u-2nd-ticket-foreign-realm`, `s4u2self-renew-options`,
`pa-s4u-x509-user-truncated`, `s4u2self-krbtgt-other`,
`tgs-locked-pac-mismatch`, `u2u-dup-skey-tgt-based`,
`tgs-expired-addr-mismatch`, `tgs-expired-badmatch`,
`u2u-2nd-ticket-kvno-miss`, `u2u-2nd-ticket-disallow-svr`,
`s4u2self-cert-only`, `tgs-forwarded-tgt-addresses`,
`tgs-pac-server-cksum-wrong-enctype`, `u2u-2nd-ticket-pac-wrong-enctype`,
`u2u-success-offered`, `tgs-forwarded-on-non-f-tgt`, `tgs-proxy-on-non-p-tgt`,
`tgs-postdate-on-non-postdatable`, `tgs-postdated-is-invalid`,
`tgs-validate-invalid-non-renewable`,
`tgs-till-in-past`, `tgs-service-expired-require-auth`, `tgs-postdated-from`,
`tgs-no-preauth-flag`, `tgs-hw-preauth-flag`, `tgs-nyv-inside-skew`,
`tgs-body-authdata`, `tgs-ad-mandatory-for-kdc`,
`tgs-body-authdata-kdc-issued-stripped`, `tgs-body-authdata-subkey`,
`tgs-body-authdata-session-ku5`, `tgs-tgt-and-or-kept`,
`tgs-truncated-cammac`,
`ec-outside-fast`, `tgs-rbcd-pac-options`,
`tgs-renew-header-end-before-start`,
`as-anonymous-unsigned-authpack-named-client`, `as-fast-hide-error-client`,
`tgs-fast-hide-client`, `pkinit-stale-freshness`, `tgs-referral-no-dot`,
`tgs-alternate-tgs-hierarchical`, `tgs-renew-postdated-from`.

Wire `e_text` is the MIT **status word**. MIT log messages are not
wire text. `errcode_to_protocol` passes `offset ∈ [0,128]`
(`kdc_util.c:696-697`).

Counts (after W1-Z Z1.2):
**434** = A1 128 + A2 88 + A3 78 + A4 137 + B1 3.
exact 348 · stricter-documented 14 · deviation 35 ·
absent 8 · deferred 29.

Draft was 209 = 108 + 56 + 45 at HEAD `bafc5f2`. Additions: A1 8 +
A2 10 (9 report rows + the `kdc_util.c:144-191` split) + A3 10 = 28
row inserts (the plan's "27" counted the split inside the A2 9).

## Ranked F-batches (security > parity > e_text)

Corrected by the verification reports. Each batch ≤ 6 commits. One MIT
check family per commit. F1 starts after this ledger lands; F3–F9 get
a short plan when reached.

### F1 FAST armor / AD-FX-ARMOR / cookie (security)

1. `kdc: Refuse FAST armor without an authenticator subkey like armor_ap_request` —
   **landed (A′-1 item 1).** AS explicit armor (`fast_util.c:70-76`) and TGS
   explicit armor without a PA-TGS-REQ subkey (`:157-166`): 12, e_text
   `FIND_FAST`, detail `ap-request armor without subkey`. TGS explicit armor
   with a PA-TGS-REQ subkey stays 24. MIT clients always send a subkey.
2. `kdc: Refuse a header ticket or authenticator carrying AD-FX-ARMOR like kdc_process_tgs_req` —
   **landed (A′-1 item 3).** `kdc_util.c:217-229` → 12 `PROCESS_TGS` (detail
   `ticket valid only as FAST armor`). Recurses into IF-RELEVANT only
   (`authdata_dec.c:115-181`). Nothing in 1.22.2 emits 71.
3. `kdc: Bind PA-FX-COOKIE to the client and expire it at 600 seconds like kdc_fast_make_cookie` —
   **landed (A′-1 item 2).** `MIT1` ‖ kvno ‖ enc(prf+(local TGT key, `COOKIE` ‖
   unparsed client), ku 513); non-`MIT1` ignored (`:588-590,:610`). The ku-54 /
   ENC_CHALLENGE_CLIENT collision is a naming collision with no shared key.

TGS reply-key strengthen is **parity** (F7): the MIT client copies
`existing_key` when `strengthen_key` is NULL.

### F2 S4U / header PAC integrity (security)

Prerequisite: `kdc: Compute is_crossrealm from the header server entry
like do_tgs_req` (`do_tgs_req.c:686`). Then `S4U2SELF_NO_PAC` 20,
`S4U2PROXY_NO_HEADER_PAC` 20, `HEADER_PAC` 13, PAC client match,
`S4U2PROXY_LOCAL_STKT_PAC` 13, U2U `2ND_TKT_PAC`, `PA-PAC-REQUEST`
include_pac=false, `disable_pac`/anonymous no-PAC. S4U2Self pw-expiry
exemption is stricter — document or match.

### F3 second ticket (security / parity)

`2ND_TKT_NOT_TGS` 12, `2ND_TKT_MISMATCH` 26, `INVALID_S4U2PROXY_OPTIONS` 13,
TGS-target `NOT_ALLOWED_TO_DELEGATE` 12, `CAN'T PROXY TGT` 13,
`BAD_ETYPE_IN_2ND_TKT` 14, RBCD/xrealm PAC, `INVALID_S4U2SELF_CHECKSUM` 41
not 50. Demoted to e_text (F9): `NO_2ND_TKT`, `EVIDENCE_TKT_NOT_FORWARDABLE`
(both already refuse with 13).

### F4 AS request validation (security)

`INVALID AS OPTIONS` 13 (`kdc_util.c:727-729`) and drop the AS
`DISALLOW_SVR` ENC_TKT_IN_SKEY exemption (`:789-793`) in the **same
commit**. Validate `msg-type` (60 `VALIDATE_MESSAGE_TYPE`) and `pvno`
(MIT drops). Failcount lockout last. `ANONYMOUS NOT ALLOWED` /
`restrict_anon` (`kdc_util.c:700-712`; TGS `tgs_policy.c:763`) vs
unsupported 13 is parity + a security.md row, not a hole.
`NEEDED_HW_PREAUTH` 25 (`do_as_req.c:455-457`; hw_only hint list).
Admin unlock: `last_admin_unlock >= last_failed` (`lockout.c:102-104`).

### F5 TGS options and ticket flags (security / parity)

Implement `get_ticket_flags` (`kdc_util.c:813`). `TGT NOT
FORWARDABLE/PROXIABLE/POSTDATABLE` 13. Ticket addresses.
`check_tgs_nontgt` 26 + `check_tgs_tgt` after decrypt and only when
`NON_TGT_OPTION` is clear. `NOT_YET_VALID` without skew.
`NON-POSTDATABLE` only on `ALLOW_POSTDATE`. The lookaside reply cache is
now implemented (`lookaside.rs`), so an identical retransmit is answered
from the cache on both UDP and TCP.

### F6 CAMMAC + HANDLE_AUTHDATA (security, latent)

`cammac_create`/`cammac_check_kdcver` (ku 64) after copy, inside
`handle_pac` / `mint_ticket`. `require_auth` → `HIGHER_AUTHENTICATION_REQUIRED`
12. `GET_AUTH_INDICATORS`. `AD-MANDATORY-FOR-KDC` → 12.

### F7 RFC 6806 negotiation, FAST reply parity (parity)

149 checksum (ku 56) + empty 136 in `enc_padata` + `TKT_FLG_ENC_PA_REP`,
gated on request 149 — flag without 149 hard-fails MIT kinit. TGS
`strengthen_key`. PA-FX-COOKIE on every e_data-bearing AS error
(**after F1 cookie**). FAST error inner-padata order. Hint-list order
`[136,(11),19,modules]`. ETYPE-INFO2 only when the reply key was not
replaced.

### F8 gather order and lookups (parity)

AS lockout last; TGS `GET_LOCAL_TGT` before times; `HEADER_PAC` before
`search_sprinc`. TCP `FIELD_TOOLONG` 61 above UDP `RESPONSE_TOO_BIG` 52
(52 is dead at MIT's default 65536). `no PA-TGS-REQ` 16. `last_req` /
`key_expiration`. `starttime == authtime` omission. CANONICALIZE
canonical sname. `CANTLOCK_DB` 29 (absent: no lockable KDB; proof proposed).
`select_session_keytype`.

### F9 e_text and the differential unmask (parity, last)

Token renames (`locked` → `CLIENT LOCKED OUT`, …). Then
`compare_krb_error` compares `e_text` on every `diffsend` case.
Whitelist names the documented stricter rows.

## Not this ledger (W1-B / W1-C)

W1-B: `tgs_req_ex` FAST sibling; `get_dest_tgt` referral memory;
start-realm in the service loop; acceptor transited re-check;
ERROR-level `asn1.decode`/`crypto.decrypt` on expected failures.
W1-C: kpasswd pre-AP-REQ drop and post-AP-REQ `chpwfail` (J4 / `schpw.c`); kadmind chpw log `from <addr>`;
`kprop.rs` per-connection rcache; `dfl` restart cell; min_life (dictionary landed W1-C C1);
kadm5 ACL denial codes (rename AUTH_INSUFFICIENT-before-lockdown landed W0e H7); policy-rejection text.

OTP kdcpreauth, PA-S4U-X509-USER, PKINIT freshness, anonymous PKINIT stay
deferred with those promotion oracles (Batch D / user non-goal).

## A1 — tgs_policy.c / do_tgs_req.c / kdc_transit.c

MIT SHA 3243ffbc…af13. Wire codes are RFC 4120 `KRB-ERROR.error-code`.
Rust sites are `crates/krb5-kdc/src/…`. `ORDER` rows are check-sequence
mismatches, not extra statuses.

| MIT file:line | check (condition) | MIT status + wire code | Rust site | Rust e_text + code | verdict | proof |
| --- | --- | --- | --- | --- | --- | --- |
| kdc_util.c:171 | no PA-TGS-REQ (`KRB5_PADATA_AP_REQ`) | PROCESS_TGS 16 `PADATA_TYPE_NOSUPP` (set do_tgs_req.c:623) | issue.rs issue_tgs_from | `PROCESS_TGS` 16 | exact | `tgs_missing_pa_tgs_req_is_padata_type_nosupp` |
| kdc_util.c:179 | AP-REQ `USE_SESSION_KEY` or `MUTUAL_REQUIRED` | PROCESS_TGS 12 `POLICY` | issue.rs process_tgs_header | `PROCESS_TGS` 12 | exact | `tgs_ap_options_use_session_key_is_policy`; diffsend `tgs-ap-options` |
| kdc_util.c:190 | TGS AP-REQ replay cache | disabled (`auth_con_setflags 0`) | issue.rs issue_tgs_body | `TGS authenticator replay` 34 | stricter-documented | `tgs_authenticator_replay_is_repeat`; docs/security.md Replay — TGS authenticator |
| kdc_util.c:233 | authenticator missing checksum | PROCESS_TGS 50 `INAPP_CKSUM` | krb5-kdc/issue.rs process_tgs_header | `PROCESS_TGS` 50 | exact | `tgs_authenticator_missing_checksum_is_process_tgs`; proposed: diffsend |
| kdc_util.c:112-140; :248; :691-697 | req-body checksum (`comp_cksum`) | unknown 15 `SUMTYPE_NOSUPP`; not coll-proof 50 (no 1.22.2 type has `CKSUM_NOT_COLL_PROOF`); bad bytes 31 `BAD_INTEGRITY`; `KRB5_BAD_ENCTYPE`/`KRB5_BAD_MSIZE` → 60; status PROCESS_TGS | krb5-kdc/issue.rs process_tgs_header | unknown 15; provider/length 60; bad bytes 31; missing 50; PROCESS_TGS | exact | `tgs_authenticator_unknown_cksumtype_is_sumtype_nosupp`; `tgs_authenticator_bad_bytes_is_bad_integrity`; `tgs_authenticator_cksum_provider_mismatch_is_generic`; `tgs_authenticator_cksum_wrong_length_is_generic`; proposed diffsend |
| kdc_util.c:379 | header ticket server KDB NOENTRY | PROCESS_TGS 7 | issue.rs issue_tgs_body | `PROCESS_TGS` 7 | exact | `tgs_fast_forged_ticket_realm_is_process_tgs`; `mit-fast-kdc-gate.sh`; `capaths-transit-gate.sh` forged realm |
| kdc_util.c:390 | header krbtgt `DISALLOW_SVR`/`DISALLOW_ALL_TIX` | PROCESS_TGS 7 | issue.rs issue_tgs_body | `PROCESS_TGS` 7 | exact | `tgs_krbtgt_disallow_all_tix_is_process_tgs`; `tgs_local_krbtgt_disallow_svr_is_process_tgs`; `capaths-transit-gate.sh` DISALLOW_ALL_TIX |
| kdc_util.c:337 | header ticket decrypt/rd_req fail | PROCESS_TGS 31 (typical `BAD_INTEGRITY`) | krb5-kdc/issue.rs decrypt_presented_tgt | `PROCESS_TGS` 31 | exact | proposed: diffsend (no unit asserts 31+text) |
| rd_req_dec.c:627; valid_times.c:36-58 | header endtime/nyv inside PROCESS_TGS (`krb5int_validate_times`: NYV against `starttime`, else `authtime` `:44-51`; expired `:53-54`; after BADMATCH `:530-534` and BADADDR `:536-540`) | PROCESS_TGS 32/33 | krb5-kdc/issue.rs check_header_times_rd_req after BADMATCH/BADADDR | `PROCESS_TGS` 32 / `PROCESS_TGS` 33 (`starttime` absent → `authtime`); expired+caddr is **38**; expired+authenticator mismatch is **36** | exact | `tgs_renew_after_endtime_is_process_tgs`; `tgs_expired_beats_unknown_sname`; `a2_r19_expired_caddr_is_badaddr`; `a2_r19_expired_authenticator_mismatch_is_badmatch`; `z1_tgs_header_tgt_without_starttime_and_future_authtime_is_nyv`; diffsend `tgt-expired`/`tgt-nyv`/`tgt-nyv-no-starttime`/`tgs-expired-vs-unknown-sname`/`tgs-expired-addr-mismatch`/`tgs-expired-badmatch` |
| rd_req_dec.c:530-532 | authenticator cname/crealm ≠ ticket client | PROCESS_TGS 36 `BADMATCH` | issue.rs process_tgs_header | `PROCESS_TGS` 36 | exact | `tgs_authenticator_cname_mismatch_is_badmatch`; `tgs_authenticator_crealm_mismatch_is_badmatch`; docs/security.md |
| do_tgs_req.c:609-610,1202-1221 | `msg_type != TGS_REQ` (`gather_tgs_req_info` returns `KRB5_BADMSGTYPE`; `process_tgs_req` sets the status and answers through `prepare_error_tgs`) | `UNKNOWN_REASON` 60 (no cname, no crealm; sname echoed) | issue.rs issue_tgs_from | `UNKNOWN_REASON` 60 (no cname, no crealm; sname echoed) | exact | `tgs_bad_msg_type_is_unknown_reason_without_cname`; `tgs_bad_msg_type_omits_crealm`; diffsend `tgs-bad-msg-type` |
| do_tgs_req.c:623 | `kdc_process_tgs_req` any fail | PROCESS_TGS + inner code | issue.rs process_tgs_header | `PROCESS_TGS` + inner code | exact | `phase7_preauth.rs` PROCESS_TGS cells; `capaths.rs` PROCESS_TGS |
| do_tgs_req.c:637 | `kdc_find_fast` fail | FIND_FAST + inner code (41/12/24/60/93); MIT log is the `k5_setmsg` text | krb5-kdc/preauth.rs verify_fast_req_checksum; krb5-kdc/preauth.rs proto_fast; krb5-kdc/issue.rs issue_as_body | FIND_FAST + inner code (G3; detail = previous message) | exact | `fast_tgs_unkeyed_type_with_bad_bytes_is_modified`; docs/security.md FAST req_checksum |
| do_tgs_req.c:637 + fast_util.c:159 | explicit AP-REQ FAST armor on TGS | FIND_FAST 24 only if subkey present (log `Ap-request armor not permitted with TGS`); without subkey, `armor_ap_request` | krb5-kdc/preauth.rs kdc_find_fast; krb5-kdc/preauth.rs unwrap_fast_tgs; krb5-kdc/preauth.rs proto_fast | FIND_FAST 24 with PA-TGS-REQ subkey; without it, `armor_ap_request` (12 if that armor has no subkey) | exact | `tgs_fast_explicit_armor_is_preauth_failed`; `tgs_fast_explicit_armor_without_pa_tgs_subkey_is_accepted`; `tgs_fast_explicit_armor_without_any_subkey_is_policy`; docs/security.md FAST TGS AP-REQ armor |
| do_tgs_req.c:642 | FAST inner body `server == NULL` | NULL_SERVER 7 | issue.rs issue_tgs_body:1015 | `NULL_SERVER` 7 | deviation | proposed: diffsend FAST inner without sname |
| do_tgs_req.c:649 `get_local_tgt` kdc_util.c:486 | no `krbtgt/<body.realm>@<body.realm>` | GET_LOCAL_TGT 60 (`KDB_NOENTRY`→GENERIC) | issue.rs issue_tgs_body | `GET_LOCAL_TGT` 60 | exact | `tgs_local_sname_unknown_body_realm_is_get_local_tgt`; `phase7_preauth.rs` GET_LOCAL_TGT; `capaths-transit-gate.sh` GARBAGE.EXAMPLE / dest RENEW |
| ORDER do_tgs_req.c:649 vs tgs_policy.c:687 | GET_LOCAL_TGT before `check_tgs_times` | 60 then times | issue.rs issue_tgs_body:1017-1020 | GET_LOCAL_TGT 60 before skeleton times; header `krb5int_validate_times` stays in PROCESS_TGS | exact | `tgs_local_sname_unknown_body_realm_is_get_local_tgt`; `capaths-transit-gate.sh` GARBAGE.EXAMPLE / dest RENEW |
| do_tgs_req.c:662; kdc_util.c:597-602 | `get_verified_pac` for a TGS principal: only the server signature, with the key that opened the ticket | HEADER_PAC 41 `MODIFIED` (`verify_checksum`) / 60 `GENERIC` | krb5-kdc/ad.rs get_verified_pac | `HEADER_PAC` 41 `MODIFIED` / 60 `GENERIC` | exact | `tgs_corrupt_pac_before_unknown_sname_is_header_pac`; `a2_r23_header_pac_wrong_cksumtype_is_generic`; diffsend `tgs-pac-server-cksum-wrong-enctype`; `scripts/cross-kdc-gate.sh` MIT TGT → Rust TGS `kvno = 1` |
| tgs_policy.c:622 `check_normal_tgs_pac` | PAC present but not client and not RBCD-deleg; slot is constraints (`:721-724`) before `check_tgs_policy`; MIT `:618-620` accepts a cross-TGS header whose PAC verifies as a delegation PAC | HEADER_PAC 13 (beats SERVER LOCKED OUT) | krb5-kdc/ad.rs check_normal_tgs_pac; issue.rs issue_tgs_body | missing PAC ok; client mismatch 13 before deny_all; cross-TGS `verify_deleg_pac` | exact (mismatch); live accept path is Samba | `tgs_pac_client_mismatch_is_header_pac`; `a2_r19_locked_host_pac_mismatch_is_header_pac`; diffsend `tgs-pac-client-mismatch`; diffsend `tgs-locked-pac-mismatch` |
| ORDER do_tgs_req.c:657 vs :669 | HEADER_PAC before `search_sprinc` | PAC 41 then LOOKING_UP_SERVER | krb5-kdc/ad.rs get_verified_pac; issue.rs search_sprinc | `HEADER_PAC` 41 before LOOKING_UP_SERVER | exact | diffsend `tgs-pac-corrupt-before-sname`; `tgs_corrupt_pac_before_unknown_sname_is_header_pac` |
| do_tgs_req.c:536 `db_get_svc_princ` | server lookup fail | LOOKING_UP_SERVER 7 (remap :575); `CANTLOCK_DB` is 29 | issue.rs search_sprinc:1898; issue.rs lookup_svc_princ | `LOOKING_UP_SERVER` 7; 29 `SVC_UNAVAILABLE` passthrough | exact | diffsend `unknown-sname`; diffsend `tgs-referral-no-dot`; `a4_18_referral_no_dot_is_looking_up_server` |
| do_tgs_req.c:409 `find_alternate_tgs` | walk_realm_tree finds no intermediate TGS | UNKNOWN_SERVER 7 | issue.rs find_alternate_tgs:1878; store.rs walk_realm_instances | `UNKNOWN_SERVER` 7 when the tree has no hop; otherwise the last existing `krbtgt/<hop>@local`. Hierarchical list is MIT `rtree_hier_realms` (client + hops + dest, including `common == 0`); transit still uses intermediates only | exact | `a4_18_alternate_tgs_issues_near_hop`; `a4_18_alternate_tgs_without_hop_is_unknown_server`; `f4_hier_alternate_issues_sub_realm`; `f4_hier_common_zero_issues_org_hop`; diffsend `tgs-alternate-tgs-hierarchical`; `scripts/capaths-transit-gate.sh` `MIT_alternate_tgs` / `RUST_alternate_tgs` |
| do_tgs_req.c:483 `find_referral_tgs` | host-based DNS referral | may issue `krbtgt/other` or fall through LOOKING_UP_SERVER | issue.rs find_referral_tgs:1848 | FQDN + `[domain_realm]` issues `krbtgt/<other>`; no-dot / numeric (`k5_is_numeric_address`) / own-realm is LOOKING_UP_SERVER 7 | exact | `a4_18_host_fqdn_canonicalize_issues_referral`; `a4_18_referral_no_dot_is_looking_up_server`; `f4_referral_numeric_ipv4_is_looking_up_server`; diffsend `tgs-referral-no-dot`; `scripts/capaths-transit-gate.sh` `MIT_host_referral` / `RUST_host_referral` |
| do_tgs_req.c:680-682 | `is_referral` = cross TGS **and** resolved ≠ requested (`krb5_principal_compare` ignores name-type) | n/a (flag) | issue.rs tgs_issuing_referral | same; host-based substitution sets the flag; explicit `krbtgt/OTHER` is not a referral even when name-types differ | exact | diffsend `s4u2self-krbtgt-other`; `a2_r17_explicit_cross_tgs_is_server_mismatch`; `f4_explicit_cross_tgs_keeps_request_name_type` |
| kdc_util.c:1322 | PA-FOR-USER decode fail | DECODE_PA_FOR_USER (ASN.1 code→60 often) | krb5-kdc/ad.rs process_s4u2self_req | `DECODE_PA_FOR_USER` 60 | exact | diffsend `pa-for-user-undecodable`; `s4u2self_for_user_undecodable_is_generic` |
| kdc_util.c:1328 | PA-FOR-USER checksum fail | INVALID_S4U2SELF_CHECKSUM 41 on verify failure / 50 unkeyed (`kdc_util.c:1245,1297`) | krb5-kdc/ad.rs verify_for_user_checksum | `INVALID_S4U2SELF_CHECKSUM` 41 / 50 unkeyed | exact | `s4u2self_bad_checksum_rejected`; diffsend `pa-s4u-x509-user-bad-checksum` |
| kdc_util.c:1424 | PA-S4U-X509-USER decode | DECODE_PA_S4U_X509_USER | krb5-kdc/ad.rs process_s4u_x509_user | `DECODE_PA_S4U_X509_USER` 60 | exact | diffsend `pa-s4u-x509-user-truncated`; `a2_r17_truncated_x509_is_decode` |
| kdc_util.c:1435 | PA-S4U-X509-USER checksum | INVALID_S4U2SELF_CHECKSUM 41 on verify failure / 50 unkeyed | krb5-kdc/ad.rs process_s4u_x509_user | `INVALID_S4U2SELF_CHECKSUM` 41 / 50 unkeyed | exact | diffsend `pa-s4u-x509-user-bad-checksum`; diffsend `pa-s4u-x509-user-nonce` |
| kdc_util.c:1443 | empty user and empty cert | INVALID_S4U2SELF_REQUEST 6 | krb5-kdc/ad.rs process_s4u_x509_user | `INVALID_S4U2SELF_REQUEST` 6 | exact | diffsend `pa-s4u-x509-user-empty`; `s4u2self_x509_empty_is_invalid_request` |
| kdc_util.c:1605 | local S4U user KDB NOENTRY | UNKNOWN_S4U2SELF_PRINCIPAL 6 | krb5-kdc/ad.rs s4u_from_userid | `UNKNOWN_S4U2SELF_PRINCIPAL` 6 | exact | `s4u2self_unknown_for_user_is_refused`; `scripts/s4u-mit-gate.sh` |
| kdc_util.c:1608 | local S4U user lookup other error (cert-only `get_s4u_x509_principal` NOTSUPP on db2) | LOOKING_UP_S4U2SELF_PRINCIPAL 60 | krb5-kdc/ad.rs s4u_from_userid | `LOOKING_UP_S4U2SELF_PRINCIPAL` 60 | exact | `s4u2self_x509_cert_only_local_is_looking_up`; diffsend `s4u2self-cert-only` |
| do_tgs_req.c:283 | 2nd ticket server lookup/key (`kdc_get_server_key` on `ticket->server`, realm included) | 2ND_TKT_SERVER (7 typical; no-key/unknown etype → 60) | issue.rs decrypt_2ndtkt | U2U and S4U2Proxy share `decrypt_2ndtkt`; lookup is `ticket.sname@ticket.realm` (no local-realm `fetch_name`); missing/`DISALLOW` second-ticket server is **7** `2ND_TKT_SERVER`; no key of the ticket etype and unknown etype 99 are **60** `2ND_TKT_SERVER`; mismatched kvno (99) is **60** `2ND_TKT_SERVER`; absent kvno = 0 = any (`kdc_util.c:371-372`). rust `kadmin-local` `addprinc` stores the local realm (`parse_name` drops a foreign realm); dump/load/iprop still round-trip a foreign-realm id from `create_interrealm` | exact | `u2u_missing_second_ticket_server_is_2nd_tkt_server`; `a2_r16.rs` `a2_r16_u2u_second_ticket_foreign_realm_is_7`; diffsend `u2u-2nd-ticket-unknown-server`; diffsend `u2u-2nd-ticket-foreign-realm`; diffsend `u2u-2nd-ticket-bad-etype`; diffsend `u2u-2nd-ticket-kvno-miss`; diffsend `u2u-2nd-ticket-disallow-svr` |
| do_tgs_req.c:288 | 2nd ticket decrypt fail | 2ND_TKT_DECRYPT 31 typical | issue.rs decrypt_2ndtkt | U2U and S4U2Proxy corrupt cipher is **31** `2ND_TKT_DECRYPT` | exact | diffsend `u2u-2nd-ticket-corrupt`; `r13_u2u_status.rs` |
| do_tgs_req.c:294 | 2nd ticket PAC verify fail | 2ND_TKT_PAC 41 / 60 | issue.rs decrypt_2ndtkt; ad.rs get_verified_pac | `2ND_TKT_PAC` 41 `MODIFIED` / 60 `GENERIC` (wrong server cksumtype on a TGT second ticket) | exact | diffsend `u2u-2nd-ticket-bad-pac`; diffsend `u2u-2nd-ticket-pac-wrong-enctype`; `u2u_bad_pac_is_modified`; `a2_r23_u2u_stkt_pac_wrong_cksumtype_is_generic` |
| do_tgs_req.c:319 | 2nd ticket session etype invalid | BAD_ETYPE_IN_2ND_TKT 14 | issue.rs get_2ndtkt_enctype | `BAD_ETYPE_IN_2ND_TKT` 14 | exact | diffsend `u2u-bad-etype`; `u2u_bad_session_etype_is_etype_nosupp` |
| do_tgs_req.c:347-355 | U2U reply session: stkt etype if the client offered it, else `select_session_keytype` | n/a (success) | issue.rs get_2ndtkt_enctype; issue.rs issue_tgs_body:1191-1194 | offered → stkt etype; omitted → `select_session_keytype` | exact | diffsend `u2u-success` `reply_session_etype:17`; diffsend `u2u-success-offered` `reply_session_etype:18` |
| do_tgs_req.c:997-1000,1056-1057 | U2U issued ticket key = stkt session; `enc_part.kvno = 0` | n/a (success) | issue.rs u2u_from_stkt; issue.rs issue_tgs_body:1195-1201 | ticket etype = stkt session; kvno absent | exact | diffsend `u2u-success` (distinct stkt session); `u2u_host_tgt_issues_kvno_zero` |
| do_tgs_req.c:737-758,984-992 | cross S4U2Proxy identity from PAC (`get_pac_princ_with_realm`); `tkt_client` / subject switch to evidence | RBCD_PAC_PRINC 13 on PAC client without realm | issue.rs issue_tgs_body; ad.rs rbcd_pac_client | PAC realm is the issued crealm; client info on a final S4U ticket omits realm | exact | `a2_r18_cross_pac_without_realm_is_rbcd_pac_princ`; `a2_r18_cross_issues_pac_client_and_realm` |
| do_tgs_req.c:740 | cross S4U2Proxy PAC client extract fail | RBCD_PAC_PRINC 13 | ad.rs rbcd_pac_client | `RBCD_PAC_PRINC` 13 | exact | `a2_r18_cross_pac_without_realm_is_rbcd_pac_princ` |
| do_tgs_req.c:787-788 | transited vs `tkt_client` realm (not header crealm) | BAD_TRANSIT 12 when the PAC realm is not in capaths | issue.rs issue_tgs_body | `tkt_client_realm` is the PAC/evidence realm for S4U2Proxy | exact | `a2_r18_cross_tkt_client_realm_is_transited` |
| do_tgs_req.c:767 | `get_auth_indicators` fail | GET_AUTH_INDICATORS (varies) | ad.rs get_auth_indicators | `GET_AUTH_INDICATORS` **60** on undecodable CAMMAC | exact | diffsend `tgs-truncated-cammac`; `a3_14.rs` `tgs_truncated_cammac_is_get_auth_indicators` |
| do_tgs_req.c:792 | header transited `tr_type != 1` on add path | VALIDATE_TRANSIT_TYPE 17 | issue.rs issue_tgs_body | `VALIDATE_TRANSIT_TYPE` 17 | exact | `transited_add_path_type_and_ill_formed` |
| do_tgs_req.c:799 | `add_to_transited` fail | ADD_TO_TRANSITED_LIST 43 (`ILL_CR_TKT`; kdc_transit.c:214+) | issue.rs issue_tgs_body | `ADD_TO_TRANSITED_LIST` 43 | exact | `transited_add_path_type_and_ill_formed`; `capaths-compress-gate.sh` |
| kdc_transit.c:143 | add-path realm compress/escape | MIT encoder (no `\,` escape) | transited append | escapes `\` `,`; uncompressed X.500 | stricter-documented | docs/security.md Append escaping / Encode-side X.500; `capaths-compress-gate.sh` |
| kdc_util.c:1647 `kdc_check_transited_list` | transited comma/hop caps | MIT checker no 256-comma / 4096-hop cap | krb5-types/lib.rs realms_for (TooManyFields) | POLICY (not a MIT status) | stricter-documented | docs/security.md Transited field-count/hop-emission cap; `capaths.rs` |
| do_tgs_req.c:945 | `reject_bad_transit` and T not set | BAD_TRANSIT 12 | issue.rs issue_tgs_body | `BAD_TRANSIT` 12 | exact | `capaths.rs` BAD_TRANSIT; `capaths-transit-gate.sh` skip cells |
| tgs_policy.c:66 | `FORWARDED` without TKT `FORWARDABLE` | TGT NOT FORWARDABLE 13 | issue.rs check_tgs_constraints_skeleton | `TGT NOT FORWARDABLE` 13 | exact | `tgs_forwarded_without_forwardable_is_tgt_not_forwardable` |
| tgs_policy.c:68 | `PROXY` without TKT `PROXIABLE` | TGT NOT PROXIABLE 13 | issue.rs check_tgs_constraints_skeleton | `TGT NOT PROXIABLE` 13 | exact | `tgs_proxy_without_proxiable_is_tgt_not_proxiable` |
| tgs_policy.c:70 | `ALLOW_POSTDATE`/`POSTDATED` without TKT `MAY_POSTDATE` | TGT NOT POSTDATABLE 13 | issue.rs check_tgs_constraints_skeleton:1940-1944 | `TGT NOT POSTDATABLE` 13 | exact | diffsend `tgs-postdate-on-non-postdatable`; `a3_11.rs` `tgs_postdate_without_may_postdate_is_tgt_not_postdatable` |
| tgs_policy.c:72 | `VALIDATE` without TKT `INVALID` | VALIDATE VALID TICKET 13 | issue.rs check_tgs_constraints_skeleton:1944-1946 | `VALIDATE VALID TICKET` 13 | exact (unit) | `tgs_renew_and_validate_together_is_badoption` (Rust-only combo) |
| tgs_policy.c:74 | `RENEW` without TKT `RENEWABLE` | TICKET NOT RENEWABLE 13 | issue.rs check_tgs_constraints_skeleton:1947-1949 | `TICKET NOT RENEWABLE` 13 | exact | diffsend `tgs-validate-invalid-non-renewable`; `a3_r26.rs` `tgs_renew_invalid_non_renewable_is_ticket_not_renewable`; `tgs_renew_non_renewable_is_badoption` |
| tgs_policy.c:100 | TKT `INVALID` and not `VALIDATE` | TICKET NOT VALID 33 | issue.rs check_tgs_constraints_skeleton:1950-1952 | `TICKET NOT VALID` 33 | exact | diffsend `tgs-postdated-is-invalid`; `as_postdated_is_invalid_until_validate` |
| tgs_policy.c:231 | `VALIDATE` and starttime > now | NOT_YET_VALID 33 | issue.rs check_tgs_constraints_skeleton:1953-1959 | `NOT_YET_VALID` 33 (no skew) | exact | diffsend `tgs-nyv-inside-skew`; `tgs_validate_future_starttime_is_not_yet_valid` |
| tgs_policy.c:241 | `RENEW` and now > `renew_till` | TKT_EXPIRED 32 | issue.rs check_tgs_constraints_skeleton:1960-1969 | `TKT_EXPIRED` 32 | deviation | `tgs_renew_rejects_renew_till_not_after_now` (code); proposed: diffsend e_text |
| tgs_policy.c:636 | `NON_TGT_OPTION` and `krb5_principal_compare(tkt->server, req->server)` (name **and** realm) | SERVER DIDN'T MATCH TICKET FOR RENEW/FORWARD/ETC 26 | issue.rs check_tgs_constraints_skeleton | name **or** realm mismatch is **26** `SERVER DIDN'T MATCH TICKET FOR RENEW/FORWARD/ETC` | exact | `tgs_renew_wrong_sname_is_badoption`; `a2_r16.rs` `a2_r16_cross_tgt_renew_realm_mismatch_is_26`; `scripts/capaths-transit-gate.sh` `MIT_renew_server_mismatch`; diffsend `tgs-renew-service-ticket` |
| tgs_policy.c:642 | `PROXY` and req.server is TGS | CAN'T PROXY TGT 13 | issue.rs check_tgs_constraints_skeleton | `CAN'T PROXY TGT` 13 | exact | `tgs_proxy_krbtgt_is_cant_proxy_tgt`; diffsend `tgs-proxy-krbtgt` |
| tgs_policy.c:657 | header server not TGS princ (normal TGS) | BAD TGS SERVER NAME 35 | issue.rs check_tgs_constraints_skeleton | `BAD TGS SERVER NAME` 35 (header cname) | exact | `tgs_not_a_tgt_decrypts_and_names_client`; diffsend `tgs-not-a-tgt` `check_client=true` |
| tgs_policy.c:662 | TGS instance ≠ `req.server.realm` | BAD TGS SERVER INSTANCE 35 | issue.rs check_tgs_constraints_skeleton:1982-1984 | `BAD TGS SERVER INSTANCE` 35 | deviation | proposed: diffsend `krbtgt/OTHER` header vs local body.realm |
| tgs_policy.c:275 | S4U2Self local server ≠ header client (`is_client_db_alias`) | INVALID_S4U2SELF_REQUEST_SERVER_MISMATCH 36 | issue.rs issue_tgs_body | `INVALID_S4U2SELF_REQUEST_SERVER_MISMATCH` 36 | exact | `s4u2self_user_tgt_host_sname_is_badmatch`; `s4u2self_cross_tgt_foreign_client_named_like_local_server_is_badmatch`; `s4u-mit-gate.sh`; `capaths-transit-gate.sh` S4U collision |
| tgs_policy.c:281 | S4U2Self `AS_INVALID_OPTIONS` | INVALID S4U2SELF OPTIONS 13 | issue.rs check_tgs_s4u2self | `INVALID S4U2SELF OPTIONS` 13 | exact | diffsend `s4u2self-renew-options` (ENC_TKT_IN_SKEY; RENEW is `check_tgs_nontgt` first); `a2_r17_s4u2self_u2u_is_invalid_options` |
| tgs_policy.c:298 | S4U2Self local TGT + referral (substitution only) | LOOKING_UP_SERVER 7 | issue.rs check_tgs_s4u2self | `LOOKING_UP_SERVER` 7 when `is_referral`; explicit `krbtgt/OTHER` is 36 | exact | diffsend `s4u2self-krbtgt-other`; `a2_r17_explicit_cross_tgs_is_server_mismatch` |
| tgs_policy.c:307 | S4U2Self local user + cross TGT + not referral | NOT_CROSS_REALM_REQUEST 6 | issue.rs check_tgs_s4u2self | `NOT_CROSS_REALM_REQUEST` 6 | exact | `s4u2self_cross_tgt_local_user_local_server_is_not_cross_realm` |
| tgs_policy.c:316 | S4U2Self foreign user + local TGT | S4U2SELF_CLIENT_NOT_OURS 12 | issue.rs check_tgs_s4u2self | `S4U2SELF_CLIENT_NOT_OURS` 12 | exact | `s4u2self_local_tgt_foreign_user_is_not_ours` |
| tgs_policy.c:325 | S4U2Self foreign + empty user name (cert-only) | INVALID_XREALM_S4U2SELF_REQUEST 12 | issue.rs check_tgs_s4u2self | `INVALID_XREALM_S4U2SELF_REQUEST` 12 | exact | `s4u2self_cross_tgt_cert_only_empty_name_is_invalid_xrealm` |
| tgs_policy.c:331 | S4U2Self header PAC missing | S4U2SELF_NO_PAC 20 | issue.rs check_tgs_s4u2self | `S4U2SELF_NO_PAC` 20 | exact | diffsend `s4u2self-no-pac`; `s4u2self_no_pac_is_tgt_revoked` |
| tgs_policy.c:339 | S4U2Self local: PAC not impersonator | S4U2SELF_LOCAL_PAC_CLIENT 13 | issue.rs check_tgs_s4u2self | `S4U2SELF_LOCAL_PAC_CLIENT` 13 | exact | diffsend `s4u2self-pac-client-mismatch`; `s4u2self_local_pac_mismatch_is_badoption` |
| tgs_policy.c:352 | S4U2Self foreign: PAC not subject+realm | S4U2SELF_FOREIGN_PAC_CLIENT 13 | issue.rs check_tgs_s4u2self:2945 | `S4U2SELF_FOREIGN_PAC_CLIENT` 13 | deferred | `a2_r17_foreign_pac_client` (unit only; MIT-kvno/diffsend needs incoming-trust key — dump lab has none; promotion: A′-3 incoming-trust cell) |
| tgs_policy.c:345 `validate_as_request` | S4U2Self local client expired | CLIENT EXPIRED 1 | issue.rs check_db_times | `CLIENT EXPIRED` 1 | exact | `s4u2self_expired_for_user_is_name_exp` (code) |
| kdc_util.c:779 via tgs_policy.c:345 | S4U2Self local client `DISALLOW_ALL_TIX` | CLIENT LOCKED OUT 18 | issue.rs validate_as_request | `CLIENT LOCKED OUT` 18 | exact | `s4u2self_disabled_for_user_is_revoked` |
| kdc_util.c:743 via tgs_policy.c:345 | S4U2Self pw expired / needchange | MIT clears pw_expire+needchange (kdc_util.c:1612) | krb5-kdc/ad.rs s4u_from_userid | n/a (clears `pw_expire` + `REQUIRES_PWCHANGE`) | exact | `s4u2self_pw_expired_user_still_issues`; `scripts/s4u-mit-gate.sh` expired cell |
| tgs_policy.c:255 | cross TGT + client realm == server realm; skip S4U2Self; before U2U (`:704-710`) | INVALID LINEAGE 12 | issue.rs issue_tgs_body after `decrypt_2ndtkt` | `INVALID LINEAGE` 12 before `check_tgs_u2u` | exact | `tgs_lineage_local_user_on_foreign_tgt_is_policy`; `a2_r19_lineage_before_u2u`; `capaths-transit-gate.sh` lineage |
| tgs_policy.c:580 | U2U without 2nd ticket | NO_2ND_TKT 13 | issue.rs check_tgs_u2u | `NO_2ND_TKT` 13 | exact | diffsend `u2u-no-2nd-tkt`; `u2u_no_2nd_tkt_is_badoption` |
| tgs_policy.c:587 | U2U 2nd ticket not local TGS | 2ND_TKT_NOT_TGS 12 | issue.rs check_tgs_u2u | `2ND_TKT_NOT_TGS` 12 | exact | diffsend `u2u-2nd-ticket-not-tgs`; `u2u_service_ticket_is_not_tgs` |
| tgs_policy.c:593 | U2U 2nd ticket client ≠ requested server | 2ND_TKT_MISMATCH 26 | issue.rs check_tgs_u2u | `2ND_TKT_MISMATCH` 26 | exact | diffsend `u2u-2nd-ticket-mismatch`; `u2u_admin_tgt_for_host_is_mismatch` |
| tgs_policy.c:432 / do_tgs_req.c:731 | S4U2Proxy without 2nd ticket | `check_tgs_s4u2proxy` would be NO_2ND_TKT 13; gather `kau_make_tkt_id(NULL)` is EINVAL → 60 UNKNOWN_REASON first | issue.rs issue_tgs_body | `UNKNOWN_REASON` 60 | exact | diffsend `s4u2proxy-no-2nd-tkt`; `s4u2proxy_no_2nd_tkt_is_unknown_reason` |
| tgs_policy.c:436 | evidence not forwardable | EVIDENCE_TKT_NOT_FORWARDABLE 13 | ad.rs check_tgs_s4u2proxy | `EVIDENCE_TKT_NOT_FORWARDABLE` 13 | exact | diffsend `s4u2proxy-not-forwardable`; `s4u2proxy_rejects_non_forwardable_evidence` |
| tgs_policy.c:443 | S4U2Proxy + NON_TGT_OPTION or ENC_TKT_IN_SKEY | INVALID_S4U2PROXY_OPTIONS 13 | ad.rs check_tgs_s4u2proxy | `INVALID_S4U2PROXY_OPTIONS` 13 (U2U combo; RENEW is `check_tgs_nontgt` first) | exact | diffsend `s4u2proxy-u2u-combo`; `s4u2proxy_u2u_combo_is_invalid_options` |
| tgs_policy.c:449 | S4U2Proxy target is TGS princ | NOT_ALLOWED_TO_DELEGATE 12 | ad.rs check_tgs_s4u2proxy | `NOT_ALLOWED_TO_DELEGATE` 12 | exact | diffsend `s4u2proxy-tgs-target`; `s4u2proxy_tgs_target_is_policy`; `scripts/s4u-mit-gate.sh` `kvno -U user -P krbtgt` |
| tgs_policy.c:455 | S4U2Proxy header PAC missing | S4U2PROXY_NO_HEADER_PAC 20 | ad.rs check_tgs_s4u2proxy | `S4U2PROXY_NO_HEADER_PAC` 20 | exact | diffsend `s4u2proxy-no-header-pac`; `s4u2proxy_no_header_pac_is_tgt_revoked` |
| tgs_policy.c:460 | S4U2Proxy header PAC not impersonator | S4U2PROXY_HEADER_PAC 13 | ad.rs check_tgs_s4u2proxy | `S4U2PROXY_HEADER_PAC` 13 | exact | diffsend `s4u2proxy-header-pac` |
| tgs_policy.c:472 | S4U2Proxy evidence PAC missing | S4U2PROXY_NO_STKT_PAC 41 | ad.rs check_tgs_s4u2proxy | `S4U2PROXY_NO_STKT_PAC` 41 | exact | diffsend `s4u2proxy-no-stkt-pac`; `s4u2proxy_no_stkt_pac_is_modified` |
| tgs_policy.c:480 | same-realm evidence server ≠ header client | EVIDENCE_TICKET_MISMATCH 26 | ad.rs check_tgs_s4u2proxy | `EVIDENCE_TICKET_MISMATCH` 26 | exact | diffsend `s4u2proxy-evidence-mismatch`; `s4u2proxy_evidence_mismatch_is_server_nomatch` |
| tgs_policy.c:488 | same-realm evidence PAC ≠ evidence client | S4U2PROXY_LOCAL_STKT_PAC 13 | ad.rs check_tgs_s4u2proxy | `S4U2PROXY_LOCAL_STKT_PAC` 13 | exact | diffsend `s4u2proxy-local-stkt-pac` |
| tgs_policy.c:501-503 | cross evidence client ≠ header client (`krb5_principal_compare`, name and realm) or not a referral TGT to us | XREALM_EVIDENCE_TICKET_MISMATCH 13 | ad.rs check_tgs_s4u2proxy | `XREALM_EVIDENCE_TICKET_MISMATCH` 13 | exact | `a2_r18_cross_stkt_realm_mismatch_is_xrealm` |
| tgs_policy.c:512 + :365 `verify_deleg_pac` | cross evidence PAC deleg info | S4U2PROXY_CROSS_STKT_PAC 13 | ad.rs check_tgs_s4u2proxy; ad.rs verify_deleg_pac | `S4U2PROXY_CROSS_STKT_PAC` 13 | exact | `a2_r18_cross_issues_pac_client_and_realm`; `scripts/s4u-mit-gate.sh` `MIT_testkdb_xrealm_rbcd` / `RUST_testkdb_xrealm_rbcd` |
| tgs_policy.c:541 | referral S4U2Proxy without PA-PAC-OPTIONS RBCD | UNSUPPORTED_S4U2PROXY_REQUEST 13 | ad.rs check_s4u2proxy_policy:1047 | `UNSUPPORTED_S4U2PROXY_REQUEST` 13 when `is_referral` and no RBCD; RBCD issues the referral TGT | exact | `a4_18_s4u2proxy_referral_without_rbcd_is_unsupported`; `a4_18_s4u2proxy_referral_with_rbcd_issues` |
| tgs_policy.c:569 | KDB deny RBCD/classic | NOT_ALLOWED_TO_DELEGATE 13 (same string as :449 but 13 not 12) | ad.rs check_s4u2proxy_policy | empty `s4u_allowed_to`/`from` is deny **13** `NOT_ALLOWED_TO_DELEGATE`; MIT db2 hooks are NULL → `UNSUPPORTED_S4U2PROXY_REQUEST` 13 | exact (our store); deviation (MIT db2 e_text) | `scripts/s4u-mit-gate.sh` `MIT_db2_s4u2proxy_nogrant`; `MIT_testkdb_s4u2proxy_rbcd_deny` |
| tgs_policy.c:753-756 | S4U2Proxy impersonator is header `ticket->enc_part2->client` (name and realm) | n/a (hook arg) | ad.rs check_s4u2proxy_policy / allowed_to_delegate_from | RBCD compare is `unparse_with_realm`; classic `s4u_allowed_to` stays realm-less | exact | `a2_r22_foreign_impersonator_vs_local_grant_is_not_allowed`; `a2_r22_realm_qualified_foreign_grant_matches`; `scripts/s4u-mit-gate.sh` `MIT_testkdb_xrealm_rbcd` / `MIT_testkdb_xrealm_rbcd_wrong_realm` / `RUST_testkdb_xrealm_rbcd` / `RUST_testkdb_xrealm_rbcd_wrong_realm` |
| kdb_test.c:761-774 | test-KDB RBCD `match_in_table` uses `krb5_unparse_name` with realm | BADOPTION 13 on miss | store.rs allow_s4u_from; ad.rs allowed_to_delegate_from | entries stored `name@REALM`; bare name qualified at insert | exact | `scripts/s4u-mit-gate.sh` `MIT_testkdb_s4u2proxy_rbcd`; `MIT_testkdb_s4u2proxy_rbcd_deny` |
| tgs_policy.c:108 | `RENEWABLE` + server `DISALLOW_RENEWABLE` | NON-RENEWABLE TICKET 12 | issue.rs check_tgs_policy_flags:3156 | `NON-RENEWABLE TICKET` 12 | exact | `tgs_renewable_when_server_disallow_is_non_renewable`; `a3_11.rs` `tgs_renewable_against_disallow_is_non_renewable`; `scripts/renew-gate.sh` `NON-RENEWABLE` both legs |
| tgs_policy.c:110 | `ALLOW_POSTDATE` + server `DISALLOW_POSTDATED` | NON-POSTDATABLE TICKET 10 | issue.rs check_tgs_policy_flags:3156 | `NON-POSTDATABLE TICKET` 10 on `MAY_POSTDATE` only | exact | `tgs_service_deny_opts_precedes_deny_all`; `a3_11.rs` `tgs_postdated_bit_alone_does_not_deny_postdate` |
| tgs_policy.c:112 | `ENC_TKT_IN_SKEY` + `DISALLOW_DUP_SKEY` in `check_tgs_svc_deny_opts` (`:746`, after constraints) | DUP_SKEY DISALLOWED 12 (before deny_all TGT BASED) | issue.rs check_tgs_policy_flags deny_opts | `DUP_SKEY DISALLOWED` 12 before `TGT BASED NOT ALLOWED` | exact | `u2u_dup_skey_disallowed_is_policy`; `a2_r19_dup_skey_beats_tgt_based`; diffsend `u2u-dup-skey-tgt-based` |
| tgs_policy.c:149 | server `DISALLOW_ALL_TIX` | SERVER LOCKED OUT 7 | issue.rs check_tgs_policy_flags | `SERVER LOCKED OUT` 7 | exact | `tgs_honors_svr_tgt_based_lockout_and_ok_as_delegate` |
| tgs_policy.c:154 | `DISALLOW_SVR` and not U2U | SERVER NOT ALLOWED 27 | issue.rs check_tgs_policy_flags | `SERVER NOT ALLOWED` 27 | exact | `tgs_honors_svr_tgt_based_lockout_and_ok_as_delegate`; `flags-gate.sh` |
| tgs_policy.c:159 | `DISALLOW_TGT_BASED` and header is TGS | TGT BASED NOT ALLOWED 12 | issue.rs check_tgs_policy_flags | `TGT BASED NOT ALLOWED` 12 | exact | `tgs_for_changepw_with_tgt_is_tgt_based_not_allowed`; `kpasswd-gate.sh` |
| tgs_policy.c:176 | server `REQUIRES_HW_AUTH` without TKT HW | NO HW PREAUTH 60 | issue.rs check_tgs_policy_flags | `NO HW PREAUTH` 60 | exact | diffsend `tgs-hw-preauth-flag`; `tgs_requires_hw_auth_without_hw_flag` |
| tgs_policy.c:182 | server `REQUIRES_PRE_AUTH` without TKT preauth | NO PREAUTH 60 | issue.rs check_tgs_policy_flags | `NO PREAUTH` 60 | exact | diffsend `tgs-no-preauth-flag`; `a3_12.rs` `tgs_requires_preauth_without_pa_flag_is_no_preauth` |
| tgs_policy.c:194 | server expiration < now | SERVICE EXPIRED 2 | issue.rs check_tgs_policy_flags:3156; issue.rs check_db_times:3040 | `SERVICE EXPIRED` 2 before `check_indicators` | exact | `tgs_rejects_expired_server`; `a3_r27.rs` `r27_tgs_expired_server_beats_require_auth`; diffsend `tgs-service-expired-require-auth` |
| tgs_policy.c:763 `check_anon` kdc_util.c:703 | `restrict_anon` + anon client + non-local-TGS server | ANONYMOUS NOT ALLOWED 12 | issue.rs `check_anon` after S4U2Proxy | `ANONYMOUS NOT ALLOWED` 12 | exact | `a4_16_restrict_anon_tgs_to_host`; `pkinit-gate.sh` / `rust-kinit-pkinit-gate.sh` `kvno` after `kinit -n` |
| tgs_policy.c:768 | `krb5_db_check_policy_tgs` | plugin status + code | plugins.rs run_as_preauth `check_tgs`; issue.rs process_tgs_header | default allow; DenyPolicy `kdcpolicy` 12 | deferred | plugin-not-KDB; promote DenyPolicy unit vs MIT kdcpolicy module |
| ORDER tgs_policy.c:746 vs :768 | svc_policy then S4U2Proxy policy then anon then KDB | MIT last is KDB | issue.rs issue_tgs_body `check_tgs` after `check_indicators` | plugin after svc_policy / indicators (DenyPolicy `kdcpolicy` 12) | exact | `deny_policy_blocks_issue_as_and_issue_tgs`; `a3_14.rs` |
| ORDER tgs_policy.c:682 vs :746 | opts/times/nontgt/s4u/lineage/u2u/pac **before** svc_policy (`do_tgs_req.c:872-882`) | HEADER_PAC / DUP_SKEY / INVALID LINEAGE before LOCKED / TGT BASED | issue.rs issue_tgs_body constraints then `check_tgs_policy_flags` | same slot order; plugin-before-fetch stays a deviation (row above) | exact | diffsend `tgs-locked-pac-mismatch`; diffsend `u2u-dup-skey-tgt-based`; `a2_r19_lineage_before_u2u` |
| do_tgs_req.c:898 | `check_indicators` require-auth | HIGHER_AUTHENTICATION_REQUIRED 12 | ad.rs check_indicators; issue.rs issue_tgs_body | HIGHER_AUTHENTICATION_REQUIRED **12** (skipped for S4U2Self) | exact | `a3_14.rs` `tgs_require_auth_is_higher_authentication`; `scripts/cross-kdc-gate.sh` password `kvno` after `setstr require_auth` (`KDC policy rejects request` + KDC log word both legs); SPAKE pair and PKINIT/EC positive cells |
| do_tgs_req.c:358 `gen_session_key` | no session etype intersection | BAD_ENCRYPTION_TYPE 14 | krb5-kdc/issue.rs select_session_keytype on the service | `BAD_ENCRYPTION_TYPE` **14** | exact | `issue_acl_ap.rs::tgs_session_enctypes_attr_is_membership`; diffsend `as-session-enctype` |
| do_tgs_req.c:1006 | `get_first_current_key` fail | FINDING_SERVER_KEY (often 60) | issue.rs issue_tgs_body | `FINDING_SERVER_KEY` **60** | exact | `as_no_server_key_is_finding_server_key` (AS twin); TGS uses the same `first_current_key` miss |
| do_tgs_req.c:1046 | `handle_authdata` fail | HANDLE_AUTHDATA (varies) | ad.rs handle_authdata:1219; issue.rs mint_ticket:2121 | `HANDLE_AUTHDATA` 12 (mandatory) / 31 (decrypt) | exact | diffsend `tgs-ad-mandatory-for-kdc`; `a3_13.rs` `tgs_mandatory_for_kdc_is_handle_authdata`; `tgs_body_ad_decrypt_fail_is_handle_authdata` |
| do_tgs_req.c:1103 | `return_enc_padata` fail | KDC_RETURN_ENC_PADATA | — | — | deferred | none (FAST+referral enc padata oracle) |
| do_tgs_req.c:1121 | successful TGS | ISSUE 0 (log only, not KRB-ERROR) | issue.rs issue_tgs_body success | no ISSUE e_text | absent | `kdc-gate.sh` / `capaths-transit-gate.sh` success logs |
| do_tgs_req.c:1203 | status still NULL on error | UNKNOWN_REASON | issue.rs issue_tgs_from | `UNKNOWN_REASON` 60 on `msg_type != 12` (the gather path that leaves status NULL) | exact | `tgs_bad_msg_type_is_unknown_reason_without_cname`; diffsend `tgs-bad-msg-type` |
| kdc_util.c:754 AS; tgs empty_server | AS/S4U `SERVICE EXPIRED` on empty_server | not TGS svc_time | krb5-kdc/issue.rs check_db_times TGS SERVER path | n/a for empty_server | exact | TGS covered `tgs_rejects_expired_server` |
| kdc_util.c:773 | AS `POSTDATE NOT ALLOWED` | POSTDATE NOT ALLOWED 10 | issue.rs validate_as_request | `POSTDATE NOT ALLOWED` 10 | exact (AS) | AS `as_cannot_postdate_when_disallow`; TGS half is row `:276` `NON-POSTDATABLE TICKET` |
| kdc_util.c:785/791 | AS SERVICE LOCKED OUT / SERVICE NOT ALLOWED | 7 / 27 | krb5-kdc/status.rs SERVER_LOCKED_OUT; krb5-kdc/status.rs SERVER_NOT_ALLOWED; issue.rs encode_krb_error | `SERVER LOCKED OUT` / `SERVER NOT ALLOWED` on AS and TGS (MIT TGS too) | exact | TGS rows above |
| do_tgs_req.c:1012 (VALIDATE and RENEW as one mask) | RENEW and VALIDATE together | MIT no dedicated status (opts interact) | issue.rs issue_tgs_body:996 | `TICKET NOT RENEWABLE` 13 | deviation | `tgs_renew_and_validate_together_is_badoption` |
| asn1_k_encode.c:103-106 (realm decoded as octets) | TGS realm not UTF-8 | MIT uses bytes | issue.rs kdc_req_body_der | `non-ascii realm` 60 | stricter-documented | docs/security.md TGS realm octets; `tgs_non_ascii_ticket_realm_is_process_tgs` |
| do_tgs_req.c:721 `decrypt_2ndtkt` vs tgs_policy.c:710 | 2nd ticket decrypt in gather, before constraints | 2ND_TKT_* then U2U then S4U2Proxy | issue.rs decrypt_2ndtkt then check_tgs_u2u then check_tgs_s4u2proxy | same order | exact | diffsend `s4u2proxy-u2u-combo`; diffsend `u2u-2nd-ticket-not-tgs` |
| kdc_util.c:217-229 | header ticket/authenticator carries `KRB5_AUTHDATA_FX_ARMOR` (armor-only ticket used as a TGT) | PROCESS_TGS 12 (log `ticket valid only as FAST armor`) | issue.rs process_tgs_header; issue.rs fx_armor_present | PROCESS_TGS 12 (detail `ticket valid only as FAST armor`) | exact | `tgs_header_ticket_ad_fx_armor_is_policy`; `tgs_header_ticket_if_relevant_ad_fx_armor_is_policy`; `tgs_header_authenticator_ad_fx_armor_is_policy`; diffsend `armor-ap-req-as-pa-tgs-req`; diffsend `tgs-ad-fx-armor-authenticator` |
| kdc_util.c:813 `get_ticket_flags` | `OPTS2FLAGS` + `COPY_TKT_FLAGS` on the issued ticket (FORWARDED, PROXY, MAY_POSTDATE, POSTDATED+INVALID, HW_AUTH, ENC_PA_REP, ANONYMOUS) | n/a (flags, not an error) | issue.rs get_ticket_flags:3196; issue.rs kdc_get_ticket_renewtime:3261 | OPTS2FLAGS + COPY_TKT_FLAGS + ENC_PA_REP; VALIDATE/RENEW return header minus INVALID; POSTDATED→INVALID; OK_AS_DELEGATE on TGS only; RENEWABLE via `kdc_get_ticket_renewtime` | exact | diffsend `tgs-forwarded-addresses`/`tgs-forwarded-tgt-addresses` `forwarded`; diffsend `tgs-postdated-is-invalid`; `a3_11.rs`; `scripts/gss-gate.sh` delegated `klist -f` `f` both acceptors of a MIT-KDC-forwarded TGT; `scripts/renew-gate.sh` `kinit -p` → `P` |
| kdc_util.c:1712-1757 `kdc_get_ticket_renewtime` | `min` of server, realm, and client `max_renewable_life` including 0; RENEWABLE_OK only if truncated rtime exceeds end | n/a | issue.rs kdc_get_ticket_renewtime:3261; kadm5.rs dispatch_kadm5_ticket:2370 | 0 is a cap of 0 (renew_till = starttime); omitted kdc.conf max_renewable_life is 0 (alt_prof.c:576-577); kadm5 modify honours KADM5_MAX_RLIFE (svr_principal.c:642-643) | exact | `a3_r26.rs` `as_renewable_with_zero_rlife_caps_renew_till_at_start`; `kadm5_modify_sets_max_rlife`; `scripts/renew-gate.sh` `modprinc -maxrenewlife 0` / `1 day`; `scripts/kdb-dump-gate.sh` rlife0 |
| kdc_util.c:1677-1704 `kdc_get_ticket_endtime` | signed `ts_delta(until, starttime)`; `until < start` writes expired `endtime` | n/a | issue.rs kdc_get_ticket_endtime:1571 | `endtime = start + signed life` (not saturating 0) | exact (unit) | `a3_r27.rs` `r27_as_till_in_past_endtime_is_till`; diffsend `tgs-till-in-past` |
| do_tgs_req.c:694-696,770-778 | `t->client` is S4U2Self `s4u.local`; else lookup only when server lacks `NO_AUTH_DATA_REQUIRED`, realm-aware (`MAP_PRINCIPALS`) | n/a | issue.rs issue_tgs_body | times/flags/rlife use impersonated user; foreign subject → no client entry | exact | `a3_r27.rs` `r27_s4u2self_caps_endtime_at_impersonated_max_life`; `r27_s4u2self_disallow_renewable_user_has_no_r`; `scripts/s4u-mit-gate.sh` maxlife 1m / `-allow_renewable` |
| do_tgs_req.c:811-852 `compute_ticket_times`; do_as_req.c:692-695 | POSTDATED `starttime = from` unconditionally; omit when `start == authtime`; RENEW `hlife = ts_delta(endtime, hstarttime)` signed (`:836-838`); RENEW+POSTDATED uses `from` then `end = min(renew_till, from + hlife)` (`:826-827`) | n/a | issue.rs issue_as_body; issue.rs issue_tgs_body | `from` even when in the past; AS from after till issues expired end; RENEW uses signed header life (no `.max(0)`); RENEW+POSTDATED starts at `from` | exact | `a3_r27.rs` `r27_tgs_postdated_omitted_from_is_epoch`; `r27_as_from_after_till_issues_expired_end`; `a3_r32.rs` `r32_renew_header_end_before_start_is_expired`; `a4_19.rs` `a4_19_renew_postdated_starts_at_from`; diffsend `tgs-postdated-from`; diffsend `tgs-renew-header-end-before-start`; diffsend `tgs-renew-postdated-from`; `valid_times.c:43-55` |
| kdc_util.c:824 `\| TKT_FLG_ENC_PA_REP` | every issued AS/TGS ticket sets the enc-pa-rep flag (RFC 6806); the enc-pa-rep padata is separate (added only when PA 149 is present) | n/a (flag) | krb5-kdc/issue.rs issue_as_body; krb5-kdc/issue.rs issue_tgs_body | ENC_PA_REP on every ticket; padata request-keyed | exact | `every_ticket_sets_enc_pa_rep_flag_without_padata`; `scripts/differential-gate.sh` `as-success`/`tgs-success` no `mit-extra-ticket-flags` whitelist |
| do_tgs_req.c:1019-1027 | ticket addresses: `req->addresses` for FORWARDED/PROXY, else header `caddrs`; RENEW/VALIDATE copy the header enc-part; reply `caddrs` only for FORWARDED/PROXY | n/a | issue.rs tgs_ticket_caddr / tgs_reply_caddr / mint_ticket | same copy rules; AS copies `request->addresses` onto ticket and reply | exact | diffsend `tgs-forwarded-addresses`; diffsend `tgs-forwarded-tgt-addresses`; `a2_10_caddr.rs` |
| do_tgs_req.c:551-555 (`search_sprinc`) | `NO_REFERRAL_OPTION` (FORWARDED/PROXY/RENEW/VALIDATE/ENC_TKT_IN_SKEY) suppresses `KRB5_KDB_FLAG_REFERRAL_OK` | n/a (drops to LOOKING_UP_SERVER 7) | issue.rs no_referral_option:1812; issue.rs search_sprinc:1898 | RENEW/FORWARDED/PROXY/VALIDATE/ENC_TKT_IN_SKEY skip referral and alternate; miss is LOOKING_UP_SERVER 7 | exact | `a4_18_renew_skips_alternate_tgs`; diffsend `tgs-canonicalize-renew`; diffsend `tgs-referral-no-dot` |
| do_tgs_req.c:919 `check_kdcpolicy_tgs` | kdcpolicy module deny / times rewrite (distinct from `krb5_db_check_policy_tgs`) | module `*status` + 12 | plugins.rs check_tgs:697; plugins.rs output_from_indicator; issue.rs issue_tgs_body | `LOCAL_POLICY` **12**; ONE_HOUR/SEVEN_HOURS rewrite (TGS divisor 2); other indicator 12 | exact | `a4_19b.rs` `a4_19_test_policy_one_hour_rewrites_endtime`; `scripts/kdcpolicy-gate.sh` `MIT_tgs_fail` / `RUST_tgs_fail` / `MIT_one_hour_tgs` / `RUST_one_hour_tgs` |
| do_tgs_req.c:686 | `is_crossrealm` = header-server-entry realm ≠ canonical server realm | n/a | issue.rs tgs_header_is_crossrealm | header ticket server realm ≠ canonical server realm | exact | `is_crossrealm_is_header_realm_vs_server_realm` |
| ORDER tgs_policy.c:652-665 vs do_tgs_req.c:617 | `check_tgs_tgt` runs inside `check_tgs_constraints` (after PROCESS_TGS/FIND_FAST/GET_LOCAL_TGT/HEADER_PAC/search_sprinc/S4U/2nd-ticket/opts/times) and **only when `NON_TGT_OPTION` is clear** | BAD TGS SERVER NAME 35 last | issue.rs check_tgs_constraints_skeleton; issue.rs non_tgt_option | `BAD TGS SERVER NAME` 35 unless forwarded/proxy/renew/validate | exact | `tgs_not_a_tgt_decrypts_and_names_client`; `tgs_renew_service_ticket_issues`; diffsend `tgs-not-a-tgt` `check_client=true`; diffsend `tgs-renew-service-ticket` |
| kdc_util.c:813-824 `OPTS2FLAGS` | any unsupported `KDCOptions` bit | MIT ignores unknown option bits (`OPTS2FLAGS` masks) | issue.rs issue_tgs_from | `unsupported KDCOptions` 13 | stricter-documented | docs/security.md:56 Unknown TGS KDCOptions bit |

## A2 — do_as_req.c / kdc_util.c / policy.c / replay.c / dispatch.c

| kdb5.c:2826-2895; kdb5.c:800-840 | `krb5_dbe_make_alias_entry` / `krb5_dbe_read_alias`; `krb5_db_get_principal` follows `KRB5_TL_ALIAS_TARGET` up to `MAX_ALIAS_DEPTH`=10 (>10 → NOENTRY), returns the canonical entry; `do_as_req.c:681-687` keeps the requested cname unless CANONICALIZE | n/a (DB layer) | krb5-kdc/store.rs alias_target, resolve_id; kdb.rs resolve_alias_id; issue.rs 313 cname | alias resolves to target; requested cname kept, `-C` canonicalizes; self/depth-11 unresolved → client not found | exact | `alias.rs` chain/self/as-cname; `scripts/kdb-dump-gate.sh:135,281` MIT↔Rust `kinit a1`/`kinit -C`/`kinit a2` both directions |

MIT 1.22.2 `src/kdc/{do_as_req,kdc_util,policy,replay,dispatch}.c` vs
`crates/krb5-kdc/src/{issue,listen,store,preauth,ad,plugins}.rs`.
Wire = RFC 4120 protocol code (MIT `errcode_to_protocol`).

| MIT file:line | check (condition) | MIT status + wire code | Rust site | Rust e_text + code | verdict | proof |
| --- | --- | --- | --- | --- | --- | --- |
| kdc_util.c:197-200; rd_req_dec.c:536-540 | TGS sender vs header `caddrs` (`k5_sockaddr_to_address` then `krb5_address_search`) | 38 `BADADDR` `PROCESS_TGS` (header cname) | issue.rs process_tgs_header `address_search` | 38 `PROCESS_TGS`, header cname | exact | diffsend `tgs-addr-mismatch` |
| addr_srch.c:55-59 | NULL list matches; a lone NetBIOS entry is empty | n/a (match) | issue.rs address_search | same | exact | `a2_10_caddr.rs` `tgs_netbios_only_caddr_matches` |
| do_as_req.c:713,245 | AS copies `request->addresses` onto the ticket and `reply_encpart.caddrs` | n/a | issue.rs mint_ticket / enc_rep_part | same | exact | `a2_10_caddr.rs` `as_copies_request_addresses` |
| rd_req_dec.c:536-540 (acceptor) | same `address_search` on AP-REQ | 38 `BADADDR` | krb5-protocol/ap_req.rs `verify_ap_req` list equality | whole-list compare when both sides present | stricter-documented | `docs/security.md` acceptor ticket addresses |
| do_as_req.c:513-517 | decoded req `msg_type != AS_REQ` | `VALIDATE_MESSAGE_TYPE` + `KRB5_BADMSGTYPE`→**60** | issue.rs issue_as_from | `VALIDATE_MESSAGE_TYPE` **60** (cname echoed, no cookie) | exact | `as_bad_msg_type_is_validate_message_type`; diffsend `as-bad-msg-type` |
| do_as_req.c:531-535 | `kdc_find_fast` fail | `FIND_FAST` + 41/60/24 | krb5-kdc/preauth.rs proto_fast; krb5-kdc/preauth.rs verify_fast_req_checksum | `FIND_FAST` **41** `MODIFIED` / **60** `GENERIC` / **12** (G3) | exact | `phase7_preauth.rs` FIND_FAST cells |
| do_as_req.c:551-554 | `request->client == NULL` | `NULL_CLIENT` **6** | `issue.rs tgs_reply` | `no cname` **6** | deviation | proposed: diffsend `as-null-cname` |
| do_as_req.c:562-565 | `request->server == NULL` | `NULL_SERVER` **7** — unreachable live (sname-less AS-REQ dies at `dispatch.c:154` and is **dropped**) | `issue.rs issue_as_body` | AS synthesizes `krbtgt`; TGS `no sname` **7** | deviation (MIT drops / unreachable single-realm) | proposed: diffsend `as-null-sname` |
| do_as_req.c:581-587 | client `KRB5_KDB_NOENTRY` | `CLIENT_NOT_FOUND` **6** (vague→**60**) | `issue.rs tgs_reply` | `unknown client` **6** | deviation | diffsend `unknown-cname`; `scripts/rust-kinit-enterprise-gate.sh` |
| do_as_req.c:588-590 | client DB err ≠ NOENTRY | `LOOKING_UP_CLIENT` + com_err | krb5-kdc/kdb.rs fetch_name:121 `Err` | `as_reply` **60** `e.to_string()` | absent | proposed: diffsend `as-client-db-err` |
| do_as_req.c:600-603 | server `KRB5_KDB_NOENTRY` | `SERVER_NOT_FOUND` **7** | `issue.rs issue_as_body` | `unknown server` **7** | deviation | diffsend `unknown-sname` |
| do_as_req.c:604-606 | AS server DB err ≠ NOENTRY | `LOOKING_UP_SERVER` + com_err | no AS analogue | — | absent | proposed: diffsend `as-server-db-err` |
| tgs_policy.c:298-302 | S4U `!cross && referral` | `LOOKING_UP_SERVER` **7** | issue.rs check_tgs_s4u2self:2922 | `LOOKING_UP_SERVER` **7** | exact | `a4_18_s4u2self_local_tgt_referral_is_looking_up_server` |
| do_as_req.c:611-615 | client/server DB *entry* realms differ (needs a multi-realm KDB) | `REFERRAL` **68** `WRONG_REALM` | `issue.rs tgs_reply` | `wrong realm` **6** (`body.realm≠store`, before lookup) — different trigger | deferred (multi-realm KDB; promotion: KDB returning different entry realms) | reachable body.realm cell is diffsend `wrong-realm` (6/6 green) |
| do_as_req.c:618-623 | `get_local_tgt` fail (AS) | `GET_LOCAL_TGT` + KDB err | issue.rs issue_as_body | `GET_LOCAL_TGT` **60** | exact | `as_missing_krbtgt_is_get_local_tgt` |
| do_tgs_req.c:649-654 | `get_local_tgt` on `sprinc->realm` | `GET_LOCAL_TGT` + KDB err | `issue.rs issue_tgs_body` / `issue.rs decrypt_2ndtkt` | `GET_LOCAL_TGT` **60** if `body.realm≠store` or the local TGT/key is missing at PAC; U2U second-ticket key miss is row 195 (`2ND_TKT_SERVER`), not this site | exact | `phase7_preauth.rs`; `capaths.rs`; `s4u2proxy_missing_local_tgt_is_get_local_tgt`; `docs/security.md:60-63` |
| kdc_util.c:727-729 | AS `kdc_options & AS_INVALID_OPTIONS` (FORWARDED/PROXY/RENEW/VALIDATE/ENC_TKT_IN_SKEY/CNAME_IN_ADDL_TKT) — and no rejection of other unknown/reserved bits | `INVALID AS OPTIONS` **13** | `issue.rs validate_as_request` tests `as_invalid_bits` only (R2-P3 dropped the extra `unsupported_bits` rejection so a reserved bit passes like MIT) | `INVALID AS OPTIONS` **13**; a reserved bit is ignored | exact | `scripts/differential-gate.sh` `as-invalid-opts` (RENEW) is 13 both legs; `as_req_with_tgs_only_option_is_invalid_as_options`; `as_request_reserved_option_bit_is_ignored_like_mit` |
| kdc_util.c:733-738 | `now > client.expiration` | `CLIENT EXPIRED` **1** (vague→**60**) | `issue.rs validate_as_request` | `CLIENT EXPIRED` **1** | exact | `issue_acl_ap.rs::as_rejects_expired_principal_before_expired_password`; `scripts/expire-gate.sh` |
| kdc_util.c:743-749 | `now > pw_expiration` && !PWCHANGE_SERVICE | `CLIENT KEY EXPIRED` **23** | `issue.rs validate_as_request` `pw_expire` | `CLIENT KEY EXPIRED` **23** | exact | `issue_acl_ap.rs::as_rejects_expired_password_unless_pwchange_service`; `scripts/expire-gate.sh` |
| kdc_util.c:762-765 | `REQUIRES_PWCHANGE` && !PWCHANGE_SERVICE | `REQUIRED PWCHANGE` **23** | `issue.rs validate_as_request` own branch after SERVICE EXPIRED | `REQUIRED PWCHANGE` **23** | exact | `scripts/differential-gate.sh` `as-needchange` (pwchgu) is `23`/`REQUIRED PWCHANGE` on both legs; `issue_acl_ap.rs::as_needchange_is_key_expired_unless_changepw` (status) |
| kdc_util.c:753-755 | `now > server.expiration` | `SERVICE EXPIRED` **2** | `issue.rs check_db_times` | `SERVICE EXPIRED` **2** | exact | `issue_acl_ap.rs::tgs_rejects_expired_server` (TGS); proposed: diffsend `as-service-expired` |
| kdc_util.c:769-774 | POSTDATE/ALLOW_POSTDATE vs DISALLOW_POSTDATED | `POSTDATE NOT ALLOWED` **10** | `issue.rs validate_as_request` | `POSTDATE NOT ALLOWED` **10** | exact | `issue_acl_ap.rs::as_cannot_postdate_when_disallow`; `scripts/postdate-gate.sh` |
| kdc_util.c:778-780 | client `DISALLOW_ALL_TIX` | `CLIENT LOCKED OUT` **18** | `issue.rs validate_as_request` (after the expiry/pwchange/postdate checks, before the failcount lockout) | `CLIENT LOCKED OUT` **18** | exact | `issue_acl_ap.rs::as_disallow_all_tix_still_client_revoked`; `scripts/policy-gate.sh` |
| kdc_util.c:784-786 | server `DISALLOW_ALL_TIX` (AS) | `SERVICE LOCKED OUT` **7** | `issue.rs validate_as_request` | `SERVICE LOCKED OUT` **7** | exact | proposed: diffsend `as-service-locked` (no e_text unit); TGS is `SERVER LOCKED OUT` `issue_acl_ap.rs:1234` |
| kdc_util.c:790-793 | server `DISALLOW_SVR` (AS) | `SERVICE NOT ALLOWED` **27** | issue.rs validate_as_request | `SERVICE NOT ALLOWED` **27** (AS has no u2u skip) | exact | `as_disallow_svr_is_service_not_allowed`; diffsend `as-service-not-allowed`; TGS `scripts/flags-gate.sh` DISALLOW_SVR |
| kdc_util.c:700-712,796-797 | `restrict_anon` && anon client && !local TGS | `ANONYMOUS NOT ALLOWED` **12** | issue.rs `check_anon` in `validate_as_request` uses `request->server` | `ANONYMOUS NOT ALLOWED` **12** | exact | `a4_16_restrict_anon_as_to_host`; `a4_16_restrict_anon_as_to_local_tgt` |
| do_as_req.c:718-734 | `REQUEST_ANONYMOUS`: named client → 13; WELLKNOWN/ANONYMOUS rewritten to `@WELLKNOWN:ANONYMOUS` and `REQUIRES_PRE_AUTH` forced before `check_padata` | `VALIDATE_ANONYMOUS_PRINCIPAL` **13** or anonymous ticket | issue.rs issue_as_body | `VALIDATE_ANONYMOUS_PRINCIPAL` **13**; anonymous issue | exact | `as_request_anonymous_from_named_client_is_validate_anonymous_principal`; `a4_16_named_anon_without_preauth_is_still_13`; `a4_16_anonymous_pkinit_issues_kx`; diffsend `as-anonymous-unsigned-authpack-named-client`; `pkinit-gate.sh` / `rust-kinit-pkinit-gate.sh` `kinit -n` |
| do_tgs_req.c (recognised options only) | TGS acts on RENEWABLE / RENEWABLE-OK / POSTDATE / FORWARDED / PROXY / ENC-TKT-IN-SKEY and ignores unknown or reserved KDCOption bits | issues the ticket | `issue.rs issue_tgs_body` refuses any bit outside the honoured set (`unsupported_bits`); the AS twin `validate_as_request` was relaxed to MIT-exact in R2-P3, the TGS twin kept fail-closed by design | `unsupported KDCOptions` BADOPTION **13** | stricter-documented (`docs/security.md` "Unknown TGS KDCOptions bit") | the AS twin proves exact (`as-request-anonymous`, `as_request_reserved_option_bit_is_ignored_like_mit`); a TGT-authenticated request carrying an unknown option is anomalous, so the TGS refuses it |
| do_as_req.c:641-648 | `select_session_keytype`==0 (server session_enctypes + permitted) | `BAD_ENCRYPTION_TYPE` **14** | krb5-kdc/issue.rs select_session_keytype on the AS server | `BAD_ENCRYPTION_TYPE` **14** | exact | `issue_acl_ap.rs::no_common_etype_is_etype_nosupp`; diffsend `as-session-enctype`; `scripts/differential-gate.sh` |
| kdc_util.c:1084-1112 | session etype vs server `session_enctypes` / AES256 default | 0 → BAD_ENCRYPTION_TYPE | krb5-kdc/issue.rs dbentry_supports_enctype (attr, else AES256-sha1, else long-term key) | membership, not client key | exact | `issue_acl_ap.rs::session_enctypes_attr_is_membership_not_client_key`; `session_enctypes_rc4_with_allow_rc4_issues_rc4_session` |
| init_ctx.c:88-97,218-232 `get_boolean`; init_ctx.c:515-530 `krb5_get_permitted_enctypes` | `allow_weak_crypto`, `allow_des3`, `allow_rc4` and `permitted_enctypes` come from `[libdefaults]` only — the KDC's own context, not `[kdcdefaults]` or a realm stanza | an rc4-only client is refused (`BAD_ENCRYPTION_TYPE` 14) when `allow_rc4` sits under `[kdcdefaults]` alone | krb5-config/lib.rs parse_kdc_libdefaults | same sections (a `[libdefaults]` inside kdc.conf counts, as in MIT's merged profile) | exact | `libdefaults_only_enctype_knobs` (krb5-config units); `scripts/rc4-session-gate.sh:342-350` MIT kinit rc4user against both KDCs prints the same `KDC has no support for encryption type` after `allow_rc4` is removed from `[libdefaults]` |
| main.c:257-261,622-626 | `kdc_ports`/`kdc_tcp_ports`/`kdc_listen` (and `reject_bad_transit`) read from `[kdcdefaults]` or a realm stanza only, never `[libdefaults]` | n/a (config) | krb5-config/lib.rs parse_kdc_libdefaults handles only the four enctype knobs, with no fallthrough to `parse_kdcdefaults` (R2-P8) | a `[kdcdefaults]` knob placed under `[libdefaults]` is ignored (default kept) | exact | `libdefaults_does_not_honour_kdcdefaults_knobs` (krb5-config units) |
| main.c:286-345 | realm stanza booleans (`restrict_anonymous_to_tgt`, `pkinit_require_freshness`, `disable_pac`, `reject_bad_transit`) win over a later `[kdcdefaults]` | n/a (config) | krb5-config/lib.rs overlay_realm_booleans | realm value kept | exact | `f6_realm_booleans_win_over_later_kdcdefaults`; `scripts/knobs-gate.sh` |
| do_as_req.c:736-741 | `select_client_key` decrypt fail | `DECRYPT_CLIENT_KEY` + decrypt err | keys already in store; no runtime decrypt | — | deferred | none (dump-time decrypt only) |
| do_as_req.c:747-751 | `kdc_fast_read_cookie` | `READ_COOKIE` | krb5-kdc/preauth.rs open_cookie | garbage/expired/wrong-client ignored (empty state); SPAKE round 2 without state is 24 | exact | `spake_garbage_cookie_is_preauth_failed`; `spake_expired_cookie_is_preauth_failed`; `spake_cookie_for_user_ignored_for_admin` |
| do_as_req.c:439-442 | `check_padata` fail | `PREAUTH_FAILED` **24** (vague→**60**) | `as_reply` crypto→**24** `preauth`; EncTs/SPAKE/PKINIT own texts | not the string `PREAUTH_FAILED` | deviation | proposed: diffsend `as-bad-enc-ts-etext`; `issue_acl_ap.rs` wrong-password **24** |
| do_as_req.c:806-808 | `prepare_error_as` sets `errpkt.client = request->client`, so the AS KRB-ERROR echoes the requested `crealm`/`cname` | no status (reply fields) | issue.rs encode_krb_error `crealm`/`cname` from the request body; hide-client replaces them | AS crealm/cname echoed; FAST hide-client anonymizes the outer error | exact | `as_error_echoes_the_requested_client_like_prepare_error_as`; diffsend `as-fast-hide-error-client`; `differential-gate.sh` AS error cases compare `crealm`/`cname` on both legs (`expect_error` `check_client`) |
| do_tgs_req.c:201-204 | `prepare_error_tgs` sets `errpkt.client` to the decrypted header ticket's client, else `NULL`; `opt_realm_of_principal` omits crealm when client is NULL | no status (reply fields) | issue.rs encode_krb_error; issue.rs tgs_header_client | TGS errors echo the TGT client; pre-decrypt failures omit cname and crealm | exact | `tgs_error_echoes_the_header_ticket_client_like_prepare_error_tgs`; `tgs_bad_msg_type_omits_crealm`; `tgs_ap_options_omits_crealm`; diffsend `tgs-bad-msg-type`/`tgs-ap-options` `check_client` |
| kdc_preauth.c:743-749; do_as_req.c:455-457 | missing PRE_AUTH / HW_AUTH after padata | `NEEDED_PREAUTH` **25** / `NEEDED_HW_PREAUTH` **25** | issue.rs as_reply; issue.rs issue_as_body | `NEEDED_PREAUTH` **25**; HW `NEEDED_HW_PREAUTH` **25** with hw_only hints | exact | `as_hw_auth_required_rejects_enc_ts`; diffsend `as-hw-preauth`; `scripts/flags-gate.sh` |
| kdc_preauth_encts.c:101; kdc_preauth_ec.c:128; spake_kdc.c:457; pkinit_srv.c:601-602 | `TKT_FLG_PRE_AUTH` only after a preauth module verifies; PKINIT sets `TKT_FLG_HW_AUTH` only when a certauth module returns `KRB5_CERTAUTH_HWAUTH` (`pkinit_srv.c:360-366`) | n/a (flag) | issue.rs issue_as_body | no verified preauth → no A; EncTs/EC/SPAKE/PKINIT set A; stock PKINIT does not set H (no certauth hook) | exact | `a3_r26.rs` `as_without_preauth_has_no_pre_authent`; `a3_r32.rs` `r32_pkinit_tgt_has_no_hw_authent`; `scripts/pkinit-gate.sh` / `scripts/rust-kinit-pkinit-gate.sh` `klist -f` no `H`; `+requires_hwauth` host is `NO HW PREAUTH` 60 |
| tgs_policy.c:174-177 | TGS server REQUIRES_HW_AUTH && !HW_AUTHENT | `NO HW PREAUTH` **60** | issue.rs check_tgs_policy_flags | `NO HW PREAUTH` **60** | exact | `issue_acl_ap.rs::tgs_requires_hw_auth_without_hw_flag` |
| do_as_req.c:218-221 | `check_kdcpolicy_as` plugin | plugin `*status` | plugins.rs check_as:686; issue.rs issue_as_body | `LOCAL_POLICY` **12** on fail-client / foreign indicator; DefaultPolicy is a no-op | exact | `a4_19b.rs` `a4_19_test_policy_fail_client_is_local_policy`; `scripts/kdcpolicy-gate.sh` `MIT_as_fail` / `RUST_as_fail` / `MIT_foreign_indicator` / `RUST_foreign_indicator` |
| policy.c:103-136 | kdcpolicy module endtime + deny (`check_kdcpolicy_as`) | plugin status + ret | plugins.rs output_from_indicator; plugins.rs apply_policy_times | ONE_HOUR/SEVEN_HOURS cap `endtime`/`renew_till`; deny is `LOCAL_POLICY` 12 | exact | `a4_19b.rs` `a4_19_test_policy_one_hour_rewrites_endtime`; `scripts/kdcpolicy-gate.sh` `MIT_one_hour_as` / `RUST_one_hour_as` |
| do_as_req.c:225-229 | `get_first_current_key(server)` fail | `FINDING_SERVER_KEY` + KDB err | issue.rs issue_as_body | `FINDING_SERVER_KEY` **60** | exact | `as_no_server_key_is_finding_server_key` |
| do_as_req.c:251-256 | `return_padata` fail | `KDC_RETURN_PADATA` + mech err | extra_padata SPAKE/PKINIT/ETYPE; no MIT return_padata | various | absent | proposed: diffsend `as-return-padata` |
| do_as_req.c:261-264 | client_keyblock ENCTYPE_NULL && no replaced reply key | `CANT_FIND_CLIENT_KEY` **14** | issue.rs issue_as_body | `CANT_FIND_CLIENT_KEY` **14** | exact | `as_cant_find_client_key_is_etype_nosupp` |
| do_as_req.c:270-281 | `handle_authdata` fail | `HANDLE_AUTHDATA` + ad err | issue.rs mint_ticket:2121; ad.rs handle_authdata | `HANDLE_AUTHDATA` on PAC logon / module path (no AS body AD) | exact | `a3_13.rs` TGS twins; mint PAC logon uses `HANDLE_AUTHDATA` |
| do_as_req.c:284-287; kdc_util.c:862-887 | `check_indicators` / `require_auth` string | HIGHER_AUTHENTICATION_REQUIRED **12** | ad.rs check_indicators; issue.rs issue_as_body | HIGHER_AUTHENTICATION_REQUIRED **12** | exact | `a3_14.rs` `as_require_auth_is_higher_authentication`; `scripts/cross-kdc-gate.sh` krbtgt `require_auth` password `kinit` (`KDC policy rejects request` + KDC log word both legs) |
| do_as_req.c:316-321 | `return_enc_padata` fail | `KDC_RETURN_ENC_PADATA` | `issue.rs mint_ticket` `encrypted_pa_data: None` | — | deferred | none (RFC 6806 enc padata oracle; F7) |
| do_as_req.c:346-347 | err && status==NULL | `UNKNOWN_REASON` | `proto()` always sets text; leftover **60** `e.to_string()` | no `UNKNOWN_REASON` | deferred | none (only if an AS path returns Protocol without text) |
| kdc_util.c:1320-1323 | `decode_krb5_pa_for_user` fail | `DECODE_PA_FOR_USER` + ASN.1 | krb5-kdc/ad.rs process_s4u2self_req | `DECODE_PA_FOR_USER` **60** | exact | diffsend `pa-for-user-undecodable`; `s4u2self_for_user_undecodable_is_generic` |
| kdc_util.c:1326-1330,1434-1438 | FOR-USER / S4U-X509 checksum fail | `INVALID_S4U2SELF_CHECKSUM` **41** failed verify / **50** unkeyed (both sites) | krb5-kdc/ad.rs verify_for_user_checksum | `INVALID_S4U2SELF_CHECKSUM` **41** / **50** unkeyed | exact | `phase7_preauth.rs::s4u2self_bad_checksum_rejected`; diffsend `pa-s4u-x509-user-bad-checksum` |
| kdc_util.c:1422-1425 | `decode_krb5_pa_s4u_x509_user` fail | `DECODE_PA_S4U_X509_USER` | krb5-kdc/ad.rs process_s4u_x509_user | `DECODE_PA_S4U_X509_USER` **60** | exact | diffsend `pa-s4u-x509-user-truncated`; `a2_r17_truncated_x509_is_decode` |
| kdc_util.c:1441-1446 | empty user && empty subject_cert | `INVALID_S4U2SELF_REQUEST` **6** | krb5-kdc/ad.rs process_s4u_x509_user | `INVALID_S4U2SELF_REQUEST` **6** | exact | diffsend `pa-s4u-x509-user-empty`; `s4u2self_x509_empty_is_invalid_request` |
| kdc_util.c:1604-1606 | S4U client `KDB_NOENTRY` | `UNKNOWN_S4U2SELF_PRINCIPAL` **6** | krb5-kdc/ad.rs s4u_from_userid | `UNKNOWN_S4U2SELF_PRINCIPAL` **6** | exact | `phase7_preauth.rs::s4u2self_unknown_for_user_is_refused`; `scripts/s4u-mit-gate.sh` |
| kdc_util.c:1607-1609 | S4U client DB err ≠ NOENTRY (cert-only NOTSUPP on db2) | `LOOKING_UP_S4U2SELF_PRINCIPAL` + com_err | krb5-kdc/ad.rs s4u_from_userid | `LOOKING_UP_S4U2SELF_PRINCIPAL` **60** | exact | `s4u2self_x509_cert_only_local_is_looking_up` |
| kdc_util.c:1453-1530 | `kdc_make_s4u2self_rep` PA-S4U-X509-USER in TGS-REP | no status (reply padata) | krb5-kdc/ad.rs make_s4u2self_rep | reply 130 when the request carried 130; user-id is nonce/user/options only | exact | `s4u2self_x509_issues_and_replies_130`; `a2_r17_reply_130_omits_subject_cert`; `scripts/s4u-mit-gate.sh` proxy cell |
| kdc_util.c:1534-1548; tgs_policy.c:273-276 | `is_client_db_alias` header client vs server | mismatch → `INVALID_S4U2SELF_REQUEST_SERVER_MISMATCH` **36** | `issue.rs issue_tgs_body` name+realm | same **36** | exact | `phase7_preauth.rs`; `scripts/s4u-mit-gate.sh`; `docs/security.md:67-69` |
| kdc_util.c:318-346 | `kdc_rd_ap_req` kvno==0: ≤3 tries, decrement kvno; local TGS `search_enctype=-1` | PROCESS_TGS / rd_req err | issue.rs decrypt_presented_tgt | kvno 0 retries ≤3; unknown kvno is 60 `PROCESS_TGS`; local TGS any etype at the labeled kvno; a ticket encrypted under kvno N−1 with labeled 0 decrypts | exact | `tgs_header_kvno_zero_issues`; `tgs_header_kvno_zero_decrypts_previous_kvno`; `tgs_header_unknown_kvno_is_generic`; diffsend `tgs-header-kvno-zero` |
| kdc_util.c:370-398,438-448 | `kdc_get_server_key`: DISALLOW_SVR/ALL_TIX → 7; enctype-mismatch (`:438-448`) → 60 GENERIC (`KRB5_KDB_NO_PERMITTED_KEY` outside 0..128); cross-TGT `is_local_tgs_principal` name+realm + `krb5_c_enctype_compare` rewrite (`kdc_util.c:97-110,313-316`) | DISALLOW half **7**; enctype-mismatch half **60** | issue.rs decrypt_presented_tgt; issue.rs find_server_key | `PROCESS_TGS` **7** on DISALLOW (TGS or service header); no matching kvno/etype is **60** `PROCESS_TGS`; local TGS = instance equals ticket.server.realm; foreign ticket looks up `ticket.server` only (incoming `krbtgt/<local>@<foreign>`) | exact | `tgs_krbtgt_disallow_all_tix_is_process_tgs`; `tgs_disallow_svr_service_header_is_process_tgs`; `tgs_header_unknown_kvno_is_generic`; `a2_r16.rs` `a2_r16_foreign_header_decrypts_via_incoming_kvno`; `docs/security.md:74-75` |
| kdc_util.c:377-379 | `krb5_db_get_principal(ticket->server)` — no fallback to `krbtgt/<ticket.realm>@local` | PROCESS_TGS 7 on NOENTRY | issue.rs decrypt_presented_tgt; store.rs create_interrealm_key / set_interrealm_decrypt_key | incoming trust is its own principal `krbtgt/<local>@<foreign>`; header and second-ticket lookup use that id. rust `kadmin-local` `addprinc` stores the local realm (`parse_name` drops a foreign realm) | exact | `a2_r16.rs` `a2_r16_incoming_trust_is_own_principal`; `a2_r16_incoming_trust_dump_load_round_trip`; `scripts/samba-crossrealm-gate.sh`; `scripts/samba-realtrust-gate.sh` |
| kdc_util.c:144-191 | TGS AP-REQ replay cache (`auth_con_setflags 0` — MIT disables) | no rcache | issue.rs issue_tgs_body | TGS replay **34** `TGS authenticator replay` | stricter-documented | `issue_acl_ap.rs::tgs_authenticator_replay_is_repeat`; `docs/security.md:19` |
| do_tgs_req.c:617-675 vs issue.rs:666-697 | **ORDER TGS**: MIT PROCESS_TGS(rd_req BADMATCH/BADADDR then times, no rcache)→FAST→NULL_SERVER→**GET_LOCAL_TGT→HEADER_PAC→search_sprinc**→constraints→svc_policy. Rust same after R19 | expired 32 / PAC 41 before LOOKING_UP_SERVER; expired+addr 38; expired+BADMATCH 36 | issue.rs process_tgs_header; issue.rs issue_tgs_body | same gather/PAC/search/constraint/policy order; replay before GET_LOCAL_TGT is the existing stricter rcache | exact | diffsend `tgs-expired-vs-unknown-sname`; `tgs-pac-corrupt-before-sname`; `tgs-expired-addr-mismatch`; `tgs-expired-badmatch`; `tgs_expired_beats_unknown_sname` |
| do_as_req.c:577-762 vs issue.rs:259-394 | **ORDER AS**: MIT NULL_C/S→lookup c/s→REFERRAL→GET_LOCAL_TGT→**validate_as (expiry then lockout)**→etype→anon→client key→cookie→**preauth**. Rust realm→opts→client→**lockout**→etype→**preauth**→HW→**then** server→times→flags | lockout after expiry; preauth after server+policy | lockout first; preauth before server/times | locked client never hits NAME_EXP; bad EncTs never hits unknown server | deviation | proposed: diffsend `as-expired-and-locked`; `as-bad-pa-unknown-server` |
| ORDER kdc_util.c:727-800 `validate_as_request` | INVALID OPTIONS → CLIENT EXPIRED → CLIENT KEY EXPIRED → SERVICE EXPIRED → REQUIRED PWCHANGE → POSTDATE → CLIENT LOCKED → SERVICE LOCKED → SERVICE NOT ALLOWED → failcount, all before preauth (`do_as_req.c:630` precedes `check_padata` `:758`) | same statuses | `issue.rs validate_as_request` called after the server lookup and before preauth | same ordered block before preauth, so a preauth-required client that trips a check gets that validate status rather than the preauth-required reply (anonymous PKINIT is present; `check_anon` / `restrict_anonymous_to_tgt` land with the AS path) | exact | `scripts/differential-gate.sh` `as-validate-before-preauth` (preauth+needchange `pwprau`) is `23`/`REQUIRED PWCHANGE` both legs; `issue_acl_ap.rs::as_validate_runs_before_preauth_like_process_as_req` |
| replay.c whole; dispatch.c:114-140 | lookaside: hash full request bytes; in-progress NULL marker → duplicate DISCARD-dropped; a produced reply (AS-REP **or** KRB-ERROR, `dispatch.c:82` code 0 after `prepare_error`) resent on retransmit; stale 2 min; 10 MiB cap, oldest-first eviction | resend the cached reply | krb5-kdc/lookaside.rs; krb5-kdc/listen.rs dispatch_via_cache (UDP + TCP, one shared cache); request bytes held once (`Arc<[u8]>` shared by the map and the FIFO) so memory tracks MIT's `req_packet + reply_packet + sizeof(entry)`; `kdc.lookaside.full` logged once at the first size-cap eviction | identical request resent → byte-for-byte the first reply | exact | `scripts/differential-gate.sh` `as-retransmit` identical on both legs; `persist_and_listener.rs::listener_retransmit_resends_the_cached_reply_like_replay_c`; `lookaside.rs` unit tests; `scripts/soak-gate.sh` RSS ramp-then-flat under the 10 MiB bound (`analyze-kdc-slo.py` fill fixtures) |
| dispatch.c:177-209 | UDP reply > `max_dgram_reply_size` (default 65536, path dead unless configured down) | `KRB_ERR_RESPONSE_TOO_BIG` **52** | krb5-kdc/listen.rs udp_loop | **52** when `max_dgram_reply_size` is under the reply | exact | `udp_oversize_reply_is_response_too_big` |
| dispatch.c:145-153 | empty / undecodable / unknown-tag pkt | no packet (`response == NULL` → `net-server.c:1101-1105` drop); status `MSG_TYPE` is not on the wire | krb5-kdc/listen.rs log_dispatch_drop; krb5-kdc/issue.rs handle_inner | an empty reply is dropped with a debug `kdc.issue` event, `outcome=ok` and no suffix; TCP writes nothing; `while dispatching (udp)` / `(tcp)` (`:14-16`) is the `error_suffix` of an error reply only (`:277`, `:432`); pvno ≠ 5 is the same drop (`asn1_k_encode.c:30`) | exact | diffsend `garbage-pdu`; diffsend `as-bad-pvno`; `handle_request_empty_is_dropped`; `as_bad_pvno_is_dropped`; `log_dispatch_drop_udp_is_not_tcp` |
| dispatch.c:154-157 | `setup_server_realm` NULL | **68** `WRONG_REALM` — unreachable on a single-realm KDC (`main.c:127-132`); never on the wire | single-realm; AS **6** `wrong realm`; TGS **60** `GET_LOCAL_TGT` | same | deviation (MIT drops / unreachable single-realm) | `docs/security.md:60-63`; diffsend `wrong-realm` |
| kdc_util.c:169-171 | no PA-TGS-REQ (`KRB5_PADATA_AP_REQ`) — split from the rcache row | PROCESS_TGS 16 `PADATA_TYPE_NOSUPP` | issue.rs issue_tgs_from | `PROCESS_TGS` 16 | exact | `tgs_missing_pa_tgs_req_is_padata_type_nosupp` |
| kdc_util.c:1881-1913; net-server.c:1278,1391-1414 | TCP `bufsiz` 1 MiB; length prefix > `bufsiz-4` | no status; `KRB_ERR_FIELD_TOOLONG` **61**; 128 KiB AS-REQ is dispatched | krb5-kdc/listen.rs handle_tcp; krb5-kdc/listen.rs MAX_TCP_REQUEST; krb5-kdc/status.rs CLIENT_NOT_FOUND | **61** above cap (`MAX_TCP_REQUEST`); 128 KiB **6** `CLIENT_NOT_FOUND` | exact | `tcp_one_mib_plus_one_is_field_toolong`; `tcp_128kib_unknown_cname_is_client_not_found`; `scripts/differential-gate.sh` both legs |
| net-server.c:85,1192-1282 (`max_stream_data_connections` 45; `kill_lru_stream_connection`) | at the cap a newly accepted stream evicts the oldest live connection, not the newcomer | no status (connection kill) | krb5-kdc/listen.rs tcp_loop; krb5-kdc/listen.rs ConnRegistry | oldest live stream shut down while the newcomer is served; `max_tcp_workers` default 45 (R2-S6) | exact | `tcp_over_cap_evicts_the_oldest_connection`; `tcp_conn_guard_deregisters_on_panic` |
| net-server.c:85,1571-1572,683,1278 | kadmind runs through the same net-server: RPC connections capped at 45 with LRU eviction, `SO_KEEPALIVE`, a fixed 1 MiB per-connection buffer streamed (not accumulated) | n/a (transport) | krb5-kdc/listen.rs ConnRegistry (reused by kadmind, cap 45 + evict) with a 5 s write timeout; krb5-admin/kadm5.rs read_record; krb5-admin/kadm5.rs MAX_KADM5_RECORD | connection cap + LRU eviction like MIT; the accumulated record is bounded at 1 MiB and a 5 s write timeout is set (stricter than MIT's streaming buffer + keepalive); no short read timeout, so an interactive session is not broken (R2-S3) | stricter-documented (`docs/security.md` "kadmind connection caps") | `read_record_bounds_the_total_accumulated_size`; `scripts/kadmin-gate.sh` both legs still function |
| kdc_util.c:800-803; lockout.c:92-113,181-190 | `krb5_db_check_policy_as` failcount lockout — **last** check in `validate_as_request`; `lockout_audit` clears fail count only for `REQUIRES_PRE_AUTH` at AS success | `CLIENT LOCKED OUT` **18** | `issue.rs validate_as_request` failcount block + `current_policy().check_as`; `store.rs record_as_outcome` | `CLIENT LOCKED OUT` **18**; success clears failcount only with `REQUIRES_PRE_AUTH` | exact | `store.rs::failed_as_stamps_last_failed`; `as_success_clears_failcount_only_with_requires_preauth`; `krb5-kdc store::tests::lockout_duration_only_unlocks_after_sleep` |
| lockout.c:102-104; kdb5.c:1539-1545,1574-1576 | not locked if `last_admin_unlock >= last_failed`; absent/short TL is stamp 0 | n/a | issue.rs last_admin_unlock; store.rs admin_unlock | `KRB5_TL_LAST_ADMIN_UNLOCK` 1792 (4-byte LE); absent TL stamp 0 so `last_failed == 0` is not locked | exact | `last_admin_unlock_skips_failcount_lockout`; `absent_unlock_tl_stamp_zero_does_not_lock_last_failed_zero`; `admin_unlock_clears_failcount_lockout`; `scripts/kadmin-gate.sh` unlock cell |
| do_as_req.c:579-580,598-599 | `KRB5_KDB_CANTLOCK_DB` on client/server lookup | no status; `KRB5KDC_ERR_SVC_UNAVAILABLE` **29** | `kdb.rs` `fetch_name` `Err` | **60** `e.to_string()` | absent | proposed: diffsend `as-db-locked` |
| do_as_req.c:236-241 | `fetch_last_req_info` + `get_key_exp(client)` into AS-REP enc-part | no status (reply fields) | issue.rs enc_rep_part; issue.rs get_key_exp | last-req type 0 at epoch; AS key-expiration is MIT min of expiration and pw expire, 0 omitted; TGS omits | exact | `a4_19.rs` `a4_19_last_req_is_lrq_none_epoch`; `a4_19_as_key_exp_is_min_of_expiration_and_pw_expire`; `a4_19b.rs` `a4_19_tgs_key_exp_is_omitted`; `scripts/expire-gate.sh` `MIT_pw_expire_warning` / `RUST_pw_expire_warning`; diffsend `as-success` last_req |
| do_as_req.c:322-330 | `reply.enc_part.kvno = client_key->key_data_kvno` is set **after** `krb5_encode_kdc_rep`, so the wire AS-REP enc-part carries no kvno | no status (reply field) | `krb5-kdc/issue.rs issue_as_body` | AS-REP enc-part `kvno` `None`; ticket enc-part keeps the server kvno | exact | `as_rep_enc_part_carries_no_kvno_like_mit`; `scripts/differential-gate.sh` `as-success` no `mit-as-enc-kvno` whitelist |
| do_as_req.c:709-712; do_tgs_req.c:850-851 | `starttime == authtime` → omit `starttime` | no status | issue.rs omit_start_if_auth:1566 | omitted on the ticket and EncKDCRepPart | exact | diffsend `tgs-postdated-from`; `a3_12.rs` `as_omits_starttime_when_it_matches_authtime`; `a3_r27.rs` `r27_tgs_postdated_omitted_from_is_epoch` |
| do_as_req.c:660-666; do_as_req.c:243 | CANONICALIZE + both TGS principals → ticket sname = `server->princ`; `reply_encpart.server` follows the ticket server | no status | `issue.rs issue_as_body` computes `ticket_sname` = the DB server name when CANONICALIZE and both requested/DB servers are krbtgt (R2-P7), used for both the ticket and the enc-part | canonical sname on the ticket and enc-part; requested sname otherwise | exact | `as_canonicalize_issues_the_krbtgt_under_the_canonical_db_name` (krbtgt/SHORT alias → krbtgt/KERBER.TEST with `-C`, requested name kept without) |
| kdc_util.c:1612-1615 | S4U2Self clears impersonated client's `pw_expiration` + `REQUIRES_PWCHANGE` (as Windows does) | n/a (exemption) | krb5-kdc/ad.rs s4u_from_userid | n/a (clears both, then `validate_as_request`) | exact | `s4u2self_pw_expired_user_still_issues`; `scripts/s4u-mit-gate.sh` expired cell |
| kdc_util.c:1467-1472 | `kdc_make_s4u2self_rep` copies nonce, user, masked options (no `subject_cert`) | n/a (reply padata 130) | ad.rs make_s4u2self_rep | rebuilds `S4uUserId` with nonce/user/realm/masked options; `subject_cert` omitted | exact | `a2_r17_reply_130_omits_subject_cert`; `a2_r17_reply_130_has_no_subject_cert`; diffsend `pa-s4u-x509-user` |
| kdc_util.c:1625-1644 | `s4u2self_forwardable`: keep F if `OK_TO_AUTH_AS_DELEGATE` or `check_allowed_to_delegate` is NOTSUPP/BADOPTION (db2 has no hook) | n/a (ticket flag) | issue.rs s4u2self_forwardable | keep F when `s4u_allowed_to` is empty (`create_host` seeds none); clear F when targets exist and no OK_TO_AUTH | exact | `a2_r17_s4u2self_keeps_f_without_clearing_targets`; `s4u2self_clears_forwardable_without_ok_to_auth`; `scripts/s4u-mit-gate.sh` |
| asn1_k_encode.c:30 | `pvno != 5` | decode error `KRB5KDC_ERR_BAD_PVNO` **3** → dispatch drop | issue.rs handle_inner | pvno ≠ 5 is dropped | exact | diffsend `as-bad-pvno`; `as_bad_pvno_is_dropped` |
| asn1_k_encode.c:1127-1148 | encode EncKDCRepPart as APPLICATION 26 for AS and TGS; decode 26 then 25 | n/a (success) | krb5-kdc/issue.rs encode_enc_kdc_rep_part; krb5-kdc/issue.rs issue_as_from; krb5-asn1/lib.rs decode_enc_kdc_rep_part; krb5-protocol/as_ex.rs decode_enc_as | EncTgsRepPart tag 26 both AS and TGS; RFC 25 still accepted on decode | exact | diffsend `as-success`; diffsend `tgs-success`; `scripts/differential-gate.sh`; other clients that require RFC APPLICATION 25 are out of scope |
| kdc_util.c:610-627 `get_verified_pac` | service ticket PAC: retry privsvr with the two previous krbtgt kvnos on `MODIFIED` **or** `KRB5_BAD_ENCTYPE` | HEADER_PAC 41 `MODIFIED` after the retries | krb5-kdc/ad.rs get_verified_pac; krb5-kdc/ad.rs verify_pac_sig; krb5-kdc/ad.rs pac_verify_retryable | retry sentinel stays `ETYPE_NOSUPP` 14 internally; krbtgt-header / krbtgt-2nd-ticket non-retry exits wire **60** `GENERIC`; after retries, `MODIFIED` 41. RENEW of a PAC service ticket after krbtgt enctype rekey issues | exact | `a2_r19_renew_pac_service_after_krbtgt_enctype_rekey`; `a2_r23_header_pac_wrong_cksumtype_is_generic`; diffsend `tgs-pac-server-cksum-wrong-enctype`; `scripts/differential-gate.sh` krbtgt rekey RENEW |
| kdc_util.c:527-556 `pac_privsvr_key` | `pac_privsvr_enctype` string attribute derives the privsvr key with PRF+ from `get_first_current_key` of the local TGT | n/a | ad.rs pac_privsvr_key; issue.rs issue_tgs_body | PRF+ `pac_privsvr` when the attr differs from the TGT etype | exact | `tgs_privsvr_enctype_signs_with_prfplus`; `scripts/cross-kdc-gate.sh:276-277` |

## A3 — kdc_preauth*.c / fast_util.c / kdc_authdata.c / cammac.c / kdc_log.c

MIT 1.22.2 `src/kdc/` vs kerber-rust. Lines actually read. Wire codes
are RFC 4120/6113 integers. After W0d G3, FAST unwrap failures wire
`FIND_FAST`; the MIT log message is `kdc.issue` `detail`.

| MIT file:line | check (condition) | MIT status + wire code | Rust site | Rust e_text + code | verdict | proof |
| --- | --- | --- | --- | --- | --- | --- |
| kdc_preauth.c:999-1000 | PREAUTH_REQUIRED hint: empty PA-FX-FAST (136) first, then ETYPE-INFO (11), then 19, then modules | no status; METHOD-DATA `[136, (11), 19, modules]` | issue.rs preauth_hint_edata; plugins.rs advertise_preauth | n/a (e_data 136); order `[136, 19, modules, 133]` | exact | `as_hint_list_is_136_info2_modules_cookie`; `ca_enabled_preauth_required_method_data_types`; `scripts/mit-fast-kdc-gate.sh`; `scripts/differential-gate.sh:334-352` |
| kdc_util.c:1768-1801; get_in_tkt.c:1365-1372; fast.c:635-675 | RFC 6806 FAST nego: the client sends empty PA-AS-FRESHNESS/PA-REQ-ENC-PA-REP; the KDC echoes a PA-REQ-ENC-PA-REP checksum (usage 56) + empty PA-FX-FAST when 149 is present; the client verifies the checksum and records `fast_avail` from the echoed 136 | `KDCREP_MODIFIED` on a missing/bad checksum | issue.rs enc_pa_rep_padata (echo); as_ex.rs build_as_req + finish_as_rep (150/149 then verify); as_ex.rs send_spake_response (the SPAKE response keeps 150/149 behind the cookie and PA-SPAKE like `k5_preauth` + `init_creds_step_request`); AsOutcome fast_avail | same; under FAST the advertised 150/149 travel inside the FAST-REQ (`kdc_find_fast` swaps the inner request in) and the checksum is verified over the outer request with the strengthened reply key like MIT (`preauth.rs attach_fast`, `as_ex.rs finish_fast_as`); `kinit` writes the `fast_avail=yes` and `pa_type=<selected>` ccache config entries keyed by the TGT server like `write_out_ccache` (`get_in_tkt.c:1617-1640`, `save_selected_preauth_type`), ahead of the credentials (`ccache.rs set_config`; `pa_config_data` has no producer here; Rust's FAST client authenticates with encrypted timestamp inside the tunnel where MIT selects encrypted challenge 138, so a preauthenticated FAST kinit records `pa_type` 2 vs MIT's 138) | exact | `as_exchange_rejects_reply_missing_enc_pa_rep_checksum`, `as_exchange_records_fast_availability`, `spake_response_request_keeps_the_advertised_padata_in_mit_order`, `kinit_records_fast_avail_and_pa_type_like_write_out_ccache`; `scripts/mit-fast-kdc-gate.sh` MIT plain kinit traces `FAST negotiation: available` and its `klist -C` lists `fast_avail`/`pa_type` for `krbtgt`; `scripts/rust-kinit-spake-gate.sh` and `scripts/rust-kinit-fast-gate.sh` MIT `klist -C` lists the same entries in the Rust-written ccache (red `e5d9e28`…`a8563e1`: the SPAKE request dropped 149 → `KDCREP_MODIFIED`) |
| spake_client.c:51,138,221 `contains_sf_none` | the SPAKE client answers a challenge only when its factor list offers SF-NONE (type 1); without second-factor support any other factor set is unanswerable | `KRB5KDC_ERR_PREAUTH_FAILED` **24** at response generation (`:221`); the receive callback (`:138`) silently declines `need_as_key` | as_ex.rs spake_contains_sf_none + send_spake_response (checked after the P-256 group test, before deriving the s2k key) | client abort `SPAKE challenge offers no SF-NONE factor` (`Error::ReplyMismatch`, as for the sibling group-mismatch refusal); no packet sent | exact | `krb5-protocol as_ex::spake_factor_tests::{sf_none_present_is_answerable,no_sf_none_is_refused}` drives the refusal (no MIT KDC emits a factor list without SF-NONE — `spake_kdc.c:243` always sets it — so the oracle cannot produce the negative packet); `scripts/rust-kinit-spake-gate.sh` proves the positive path: MIT's SF-NONE challenge → Rust kinit succeeds |
| kdc_preauth.c:1003-1004 + :767-799 | get_preauth_hint_list add_etype_info: one PA-ETYPE-INFO2 entry for the chosen client key | chosen client etype | issue.rs preauth_required (one entry, `ckey` etype) | one entry = chosen etype | exact | `preauth_required_hint_lists_one_etype_info2_entry_like_mit`; `differential-gate.sh` `pauser-no-preauth` compares the ETYPE-INFO2 etype set exactly on both legs |
| do_as_req.c:443-447 `finish_preauth` | a PREAUTH_FAILED (24) error carries the `get_preauth_hint_list` e_data (like PREAUTH_REQUIRED); other preauth codes (SKEW 37) carry none | no status (reply e_data) | issue.rs attach_preauth_hint on a 24 from run_as_preauth | 24 carries the METHOD-DATA hint | exact | `as_preauth_failed_carries_the_hint_list_like_mit`; `differential-gate.sh` `as-optimistic-encts-wrong-etype` compares the e_data structurally on both legs |
| kdc_preauth.c:779-823 | PA-ETYPE-INFO (11) + PW-SALT (3) for pre-info2 clients | in hint + AS-REP if !key_modified | issue.rs etype_info_padata; issue.rs as_rep_key_info | 11+3 when the request is pre-info2 only | deferred | code exists; live rc4 `allow_rc4` pre-info2 cell is W1-D |
| kdc_preauth.c:1141-1170 | MORE_PREAUTH_DATA_REQUIRED: add ETYPE-INFO2 unless cookie already seen | **91** `MORE_PREAUTH_DATA_REQUIRED` + PA 19 (unless cookie already seen); `e_text` `PREAUTH_FAILED` (`do_as_req.c:439-442,809`) | `issue.rs issue_as_body` `MORE_PREAUTH_DATA_REQUIRED` + `maybe_add_etype_info2` | SPAKE challenge **91** `MORE_PREAUTH_DATA_REQUIRED` `PREAUTH_FAILED` + PA 19 when no cookie | exact | `handle_request_spake_91_e_text_is_preauth_failed`; diffsend `as-spake-round1`; `scripts/spake-gate.sh`; `scripts/rust-kinit-spake-gate.sh` |
| do_as_req.c:785-814; pkinit_srv.c:932 | AS error e_data gets PA-FX-COOKIE (133) **only when `e_data_in != NULL`**; `typed_e_data` (PA_TYPED_E_DATA modules, PKINIT) encodes TYPED-DATA tags [0]/[1]; empty cookie = 3-byte `MIT` | cookie on e_data-bearing errors; 65 TYPED-DATA | krb5-kdc/preauth.rs prepare_as_edata; krb5-kdc/preauth.rs dh_params_not_accepted; krb5-kdc/preauth.rs make_cookie_at | 3-byte `MIT` on hint e_data; MIT1 when SPAKE set state; PKINIT 65 is TYPED-DATA `[109, 133]`; TGS FAST errors have no cookie | exact | `pkinit_unknown_dh_is_typed_edata_with_cookie`; `scripts/pkinit-gate.sh` `rust_kdc_pkinit_dh1024`; `scripts/rust-kinit-pkinit-gate.sh` `mit_kdc_pkinit_dh1024`; `spake_challenge_then_as_rep` |
| fast_util.c:545-610 + :656-721 | cookie MIT1+kvno+enc, ku 513, PRF+ `COOKIE`+princ, TTL 600s; mint/read use `get_first_current_key`; unknown kvno ignored (`return 0` at :610; PREAUTH_EXPIRED dead in 1.22.2) | `READ_COOKIE` do_as_req.c:747-752 (fn actually `return 0`) | krb5-kdc/preauth.rs make_cookie; krb5-kdc/preauth.rs open_cookie; krb5-kdc/preauth.rs derive_cookie_key | MIT1 ‖ kvno ‖ enc(PRF+ first current TGT key, `COOKIE`‖unparsed client, ku 513); 600 s TTL; unknown kvno / garbage ignored | exact | `spake_cookie_round_trips_on_golden_dump`; `spake_unknown_cookie_kvno_is_preauth_failed`; `spake_challenge_then_as_rep`; `scripts/cross-kdc-gate.sh` SPAKE `pa_type = 151` both legs |
| kdc_preauth.c:129-141 | auto-register pkinit, otp, spake, encrypted_challenge, encrypted_timestamp | module edata into hint | plugins.rs process_as:96; plugins.rs process_as:143; plugins.rs process_as:197; plugins.rs process_as:245; no otp | otp types 141/142 unused | absent (otp) | propose `scripts/proposed mit-otp-gate.sh` or defer otp; pkinit/spake: `scripts/pkinit-gate.sh` `scripts/spake-gate.sh` |
| preauth_ec.c:48-137 `ec_process` | a preauth-required FAST kinit answers with PA-ENCRYPTED-CHALLENGE (138) under the armor key, so `write_out_ccache` records `pa_type` 138 | ccache `pa_type = 138` | as_ex.rs continue_fast (encrypted timestamp inside the FAST tunnel) | `pa_type` **2** — the Rust FAST client sends PA-ENC-TIMESTAMP inside the armor, which the MIT KDC accepts (`rust-kinit-fast-gate` green), so this is a recorded-hint divergence, not an interop break | deviation (R2-D1/P1: porting the encrypted_challenge client is a full preauth-mechanism port for a cosmetic pa_type; reopen if an app keys off the recorded pa_type) | `kinit_records_fast_avail_and_pa_type_like_write_out_ccache` (unarmored records 151); `scripts/rust-kinit-fast-gate.sh` (FAST still records 2; MIT KDC accepts the FAST enc-timestamp) |
| pkinit_crypto_openssl.c:1481-1487,3010-3043 | DH/ECDH group table + `pkinit_dh_min_bits` (P-256/P-384/P-521, MODP 2048/4096); MIT accepts P-384 by default | n/a | krb5-types/src/pkinit.rs | Rust KDC rejects P-384 with **65** TYPED-DATA (P-256 only); MIT accepts | deviation (item 17) | `scripts/pkinit-gate.sh` `rust_kdc_pkinit_dh1024` (honest shape cell); proposed: group table port |
| pkinit_srv.c:141 `pkinit_server_get_edata` + `:935-939` | PA-PK-AS-REQ + PA-PKINIT-KX when CA; PKINIT_KX is PA_INFO so `hw_only` skips 147 (`:928-929`) | empty 16 and 147 (16 only under hw_only) | plugins.rs advertise:121 only if `pkinit_ca`; omit 147 when `REQUIRES_HW_AUTH` | 16+147 iff CA and not hw_only | exact | `r30_pkinit_hint_is_16_147`; `r32_pkinit_client_requires_hwauth_is_needed_hw_preauth`; `pkinit_advertised_in_method_data_when_ca_enabled`; `pkinit_not_advertised_without_ca` |
| spake_kdc.c:299 `spake_edata`; groups.c:60,235-238 | empty 151 in hint iff groups configured | 151 | plugins.rs advertise:183 | 151 iff `spake_preauth_groups` is non-empty | exact | `r30_spake_not_advertised_without_groups`; `ca_enabled_preauth_required_method_data_types` (types `[136, 19, 16, 147, 151, 2, 133]`) |
| spake_kdc.c:337-344 `verify_support` | first offered group that is permitted, else **24** | `PREAUTH_FAILED` **24** | krb5-kdc/preauth.rs process_spake | `PREAUTH_FAILED` **24** when no offered group is permitted (P-256 only implemented) | exact | `r30_verify_support_unpermitted_offer_is_24`; `scripts/mit-fast-kdc-gate.sh` default-client edwards25519 vs P-256 KDC |
| kdc_preauth_ec.c:37-48 | EC hint only if armor_key && client keys; FAST-inner METHOD-DATA keeps empty 136 and 151 | empty 138 | plugins.rs advertise:299; issue.rs preauth_hint_edata | empty 138 with armor; FAST-inner keeps 136/151 | exact | `as_fast_hint_keeps_136_151_lists_138_omits_2`; `encrypted_challenge_skew_is_fast_wrapped`; `scripts/rust-kinit-fast-gate.sh` MIT `KRB5_TRACE` `Processing preauth types` 136+138 both KDC legs |
| kdc_preauth_ec.c:71-76 | EC outside FAST | wire **24** `PREAUTH_FAILED` (ENOENT filtered; log `Encrypted Challenge used outside of FAST tunnel`) | issue.rs issue_as_body | `PREAUTH_FAILED` **24** | exact | diffsend `ec-outside-fast`; `as_ec_outside_fast_is_preauth_failed` |
| kdc_preauth_ec.c:101-118 | EC decrypt fail | `PREAUTH_FAILED` 24 `Incorrect password in encrypted challenge` | issue.rs enc_rep_part | `encrypted challenge` 24 | deviation | `encrypted_challenge_wrong_key_locks_at_max_fail`; e_text mismatch |
| kdc_preauth_ec.c:124-126 | EC clockskew | SKEW 37 | issue.rs enc_rep_part | `encrypted challenge skew` 37 | exact | `encrypted_challenge_stale_ts_is_skew` |
| kdc_preauth_ec.c:87-140 | EC success: PRE_AUTH + optional auth-indicator | CAMMAC later | issue.rs issue_as_body:500; store.rs Policy.encrypted_challenge_indicator | indicator then CAMMAC in mint_ticket | exact | `scripts/rust-kinit-fast-gate.sh` `encrypted_challenge_indicator` + `require_auth` (MIT `kinit -T` pa_type 138 then `kvno` issued; password `kvno` is 12 both legs) |
| pkinit_srv.c:591-596 | signed PKINIT asserts `[realms] pkinit_indicator` | CAMMAC later | issue.rs issue_as_body:444 | indicator then CAMMAC in mint_ticket | exact | `scripts/pkinit-gate.sh` rust KDC + `scripts/rust-kinit-pkinit-gate.sh` MIT KDC: MIT `kinit -X` then `kvno` issued; password `kvno` is 12 |
| pkinit_srv.c:371-373 | certauth-module indicators (`ais`) | CAMMAC later | — | no certauth trait | absent | none (no certauth plugin surface; KDC-configured `pkinit_indicator` is the row above) |
| spake_kdc.c:155-181 | SPAKE success asserts `[realms] spake_preauth_indicator` | CAMMAC later | issue.rs issue_as_body:481 | indicator then CAMMAC in mint_ticket | exact | `scripts/cross-kdc-gate.sh` `require_auth spake`: SPAKE `kvno` issued both legs; password `kvno` is 12 both legs |
| kdc_preauth_ec.c:154-205 | EC return: KDC challenge ku 55 | PA 138 in AS-REP (FAST inner) | issue.rs enc_pa_rep_padata | 138 on success | exact | `encrypted_challenge_skew_is_fast_wrapped`; phase7 FAST AS |
| kdc_preauth_encts.c:39-43 | ENC_TIMESTAMP hint iff !armor && have keys | empty 2 | plugins.rs advertise:230 | empty 2 without armor; omitted with armor | exact | `as_fast_hint_keeps_136_151_lists_138_omits_2`; `as_hint_list_is_136_info2_modules_cookie`; `scripts/rust-kinit-fast-gate.sh` FAST-inner TRACE omits 2 under armor |
| kdc_preauth_encts.c:47-118 | ENC_TS verify; NO_MATCHING_KEY→24 | 24; SKEW 37 pass-through (filter :1101) | plugins.rs process_as:245 (EncTsMod) no key of PA etype → 24; issue.rs verify_enc_timestamp replay 34 | no-matching-key 24 matches; enc-ts replay 34 is stricter (MIT has no enc-ts rcache) | stricter-documented | `as_enc_timestamp_wrong_etype_is_preauth_failed_like_mit`; `differential-gate.sh` `as-optimistic-encts-wrong-etype` (24 both legs); `pa_enc_timestamp_replay_is_repeat`; docs/security.md replay row |
| kdc_preauth.c:1394-1506; do_as_req.c:251-256 | return_padata: AS-REP outer padata is `add_etype_info`/`add_pw_salt` output only; MIT 1.22.2 never emits PA-SUPPORTED-ENCTYPES (165) | `KDC_RETURN_PADATA` | issue.rs as_rep_key_info; no `extra_padata` seed | AS-REP/TGS-REP outer padata = MIT etype-info2 set | exact | `differential-gate.sh` `as-success`/`tgs-success` compare the full outer padata-type set on both legs |
| return_enc_padata kdc_preauth.c:1636-1663; do_tgs_req.c:1098-1104 | enc_padata: referral + FAST nego + PAC-OPTIONS | `KDC_RETURN_ENC_PADATA` | issue.rs return_enc_padata | FAST nego + PAC-OPTIONS RBCD echo; referral PA-20 omitted | exact | diffsend `tgs-rbcd-pac-options`; `tgs_pac_options_rbcd_is_echoed_in_enc_padata` |
| kdc_preauth.c:1578-1609 | PA-PAC-REQUEST (128) include_pac; default TRUE; undecodable → TRUE; TGS ignores it (`handle_pac` is_as_req only) | omit PAC on AS if false | issue.rs include_pac_for_reply | AS omits PAC if false; TGS includes PAC iff the subject had one | exact | `as_pac_request_false_and_disable_pac_omit_pac`; diffsend `tgs-pac-request-false` (issued PAC); diffsend `tgs-from-pacless-tgt` |
| fast_util.c:207-224 (post-E3) | verify req_checksum **then** keyed-cksum | bad bytes 41 FIND_FAST; unkeyed 12 FIND_FAST (log `Unkeyed checksum used in fast_req`); unknown type 60 FIND_FAST; keyed types match by enc provider (`crypto_int.h:596-608`); cksumtype 0 → mandatory then `is_keyed(0)` → 12 | krb5-kdc/preauth.rs verify_fast_req_checksum; krb5-kdc/preauth.rs proto_fast; krb5-crypto/ops.rs verify_checksum_type | 41/12/60 FIND_FAST (G2+G3+H2; detail holds the MIT log text) | exact | `fast_as_rsa_md5_unkeyed_is_policy`; `fast_as_unkeyed_type_with_bad_bytes_is_modified`; `fast_as_crc32_checksum_is_generic`; `fast_as_short_mac_is_generic`; `fast_as_arcfour_hmac_type_over_aes_key_wrong_bytes_is_modified`; `fast_as_same_provider_type_wrong_bytes_is_modified`; `fast_as_cross_provider_type_is_generic`; `fast_as_cksumtype_zero_valid_mac_is_policy`; proposed diffsend `-138`/type-0 |
| do_as_req.c:526-532 | AS checksum over wire KDC-REQ-BODY (field 4) | `FIND_FAST` | krb5-kdc/preauth.rs proto_fast | `FIND_FAST` | exact | E3; `scripts/mit-fast-kdc-gate.sh` |
| fast_util.c:159-163 | TGS explicit AP-REQ armor **with** tgs_subkey | FIND_FAST 24 (log `Ap-request armor not permitted with TGS`) | krb5-kdc/preauth.rs kdc_find_fast; krb5-kdc/preauth.rs proto_fast | FIND_FAST 24 (detail `Ap-request armor not permitted with TGS`) | exact | `tgs_fast_explicit_armor_is_preauth_failed` |
| fast_util.c:157-166 | TGS explicit AP-REQ armor **without** tgs_subkey → `armor_ap_request` | may succeed or FIND_FAST 12 (log `ap-request armor without subkey`) | krb5-kdc/preauth.rs kdc_find_fast; krb5-kdc/preauth.rs armor_key_from_ap | `armor_ap_request`; 12 when the armor authenticator has no subkey | exact | `tgs_fast_explicit_armor_without_pa_tgs_subkey_is_accepted`; `tgs_fast_explicit_armor_without_any_subkey_is_policy`; docs/security.md FAST TGS AP-REQ armor |
| fast_util.c:180-184 | TGS FAST, no armor, no subkey | FIND_FAST 24 (log `No armor key but FAST armored request present`) | krb5-kdc/preauth.rs kdc_find_fast; krb5-kdc/preauth.rs proto_fast | FIND_FAST 24 (G3) | exact | `tgs_fast_without_subkey_is_preauth_failed` |
| fast_util.c:70-76 | AS AP-REQ armor missing authenticator subkey | FIND_FAST 12 POLICY (log `ap-request armor without subkey`) | krb5-kdc/preauth.rs armor_key_from_ap; krb5-kdc/preauth.rs proto_fast | FIND_FAST 12 (detail `ap-request armor without subkey`) | exact | `fast_as_armor_without_subkey_is_policy`; diffsend `fast-armor-no-subkey` |
| fast_util.c:277-355 + :427-440 | FAST reply always strengthen_key; CF2 replykey (`kdc_fast_response_handle_padata` / `kdc_fast_handle_reply_key`; no `kdc_fast_strengthen_reply_key` symbol). MIT client copies existing_key when strengthen_key is NULL | silent | issue.rs issue_as_body; issue.rs issue_tgs_body | AS and TGS FAST replies strengthen then CF2 `replykey`; rust client hard-fails a missing strengthen-key (docs/security.md) | exact | `fast_as_exchange_strengthen_and_finished`; `tgs_fast_inner_nonce_not_outer`; `scripts/rust-kinit-fast-gate.sh` `--fast -S` rust+MIT KDC (`fast_strengthen` / `FAST reply key`) |
| send_tgs.c:172-178; fast.c:134-137 | every TGS-REQ is FAST-armored (`krb5int_fast_tgs_armor` with NULL armor ccache → subkey/ticket CF2) | n/a (request) | tgs.rs tgs_once | always CF2 `subkeyarmor`/`ticketarmor` and PA-FX-FAST, including after a password AS | exact | `r30_tgs_after_password_as_is_fast_armored`; `scripts/rust-kinit-fast-gate.sh` MIT-KDC `--fast -S` |
| decode_kdc.c:64-67 | TGS `KRB5_ERR_FAST_REQUIRED` (no PA-FX-FAST) is ignored; present FAST still requires finished | n/a (client) | tgs.rs tgs_fast_reply_key:400 | missing PA-FX-FAST decrypts under the authenticator subkey; present FAST still requires finished + strengthen | exact | `r33_tgs_unwrapped_fast_is_accepted`; `heimdal-gate.sh` |
| fast_util.c:443-447; do_as_req.c:324-325 | `kdc_fast_hide_client`: hide-client-names (bit 1) makes the outer AS-REP client the anonymous principal `WELLKNOWN/ANONYMOUS@WELLKNOWN:ANONYMOUS` | anonymous outer client on the AS-REP | issue.rs issue_as_body sets the outer AS-REP cname/crealm to the anonymous principal when the FAST request set bit 1; the real client stays inside the FAST reply | anonymous outer client on the AS-REP | exact | `fast_hide_client_names_returns_the_anonymous_outer_client`; docs/security.md |
| kdc_preauth.c:1092-1133 | filter_preauth_error: unknown → 24 | 24 default | krb5-kdc/issue.rs verify_enc_timestamp; krb5-kdc/issue.rs verify_encrypted_challenge | **34** `REPEAT` on a replayed enc-challenge blob vs MIT's **24** — a different wire code, not just a stricter one | deviation (R2-D1: the replay cache answers before `filter_preauth_error` runs) | `encrypted_challenge_replayed_blob_is_repeat`; security.md |
| do_as_req.c:270-281; do_tgs_req.c:1038-1046 | handle_authdata fail | `HANDLE_AUTHDATA` + LOG_INFO `AS_REQ/TGS_REQ : handle_authdata (%d)` | ad.rs handle_authdata:1219; status.rs HANDLE_AUTHDATA | `HANDLE_AUTHDATA` 12/31 | exact | diffsend `tgs-ad-mandatory-for-kdc`; `a3_13.rs` |
| kdc_authdata.c:576-627 | HANDLE_AUTHDATA order: copy TGS body AD → kdcauthdata plugins → copy TGT AD → handle_pac | POLICY 12 if AD-MANDATORY-FOR-KDC | ad.rs handle_authdata:1219; ad.rs copy_request_authdata; ad.rs copy_tgt_authdata; ad.rs is_kdc_issued_authdatum; plugins.rs register_authdata | copy + filter + modules (empty unless `register_authdata`); PAC via mint_ticket | exact | diffsend `tgs-body-authdata`; diffsend `tgs-ad-mandatory-for-kdc`; diffsend `tgs-body-authdata-kdc-issued-stripped`; `scripts/cross-kdc-gate.sh` MIT TGT `1/128` through Rust TGS |
| pac_sign.c:389-419 | dummy PAC memmove to index 0; signed PAC replaces `[0]` | n/a (issue side) | issue.rs mint_ticket:2112 | PAC placeholder and signed PAC at index 0 | exact | `a3_r28.rs` `r28_pac_is_first_authdata`; diffsend `tgs-body-authdata` `pac_first`; `scripts/cross-kdc-gate.sh` `1/128,1/96` |
| greet_auth.c:62-72 | TGS-only greet: `krb5_make_authdata_kdc_issued` then IF-RELEVANT; `krb5_merge_authdata` puts greet first | n/a (issue side) | plugins.rs handle:475; plugins.rs make_authdata_kdc_issued:447 | IF-RELEVANT{KDC-ISSUED{-42}} merged first; usage 19; issuer is the local TGS (`do_tgs_req.c:986-990` `header_server`) | exact | `a3_r28.rs` `r28_greet_is_kdc_issued`; `r28_greet_precedes_copied_ad`; diffsend `tgs-body-authdata` `greet` |
| kdc_authdata.c:258-265 | body AD: session+ku 4 then `client_key`+ku 5 | HANDLE_AUTHDATA on decrypt fail | ad.rs copy_request_authdata:1185 | session ku 4 then client_key ku 5 (subkey or session) | exact | diffsend `tgs-body-authdata-subkey`; diffsend `tgs-body-authdata-session-ku5`; `a3_r28.rs` `r28_body_ad_subkey_ku5_is_copied` |
| kdcauthdata_plugin.h:105-118 | `handle` receives `enc_tkt_reply` (new session) and TGS principal | module error logged, not fatal | plugins.rs handle:430 | `session` / `issuer` are reply session and local TGS; errors logged | exact | `plugins.rs` `greet_is_tgs_only`; `a3_r28.rs` `r28_greet_is_kdc_issued` |
| kdc_authdata.c:300-336; cammac.c:55-127 | auth indicators → AD-97 inside CAMMAC (96) + IF-RELEVANT; ku 64 (`add_auth_indicators`) | ticket AD | ad.rs add_auth_indicators; ad.rs cammac_create; issue.rs mint_ticket | AD-97 in CAMMAC 96 in IF-RELEVANT; ku 64 | exact | `ad.rs` `cammac_round_trip_and_bad_mac_ignored`; `scripts/cross-kdc-gate.sh` SPAKE TGT `96` |
| cammac.c:134-175 | cammac_check_kdcver over EncTicketPart with CAMMAC elements as AD | ignore unverified CAMMAC | ad.rs cammac_check_kdcver:1391; ad.rs get_auth_indicators | unverified CAMMAC skipped; decode fail is GET_AUTH_INDICATORS | exact | `ad.rs` `cammac_bad_kdcver_mac_is_skipped`; diffsend `tgs-truncated-cammac` |
| cammac.c:168 keyed gate | `krb5_c_verify_checksum` with no `krb5_c_is_keyed_cksum` | accept unkeyed / type 0 | ad.rs cammac_check_kdcver:1420 (`verify_checksum_keyed`) | unkeyed / type 0 skipped | stricter-documented | `a3_r29.rs`; `docs/security.md` CAMMAC KDC verifier keyed checksum |
| kdc_authdata.c:382-439 | first-hop local S4U2Proxy `update_delegation_info` | n/a (issue side) | ad.rs update_delegation_info | `proxy_target` without realm; transited hop is evidence server with realm | exact | `scripts/s4u-mit-gate.sh` `MIT_testkdb_s4u2proxy_happy`; `a2_r18_local_s4u2proxy_client_info_omits_realm` |
| kdc_authdata.c:520-544 | local constrained: write DI; else copy DI; final S4U CLIENT_INFO omits realm | n/a (issue side) | ad.rs sign_reply_pac_s4u; issue.rs issue_tgs_body | same DI and client-info; Rust S4U2Self mint still adds extra PAC buffers that copy into the proxy PAC | exact (DI + client info); deviation (extra PAC types) | `scripts/s4u-mit-gate.sh` `--print-delegation`; `a2_r18_cross_issues_pac_client_and_realm` |
| kdc_authdata.c:517-552 | TGS copy-and-resign: MIT db2 copies CLIENT_INFO + DELEGATION_INFO only | n/a (issue side) | ad.rs sign_reply_pac_inner | copies every non-signature subject buffer (AD-like superset; Samba L1/L2 verify the bytes) | deviation | `crates/krb5-kdc/tests/ad_pac.rs`; `scripts/samba-pac-l2-gate.sh`; `scripts/cross-kdc-gate.sh` PAC types |
| kdc_authdata.c:517-564 vs :456-494 | PAC: issue_pac → **add_auth_indicators (CAMMAC)** → client/deleg info → sign PAC | CAMMAC in tkt before PAC sign | issue.rs mint_ticket:2112 | CAMMAC before PAC checksum; PAC then spliced to index 0 | exact | `ad.rs` `cammac_round_trip_and_bad_mac_ignored`; `scripts/cross-kdc-gate.sh` `1/128,1/96` |
| kdc_authdata.c:488-494 | TGS: no PAC if subject_pac==NULL (still indicators) | skip PAC | issue.rs include_pac_for_reply | no PAC when the subject had none | exact | diffsend `tgs-from-pacless-tgt`; `tgs_without_pac_still_issues` |
| do_tgs_req.c:657-664 | HEADER_PAC verify immediately after FAST unwrap | `HEADER_PAC` | krb5-kdc/ad.rs get_verified_pac; issue.rs issue_tgs_body | `HEADER_PAC` 41/13 | exact | diffsend `tgs-pac-corrupt-before-sname`; `tgs-pac-client-mismatch` |
| kdc_log.c:76-90 | AS fail/success LOG_INFO `AS_REQ (%s) %s: STATUS: cname for sname` | two formats: success `AS_REQ (%s) %s: ISSUE: authtime %u, %s, %s for %s` (76-83); fail `AS_REQ (%s) %s: %s: %s for %s%s%s` (85-89) | issue.rs handle_request; audit.rs log_success; audit.rs log_failure | `kdc.issue` JSON: `kind`, `req_etypes`, `from`, `status=ISSUE`, `authtime`, `etypes`, `client`, `server` | exact | docs/logging.md; `scripts/kdc-gate.sh` `RUST_issue_as` / `MIT_issue_as`; `a4_20_as_issue_log_has_client_server_etypes` |
| kdc_log.c:142-152 | TGS LOG_INFO same shape | `TGS_REQ (%s) %s: %s: authtime %u, %s%s %s for %s%s%s` (146-152) plus PROTOCOL-TRANSITION / CONSTRAINED-DELEGATION lines (153-160) and SERVER_NOMATCH special case (142-143) | audit.rs tgs_success; audit.rs tgs_failure | same tuple + `s4u` / `s4u_client`; `nomatch` for SERVER_NOMATCH | exact | docs/logging.md; `scripts/kdc-gate.sh` `RUST_issue_tgs` / `MIT_issue_tgs` |
| kdc_log.c:201-206 | unexpected transit check: LOG_ERR | ERR | audit.rs unexpected_transit_false; issue.rs issue_tgs_body | `tracing::error` `UNEXPECTED_TRANSIT` | exact | `unexpected_transit_false`; capaths-transit-gate.sh |
| kdc_preauth.c:1224-1228 | preauth verify fail LOG_INFO `preauth (%s) verify failure: %s` | INFO | krb5-kdc/preauth.rs send_spake_challenge PKINIT `tracing::error!` on expected CMS/cert/checksum/AuthPack | ERROR vs MIT INFO | deviation | propose demote PKINIT client fails to info; kdc-gate log |
| kdc_audit.c:143-171 + :178-210 + :258-341 | audit plugin: SHA256 tkt_id, req_id[32], cl_addr/port, stage, kau_as/tgs/s4u/u2u | plugin | audit.rs make_tkt_id; audit.rs new_req_id; audit.rs REQID_LEN; audit.rs TestAudit; audit.rs log_success | `tkt_out_id` = SHA-256 of ciphertext (64 hex upper); `req_id` 31 alphanum (`REQID_LEN` incl. NUL); stages 1–5; `to_json` emits a subset of `j_dict.h:37-88` (no `sess_etype`/`srv_etype`/`rep.ticket`/S4U user keys/…) | deviation | `a4_20_tkt_id_is_sha256_of_ticket_ciphertext`; `a4_20_test_audit_writes_mit_field_names`; `scripts/kdc-gate.sh` `RUST_audit_fields` / `MIT_audit_fields` |
| do_tgs_req.c:1181-1184 + :667; kdc_audit.c:258-341 | TGS seeds `kau_tgs_req(TRUE)` at `AUTHN_REQ_CL`; unknown server fail is `SRVC_PRINC`; S4U/U2U `event_name` | plugin | audit.rs seed_tgs_req; audit.rs tgs_fail_stage | seed stage 1 no tkt_out_id, same req_id as the finish record; LOOKING_UP_SERVER/UNKNOWN_SERVER stage 2; S4U2SELF/S4U2PROXY/U2U | exact | `f5_tgs_seed_is_authn_req_cl_without_tkt_out`; `f5_unknown_server_failure_is_srvc_princ`; `scripts/kdc-gate.sh` `MIT_tgs_audit_seed` / `RUST_tgs_audit_seed` |
| kdc_preauth.c:826-871 + :508-564 | PA-AS-FRESHNESS token 600s; stale/bad → internal 90 → wire 24 + fresh token; `pkinit_require_freshness` signed-only | PREAUTH_EXPIRED (filtered to 24) | krb5-kdc/preauth.rs mint_freshness_token; krb5-kdc/preauth.rs check_freshness_token; krb5-kdc/issue.rs preauth_hint_edata; krb5-protocol/as_ex.rs continue_pkinit | 600s ku 514; hint 150 last-before-cookie; require signed-only | exact | `a4_17_empty_150_hint_carries_populated_token`; `a4_17_stale_freshness_token_is_24`; diffsend `pkinit-stale-freshness`; `scripts/pkinit-gate.sh` / `scripts/rust-kinit-pkinit-gate.sh` require-freshness |
| kdc_preauth.c:902-904 | empty hint list LOG_INFO | INFO | plugins.rs advertise_preauth always ads 136+19+modules | n/a | exact | `ca_enabled_preauth_required_method_data_types` |
| n/a (harness compare; `diff.rs compare_preauth_e_data`) | PREAUTH 25/24/91/65 e_data type **multiset**; item 15 pins **wire order** via `kdc-padata-proxy` on the diffsend corpus | n/a | diff.rs compare_preauth_e_data; `preauth.rs` `pa_cookie_last` | type multisets equal; wire order 25/24 `[136,19,151,2,133]`, hw `[136,19,133]`, 91 `[151,19,133]` | exact | `as_spake_91_e_data_is_151_19_133`; `differential-gate.sh` corpus parse `pauser-no-preauth` / `as-spake-round1` |
| kdc_util.c:217-229 | header ticket **or** authenticator carries AD-FX-ARMOR (71) → ticket usable only as FAST armor (same family as A1 `kdc_util.c:217-229`) | `PROCESS_TGS` 12 POLICY (msg `ticket valid only as FAST armor`, log-only) | issue.rs process_tgs_header; issue.rs fx_armor_present; issue.rs find_authdata | PROCESS_TGS 12 (detail `ticket valid only as FAST armor`); IF-RELEVANT recurse; not applied to FAST armor AP-REQ | exact | `tgs_header_ticket_if_relevant_ad_fx_armor_is_policy`; diffsend `armor-ap-req-as-pa-tgs-req` |
| fast_util.c:62-67 | armor AP-REQ ticket server ≠ local TGS after `rd_req` (`krb5_principal_compare_any_realm`) | `FIND_FAST` 26 SERVER_NOMATCH (log `ap-request armor for something other than the local TGS`) | krb5-kdc/preauth.rs armor_key_from_ap; krb5-kdc/preauth.rs proto_fast | `FIND_FAST` 26 after decrypt (detail `FAST armor TGT`) | exact | `fast_as_armor_for_host_ticket_is_server_nomatch`; `docs/security.md:71-73` |
| fast_util.c:53-59 | armor `krb5_rd_req` failure (bad key / unknown server / expired / NYV starttime) | `FIND_FAST` + inner code (log `%s while handling ap-request armor`) | krb5-kdc/preauth.rs armor_key_from_ap; krb5-kdc/preauth.rs proto_fast | `FIND_FAST` 35 / 31 / 33 / 32 (NYV starttime is 33) | exact | `explicit_as_armor_future_starttime_is_tkt_nyv`; `explicit_as_armor_invalid_tgt_is_tkt_nyv`; `fast_as_corrupt_enc_fast_req_is_bad_integrity_find_fast` |
| fast_util.c:167-170 | unknown FAST armor type | `FIND_FAST` 24 (log `Unknown FAST armor type %d`) | krb5-kdc/preauth.rs proto_fast | `FIND_FAST` 24 (G3; detail `Unknown FAST armor type {}`) | exact | `fast_as_unknown_armor_type_is_preauth_failed`; `docs/security.md:84-85` |
| fast_util.c:225-228 (`UNSUPPORTED_CRITICAL_FAST_OPTIONS 0xbfff0000`) | critical fast-option bits 0 and 2..15 are refused; bit 1 = hide-client-names is **supported** (k5-int.h:803) | `FIND_FAST` 93 | krb5-kdc/issue.rs check_fast_options refuses bits 0 and 2..15 and skips bit 1 (R2-P4); krb5-kdc/preauth.rs proto_fast | `FIND_FAST` 93 for bits 0 and 2..15; bit 1 honoured | exact | `unknown_critical_fast_option_is_refused` (bits 0, 2); `fast_hide_client_names_returns_the_anonymous_outer_client` (bit 1) |
| do_as_req.c:831; do_tgs_req.c:235,1111 | `kdc_fast_hide_client` also anonymizes the client on a FAST **error** reply and on the TGS-REP / TGS error | anonymous client on FAST error and TGS replies | issue.rs `encode_krb_error` / TGS-REP outer names honor hide-client; inner FX-ERROR keeps the real client | anonymous client on FAST error and TGS replies | exact | `a4_16_fast_hide_as_error_client`; `a4_16_tgs_fast_hide_outer_tgs_rep`; diffsend `as-fast-hide-error-client`; diffsend `tgs-fast-hide-client` |
| fast_util.c:364-424 `kdc_fast_handle_error` | FAST error shape: inner padata = `[caller e_data…, PA-FX-COOKIE, PA-FX-ERROR]`, inner FX-ERROR e_data zeroed (`:384-386`), `strengthen_key = NULL`, `finished = NULL`; outer = single PA-FX-FAST | silent | issue.rs wrap_as_fast | inner FX-ERROR e_data none; cookie in FAST inner padata; FX_ERROR last | exact | `fast_error_inner_fx_error_omits_edata`; `encrypted_challenge_skew_is_fast_wrapped` |
| fast_util.c:588-590, :610 | non-`MIT1` or undecryptable cookie is **silently ignored** (`return 0` at :610 — the `PREAUTH_EXPIRED` set at :598 is dead code in 1.22.2) | none | krb5-kdc/preauth.rs open_cookie | ignored (empty state); SPAKE round 2 without state is 24 | exact | `spake_garbage_cookie_is_preauth_failed` |
| do_tgs_req.c:767 (`get_auth_indicators`, kdc_authdata.c:340-380) | extract verified auth indicators from the header ticket | `GET_AUTH_INDICATORS` | ad.rs get_auth_indicators; issue.rs issue_tgs_body | extract verified CAMMAC; S4U2Self skipped | exact | diffsend `tgs-truncated-cammac`; `scripts/cross-kdc-gate.sh` MIT SPAKE TGT through Rust TGS |
| kdc_authdata.c:484-493 | no PAC when the realm sets `disable_pac`, or the reply ticket is ANONYMOUS (still adds indicators) | skip PAC | issue.rs include_pac_for_reply; store.rs Policy.disable_pac | skip PAC | exact | `as_pac_request_false_and_disable_pac_omit_pac`; `disable_pac_from_kdcdefaults_and_realm` |
| kdc_preauth.c:769-829,1487-1495 | `add_etype_info`/`add_pw_salt`: AS-REP `padata` gets PA-ETYPE-INFO2 always, PA-ETYPE-INFO + PW-SALT for a des3/rc4-only request, **only when the reply key was not replaced** (`key_modified`); single entry, salt from the canonical client (`_make_etype_info_entry`) | `KDC_RETURN_PADATA` | issue.rs as_rep_key_info, enctype_requires_etype_info_2 | ETYPE-INFO2 + salt of the reply key; skipped on PKINIT/SPAKE | exact | `alias.rs as_rep_via_alias_carries_the_target_salt_in_etype_info2`; `scripts/kdb-dump-gate.sh:135` MIT `kinit a1` derives the target salt; `differential-gate.sh` `as-success` compares the full outer padata set |
| kdc_authdata.c:562; pac_sign.c:368-410,239-243; pac.c:583-592 `k5_pac_should_have_ticket_signature` | ticket (16) and full (19) checksums only for service tickets, never for `krbtgt/*` or `kadmin/changepw` | n/a (issue side) | krb5-kdc/ad.rs should_have_ticket_signature; krb5-kdc/ad.rs sign_pac; krb5-kdc/ad.rs verify_pac_signatures | n/a | exact | `tgt_pac_carries_no_ticket_or_full_checksum`; `tgt_pac_verifies_without_ticket_or_full_checksum`; `ticket_signature_predicate_matches_mit`; `scripts/cross-kdc-gate.sh` both directions |
| pac.c:281-317 `krb5_pac_parse` | version 0; 1..4096 buffers; 8-byte aligned offsets not inside the header, size within the PAC | EINVAL/ERANGE → 60 `GENERIC` | krb5-types/pac.rs parse; krb5-kdc/ad.rs map_pac_err | `HEADER_PAC` 60 `GENERIC` | exact | `pac_parse_rules.rs`; `accept_t_pac_fuzz_blobs_parse_or_truncate` |
| pac.c:137-147 `k5_pac_locate_buffer` | a buffer type present twice is EINVAL | EINVAL → 60 `GENERIC` | krb5-types/pac.rs unique_buffer; krb5-kdc/ad.rs verify_pac_checksums | `HEADER_PAC` 60 `GENERIC` | exact | `duplicate_signature_buffer_is_generic_60` |


## A4 — kadmin/server/*.c, lib/kadm5/*

kadm5.acl, kadmind RPC denials, and kpasswd (`schpw.c`) plus the I8
checksum/rc4/declared-cksumtype rows that sat under A3.

| MIT file:line | check (condition) | MIT status + wire code | Rust site | Rust e_text + code | verdict | proof |
| --- | --- | --- | --- | --- | --- | --- |
| auth_acl.c:330-338,472-545,638-648 | kadm5.acl target + restrictions; rename = delete src AND add dest without restrictions; unparseable target / unknown restriction is `acl_init` fail | AUTH_ADD / AUTH_INSUFFICIENT | acl.rs parse_line; acl.rs check; acl.rs check_rename | target-scoped; `-clearpolicy` imposed | exact | `acl_target_pattern_scopes_add_and_delete`; `scripts/kadmin-gate.sh` scoped addprinc/renprinc/restricted create; `acl_uppercase_letter_revokes`; J1a–J1c kadmin-gate both legs |
| checksum_hmac_md5.c:53-66 | `-137` MD5_HMAC_ARCFOUR uses the raw key; `-138` HMAC-MD5-ARCFOUR uses HMAC(key, `"signaturekey\0"`) | n/a | ops.rs hmac_md5_arcfour_checksum | `-137` raw key; `-138` KS | exact | `verify_checksum_type_md5_hmac_rc4_uses_raw_key` (unit; MIT clients emit `-138`) |
| enc_rc4.c:17-35 | RC4 usage `3→8, 9→9, 23→13` | n/a | weak.rs arcfour_translate_usage | 9 is 9 | exact | `arcfour_usage_9_is_9`; live rc4 both directions `w1h/rc4-1.log` / `rc4-2.log` (TGS-REP usage 9) |
| schpw.c:47-82,89-95,110-111,126-166,273-350,384-397; net-server.c:1103 | kpasswd pre-AP-REQ bailout (no datagram); AP-REQ `>=` remaining is bailout; AP-REQ fail `chpwfail` AUTHERROR 3 with `error_code` **60**; PRIV fail after AP-REQ KRB-PRIV HARDERROR 2; no per-listener rcache | no datagram / framed `chpwfail` 60 / two replies on UDP retransmit | krb5-admin/listen.rs handle_kpasswd_from | **60**; empty `e_text`; `e_data` result‖text | exact | `kpasswd_bad_ap_req_is_chpwfail_autherror`; `kpasswd_ap_req_fills_datagram_is_bailout`; `scripts/kpasswd-gate.sh` raw + fill + retransmit both legs |
| recvauth.c:132-138,150-186; rd_req.c:56-57 | kpropd junk AP-REQ: not APPLICATION 14 → `KRB_AP_ERR_MSG_TYPE`; else `problem - ERROR_TABLE_BASE_krb5` > 127 → **60**; `e_text = error_message` + NUL | **40** `Invalid message type\0` for `\xff\x00\x01` | kprop.rs kprop_rd_req_error | **40** + NUL | exact | `kpropd_ap_req_fail_is_krb_error`; `scripts/kprop-gate.sh` junk AP-REQ both legs |
| svc.c:328-367,486-520; rpc_callmsg.c:107-108; kadm_rpc_svc.c:80-88 | kadmind RPC: unknown program `PROG_UNAVAIL`; 2112 wrong vers `PROG_MISMATCH` low/high 2; AUTH_NONE `AUTH_TOOWEAK`; REPLY-typed no reply | accepted 1 / accepted 2+2+2 / denied AUTH_TOOWEAK 5 / idle | kadm5.rs handle_rpc | same | exact | `bad_program_is_prog_unavail`; `kadm_vers_99_is_prog_mismatch_2_2`; `reply_typed_rpc_is_no_reply`; `scripts/kadmin-gate.sh` framing both legs |
| crypto_int.h:596-608; krb5_c_verify_checksum | declared cksumtype `ctp` selects the verifier | n/a | krb5-crypto/ops.rs verify_checksum_type | declared type used | exact | `verify_checksum_type_honours_declared_unkeyed`; `verify_checksum_type_md5_hmac_rc4_uses_raw_key` |
| rd_req_dec.c:748-749 | AP-REQ authenticator checksum keyed with the ticket session key; GSS acceptor supplies empty `app_cksum` so a non-0x8003 type is verified over empty data | n/a | krb5-protocol/ap_req.rs verify_inner | session key; 0x8003 skipped | exact | `accept_non_8003_over_data_is_bad_sig`; `accept_non_8003_empty_with_subkey_uses_session_key` |
| rd_req_dec.c:325-347 `try_one_princ`, :374-385 `is_matching`, :118-148 `keytab_fetch_error`; kt_file.c:355-384 | ticket kvno + etype `krb5_kt_get_entry`; kvno 0 = any; a fully-specified name pins the exact kvno, a wildcard/host-based name (`is_matching`) iterates (`decrypt_try_server`) and tries similar-enctype keys regardless of kvno; an entry for the principal+enctype at another kvno is `KRB5_KT_KVNONOTFOUND` → `BADKEYVER` "Cannot find key for %s kvno %d in keytab" when the pinned name is the ticket server (`krb5_principal_compare`), no entry → `NOKEY` (explicit server) | BADKEYVER 44 / NOKEY 45 / BAD_INTEGRITY 31 | krb5-protocol/ap_req.rs verify_inner; krb5-admin/listen.rs handle_kpasswd_rfc3244 (`expected_server` = `kadmin/changepw`); krb5-gss/lib.rs accept_sec_context_kt; krb5-protocol/vfy_increds.rs verify_init_creds | skip non-matching kvno when `key_kvnos` is set (0 is wildcard); etype must match the ticket; `found_wrong_kvno` with nothing tried under a pinned name equal to the ticket sname → 44 with MIT's text, else 45; product GSS `accept_sec_context` leaves `key_kvnos` None (iteration), the `_kt` path pins only when `expected_server` is `Some` | exact | `b2_rd_req_kvno_mismatch_is_nokey` (no pinned name → 45); `b2_rd_req_relabeled_ticket_kvno_is_nokey`; `z1_pinned_name_wrong_kvno_is_badkeyver`; `z1_gss_accept_kt_pins_ticket_kvno`; `z1_gss_accept_kt_matching_kvno_verifies`; `scripts/client-differential-gate.sh` `MIT_gss_happy` / `MIT_vfy_increds_host` / `MIT_vfy_increds_outdated` + `RUST_vfy_increds_outdated` (both `Cannot find key for host/vfy.kerber.test… kvno 3 in keytab`, Rust as `KRB-ERROR 44`) / `RUST_z13_nyv_refused_kvno_refused` / `MIT_z13_nyv_refused_kvno_refused` (Rust `KRB-ERROR 44: Cannot find key for host/testhost.kerber.test kvno 99 in keytab`, MIT `mech: Cannot find key for host/testhost.kerber.test@KERBER.TEST kvno 99 in keytab` — MIT `gss-server` with a fully-qualified acceptor name pins like Rust; `security.md` records the exact-only corner) |
| s4u_creds.c:1013-1031,1195-1262 | `kvno -U -P` S4U2Proxy: `CNAME_IN_ADDL_TKT`, evidence ticket, PA-PAC-OPTIONS RBCD | n/a | krb5-protocol/tgs.rs tgs_s4u2proxy | FAST outer `[1, 136, 167]` | exact | `b2_s4u2proxy_outer_padata_is_1_136_167`; `scripts/client-differential-gate.sh` `MIT_kvno_P_tgs_padata` |
| val_renew.c:62-67,116-121 | `kinit -v` `KDC_OPT_VALIDATE` plus `old_creds.ticket_flags & KDC_TKT_COMMON_MASK`; no `CANONICALIZE` | n/a | krb5-protocol/tgs.rs tgs_validate_options | common mask + VALIDATE | exact | `b2_validate_options_are_common_mask_plus_validate`; `scripts/client-differential-gate.sh` `MIT_kinit_validate` |
| valid_times.c:36-58; rd_req_dec.c:627,635-636 | acceptor `krb5int_validate_times`: NYV against starttime else authtime, then endtime expiry; after the time checks the `TKT_FLG_INVALID` flag maps to the library-local `KRB5KRB_AP_ERR_TKT_INVALID` (`krb5_err.et` offset 145, clamped to 60 on the wire by `errcode_to_protocol`) | 33 `TKT_NYV` / 32 `TKT_EXPIRED` / 145 `TKT_INVALID` | krb5-protocol/ap_req.rs verify_inner | starttime else authtime for NYV; endtime for expiry; INVALID → 145 after the time checks | exact | `z1_no_starttime_future_authtime_is_nyv`; `z1_invalid_flag_is_tkt_invalid`; `scripts/client-differential-gate.sh` `RUST_z13_nyv_refused_kvno_refused` / `MIT_z13_nyv_refused_kvno_refused` |
| rd_req_dec.c:590-610; chk_trans.c:309-355 | acceptor transited re-check when `TRANSITED_POLICY_CHECKED` is unset and the field is non-empty | 43 `ILL_CR_TKT` | krb5-protocol/ap_req.rs check_ap_req_transited | `ILL_CR_TKT` 43; T flag or empty contents skip; anonymous crealm skips | exact | `b2_rd_req_transited_unchecked_evil_hop_is_ill_cr_tkt`; `scripts/client-differential-gate.sh` `MIT_gss_happy` |
| gc_via_tkt.c:247-297,108-110 | TGS-REP client vs TGT client (S4U2Self NOSUPP when client == requested server; S4U2Proxy final hop skips); ticket server == enc server; request till/rtime/from; strip `ok-as-delegate` on a foreign TGT that lacks the flag | `KDCREP_MODIFIED` / `PADATA_TYPE_NOSUPP` | krb5-protocol/tgs.rs tgs_reply_client_ok; tgs.rs tgs_reply_server_consistent; tgs.rs tgs_reply_req_times; tgs.rs tgs_strip_ok_as_delegate | `ReplyMismatch`; O cleared on foreign TGT without O | exact | `b2_tgs_reply_wrong_client_is_modified`; `scripts/client-differential-gate.sh` `MIT_tgs_reply_client` |
| gc_via_tkt.c:302-307 | TGS-REP starttime vs request timestamp ± clockskew when request starttime is 0 | `KDCREP_SKEW` | (absent; no `krb5_context` `time_offset`) | not applied | deferred | none (promotion: per-context `time_offset` from AS `kdc_timesync`; +3d `kinit` then `kvno` is the oracle) |
| sname_match.c:30-57; rd_req_dec.c:382,397 | `krb5_sname_match`: NULL matching accepts any; NT-SRV-HST + 2 comps checks realm/service and hostname unless empty or `ignore_acceptor_hostname` | 35 `NOT_US` | krb5-protocol/ap_req.rs sname_match | same; default ignore false from `[libdefaults]` | exact | `b2_sname_match_ignore_acceptor_hostname_skips_host`; `scripts/client-differential-gate.sh` `MIT_sname_match` |
| get_creds.c:503-516 | first referral TGS error + specified server realm → non-referral retry without `CANONICALIZE` | same KDC error if retry fails | krb5-protocol/tgs.rs tgs_try_fallback; tgs.rs tgs_service_once | `NonReferral` then `tgs_non_referral_options` | exact | `b2_try_fallback_specified_realm_is_non_referral`; `scripts/client-differential-gate.sh` `MIT_try_fallback` |
| get_creds.c:517-542 | first referral TGS error + referral realm → `krb5_get_fallback_host_realm` then maybe rewrite server realm | `HOST_REALM_UNKNOWN` / second TGS | (absent; no host-realm DNS modules) | keep the first KDC error; `< 2` comps is `ReplyMismatch` host realm unknown | deferred | none (promotion: B3 `hostrealm.c` / `krb5_get_fallback_host_realm`; MIT `t_hostrealm.py` is the oracle) |
| fwd_tgt.c:147-153 | `krb5_fwd_tgt_creds` options: `ticket_flags & KDC_TKT_COMMON_MASK` plus `FORWARDED`; `forwardable==0` clears `FORWARDABLE` | n/a | krb5-protocol/tgs.rs tgs_forward_options | common mask + FORWARDED; no CANONICALIZE | exact | `b2_fwd_tgt_options_are_common_mask_plus_forwarded`; `scripts/client-differential-gate.sh` `MIT_fwd_tgt` |
| fwd_tgt.c:123-145 | when the TGT has addresses, `k5_os_hostaddr(rhost)` (or NT-SRV-HST server hostname) is placed on the FORWARDED TGS | n/a | (absent; no OS hostaddr) | request `addresses` stay empty | deferred | none (promotion: B3 `localaddr.c` / `k5_os_hostaddr`; `scripts/gss-gate.sh` delegated `klist -f` `f` is the oracle once addresses are filled) |
| rd_req_dec.c:631 | authenticator `ctime` vs now ± `clockskew` | 37 `SKEW` | krb5-protocol/ap_req.rs verify_inner | `SKEW` 37 | exact | `b2_rd_req_authenticator_skew_is_37` |
| rd_req_dec.c:620-625 | rcache store of the authenticator ciphertext | 34 replay | krb5-protocol/ap_req.rs verify_inner | 34 `authenticator replay` | exact | `scripts/client-differential-gate.sh` `MIT_gss_replay_token` / `GSS_replay_major_34` |
| get_creds.c:609; k5-int.h:2061 | `KRB5_REFERRAL_MAXHOPS` 10; start-realm bounce is unreachable | `KDC_UNREACH` / mismatch | krb5-protocol/tgs.rs chase_step | `ReplyMismatch` start realm / loop; 10 hops | exact | `chase_step_start_realm_referral_is_reply_mismatch` |
| get_creds.c:987-1038 | `begin_get_tgt` dest TGT then closer hops along `k5_client_realm_path` | n/a | krb5-protocol/tgs.rs get_dest_tgt | dest first, then closer | exact | `closer_hop_dest_first_then_capaths` |
| princ_comp.c:154-160 | `krb5_principal_compare` name-string + realm; name-type ignored | n/a | krb5-protocol/tgs.rs tgs_reply_client_ok | name-string + realm | exact | `b2_tgs_reply_wrong_client_is_modified`; `b2_sname_match_non_hst_is_principal_compare` |
| mk_req_ext.c:76-80, generate_authenticator | AP-REQ authenticator subkey, seq, checksum | n/a | krb5-protocol/ap_req.rs build_ap_req_with_cksum | subkey + seq + cksum | exact | `accept_non_8003_empty_with_subkey_uses_session_key`; `scripts/gss-gate.sh` |
| mk_rep.c:143; rd_rep.c:69 | AP-REP echoes authenticator `ctime`/`cusec` | n/a | krb5-protocol/ap_rep.rs build_ap_rep; ap_rep.rs verify_ap_rep | echo `ctime`/`cusec` | exact | `ap_rep_mutual_and_safe_priv` |
| mk_cred.c:215; rd_cred.c:143 | `krb5_mk_1cred` / `krb5_rd_cred` KRB-CRED | n/a | krb5-protocol/safe_priv.rs build_krb_cred | KRB-CRED wrap | exact | `accept_plaintext_krb_cred_is_refused`; `delegation_sets_the_deleg_flag`; `scripts/gss-gate.sh` |
| srv_rcache.c; rc_file2.c | on-disk `dfl` rcache format and restart | 34 replay | (absent; `ReplayCache` is process-local) | in-memory only | deferred | none (promotion: B3 `rc_file2.c`; `scripts/gss-gate.sh` replay 34 is the in-process oracle) |
| get_creds.c:135-152 `k5_get_cached_cred` | retrieve a matching service ticket from the ccache before TGS | n/a | (absent on the `kvno` path; FILE ccache store exists) | every `kvno` hits the KDC | deferred | none (promotion: B3 `cc_retr.c`; `scripts/ccache-gate.sh` is the oracle) |
| s4u_creds.c:1050-1099 | S4U2Proxy referral walk when the evidence ticket is foreign | `WRONG_PRINC` / next TGS | (absent; `kvno -P` is same-realm) | same-realm only | deferred | none (promotion: cross-realm `kvno -U -P`; `scripts/s4u-mit-gate.sh` xrealm cells) |
| auth_con.c | full `krb5_auth_context` flag/address/rcache API | n/a | (absent as a standalone object; GSS holds seq/subkey/replay) | GSS subset only | deferred | none (promotion: library `krb5_auth_context`; `scripts/gss-gate.sh` is the product oracle) |
| accept_sec_context.c:464-618,571-577,380-384,1089 | `process_checksum` + established flags: missing cksum → flags 0 and no AP-REP; non-0x8003 REPLAY\|SEQUENCE\|MUTUAL; `cb_len != 16` failure; all-zero token CB accepted; mismatch bindings; `INITIATOR_FLAGS`; `GSS_C_CHANNEL_BOUND`; `option_id == 1`; `GSS_C_DELEG_FLAG` after a stored delegation; `GSS_EXTS_FINISHED` (2) → failure; bad KRB-CRED → failure; `GSS_C_PROT_READY_FLAG` on the established context | n/a | krb5-gss/lib.rs process_checksum; krb5-gss/lib.rs accept_sec_context_kt | `gss failure`; CHANNEL_BOUND 0x0800; DELEG 0x1; PROT_READY 0x80 | exact | `unwrap_conf_reports_confidentiality`; `delegation_sets_the_deleg_flag`; `acceptor_sets_prot_ready_flag`; `accept_plaintext_krb_cred_is_refused`; `scripts/gss-gate.sh` deleg flag both legs |
| rd_safe.c:43-125 | KRB-SAFE checksum over the received body DER (`krb5_safe_with_body`); APPLICATION 20 is required; `k5_privsafe_check_addrs` before checksum; dummy then RFC 1510 body | n/a | krb5-protocol/safe_priv.rs unwrap_krb_safe_ex; krb5-protocol/safe_priv.rs verify_krb_safe_checksum; krb5-protocol/safe_priv.rs check_privsafe_addrs | dummy then body; 40 `MSG_TYPE` / 38 `BADADDR` / 15 `SUMTYPE_NOSUPP` / 50 `INAPP_CKSUM` / 41 `MODIFIED` | exact | `accept_noncanonical_seq_integer_body`; `build_krb_safe_signs_the_full_dummy_not_the_body_alone`; `reject_non_safe_application_tag_is_msg_type_40`; `scripts/rd-safe-oracle-gate.sh` `SAFE_NONCANON_BODY_OK`; `scripts/kprop-gate.sh` `SUCCEEDED` both legs |
| privsafe.c:366-375 `k5_privsafe_check_addrs` | when no local address is set, walk `krb5_os_localaddr` and reject an r-address not among them | 38 `BADADDR` | krb5-protocol/safe_priv.rs check_privsafe_addrs (accepts r-address when `local` is None) | `BADADDR` only when a local address is supplied | deviation | `docs/security.md`; no product path emits an r-address (kprop/kpasswd), and the protocol crate has no OS address enumeration |
| rd_priv.c:77-78 | `k5_privsafe_check_addrs` on KRB-PRIV `s_address`/`r_address` | 38 `BADADDR` | krb5-protocol/safe_priv.rs unwrap_krb_priv_chained (no addresses threaded) | address check absent on the KRB-PRIV path | deviation | `docs/security.md`; no product KRB-PRIV path (kpasswd) sets addresses |
| generic/util_seqstate.c:84-117 `g_seqstate_check` | `GSS_S_COMPLETE` when neither REPLAY nor SEQUENCE negotiated; else GAP/UNSEQ/OLD/DUPLICATE as supplementary status with the plaintext delivered | supplementary | krb5-gss/lib.rs accept_seq (enforces a window unconditionally, drops the message) | out-of-order rejected always | deviation | `docs/security.md`; MIT peers always negotiate REPLAY\|SEQUENCE so the window is enforced identically for them; `wrap_mic_replay_inside_window_is_rejected` |
| unwrap.c:191-240,272-371 | GSS wrap unwrap: direction, filler, EC strip, `verify_enc_header` (RRC not compared) | n/a | krb5-gss/lib.rs unwrap_v3 | direction Integrity; filler/EC Truncated; EC stripped | exact | `unwrap_conf_ec_padding_is_stripped`; `unwrap_direction_flipped_is_bad_sig`; `rrc_reduces_modulo_the_payload_length`; `scripts/gss-gate.sh` DCE + mutation cells both legs |
| unwrap.c:363-364; authgss_prot.c:238-240 | `unwrap` reports `conf_state`; the RPCSEC_GSS privacy service rejects an integrity-only body | n/a | krb5-gss/lib.rs unwrap_conf; krb5-admin/kadm5.rs rpcsec_dispatch (privacy arm requires conf) | privacy body not sealed → GARBAGE_ARGS | exact | `unwrap_conf_reports_confidentiality`; `scripts/kadmin-gate.sh` privacy mode both legs |
| unwrap.c:255-263 `rotate_left` | RRC reduced modulo the payload length | n/a | krb5-gss/lib.rs rotate_rrc; krb5-gss/lib.rs apply_send_rrc | rrc %= len | exact | `rrc_reduces_modulo_the_payload_length` |
| accept_sec_context.c:1021-1060,1101-1110; mk_rep.c:78-111; init_sec_context.c:632-640,798-820 | mutual AP-REP: for CFX (`proto == 1`) the acceptor always sets `KRB5_AUTH_CONTEXT_USE_SUBKEY`, `krb5_mk_rep` puts a random `seq-number` (`krb5_generate_seq_number`) and a fresh acceptor subkey in the AP-REP, `seq_send` is that seq and both peers key wrap/MIC with the acceptor subkey under `FLAG_ACCEPTOR_SUBKEY`; without mutual `seq_send = seq_recv` (the authenticator seq) and the initiator seeds `seq_recv` from its own seq; the initiator takes `seq_recv` and the subkey from the AP-REP; a `KG_TOK_CTX_ERROR` (0x0300) reply is a KRB-ERROR | `GSS_S_COMPLETE`; `GSS_S_DEFECTIVE_TOKEN` on a non-AP-REP | krb5-gss/lib.rs accept_sec_context (AP-REP `seq` 0, no subkey, `send_seq` 0); krb5-gss/lib.rs process_ap_rep (initiator consumes an acceptor subkey and `FLAG_ACCEPTOR_SUBKEY`) | acceptor AP-REP `seq-number` 0 with no acceptor subkey, tokens keyed with the initiator subkey from seq 0 in both modes; initiator side exact | deviation (acceptor: no acceptor subkey, fixed initial seq, non-mutual `seq_send` not seeded from the authenticator seq — an initiator that requested SEQUENCE without MUTUAL expects acceptor tokens from its own seq); exact (initiator) | `process_ap_rep_acceptor_subkey_unwrap`; `scripts/gss-gate.sh:90-130` MIT MUTUAL\|CONF\|INTEG initiator → Rust acceptor wrap round trip; `:171-238` Rust initiator → MIT acceptor (acceptor subkey consumed); `scripts/kadmin-gate.sh` RPCSEC_GSS both legs |
| wrap_size_limit.c:98-150 `krb5_gss_wrap_size_limit` | CFX: largest input whose `krb5_encrypt_size(sz) + 16 ≤ req_output_size`, minus 16 for the encrypted header copy; integrity: `req_output_size - 16 - cksumsize` | n/a | (absent; krb5-gss/lib.rs wrap_iov_length gives the exact header/padding/trailer sizes) | no `wrap_size_limit` entry point | absent | static: `wrap_iov_length` (`wrap_iov_slices_match_wrap_token`); no product caller sizes a payload by `gss_wrap_size_limit` |
| k5sealiov.c:57-110,350-425; k5unsealiov.c:230-310,381 | wrap_iov: `SIGN_ONLY` buffers are checksummed, not encrypted; CFX EC is the block size (or `k5_padlen`) in header/trailer instead of padding; DCE (`GSS_C_DCE_STYLE`): the caller pads (`gss_padlen` 0, `conf_data_length % k5_padlen` is `KRB5_BAD_MSIZE`), the token is `TOKEN \| DATA` with RRC rotating the trailer into the header, no padding buffer needed for MIC/DCE; unwrap_iov locates the header, honours `SIGN_ONLY` as associated data and reduces RRC | n/a | krb5-gss/lib.rs wrap_iov; krb5-gss/lib.rs wrap_iov_integ; krb5-gss/lib.rs unwrap_iov_sealed; krb5-gss/lib.rs unwrap_iov_integ; krb5-gss/lib.rs rotate_rrc | same slices as wrap; sign-only buffers MAC-only; DCE RRC unwrap; IOV integrity accepted for AES (RFC 4121/8009) only | exact (AES); deviation (non-AES IOV sign-only/integrity refused) | `wrap_iov_slices_match_wrap_token`; `wrap_iov_sign_only_is_checksummed_not_encrypted`; `wrap_iov_rfc8009_sign_only_round_trips`; `wrap_iov_integ_aes_round_trips`; `unwrap_iov_integ_rejects_non_aes`; `wrap_iov_hostile_header_is_truncated`; `scripts/gss-gate.sh:265-319` MIT `gss_wrap_iov` and `SIGN_ONLY` → Rust `unwrap_iov`; `:403-460` MIT DCE `wrap_iov` → Rust unwrap and → MIT unwrap |
| spnego_mech.c:1331-1346 `acc_ctx_new`, 3557-3578 `negotiate_mech`, 539-640 `handle_mic` / `process_mic` | acceptor picks the first initiator mech it supports; first in the list → `ACCEPT_INCOMPLETE` with the optimistic token consumed, later in the list → `REQUEST_MIC` (token discarded, second leg) and `mic_reqd`; none → `REJECT` `GSS_S_BAD_MECH`; a received `mechListMIC` is verified over `DER_mechTypes` and then a MIC must be sent; a MIC is sent only when `mic_reqd`; a MIC after a MIC or a missing required MIC is `GSS_S_DEFECTIVE_TOKEN` | `accept-completed` / `accept-incomplete` / `request-mic` / `reject` | krb5-gss/lib.rs spnego_accept; krb5-gss/lib.rs mech_list_has_krb5; krb5-gss/lib.rs verify_spnego_mic | krb5 must be in `mechTypes` (else refused); the optimistic token is consumed as krb5 wherever krb5 sits in the list; an incoming MIC is verified over the DER list; a MIC is always sent with `accept-completed`; the initiator's follow-up MIC is verified by `verify_spnego_mic` | deviation (single-leg acceptor: no `request-mic` state, MIC unconditional; the downgrade guard is moot for a krb5-only acceptor because a krb5-less list is refused) | `spnego_accept_emits_neg_token_resp`; `spnego_accept_rejects_mech_list_without_krb5`; `spnego_duplicate_mech_types_is_truncated`; `scripts/gss-gate.sh:239-264` MIT SPNEGO initiator → Rust acceptor (MIC exchanged, wrap round trip); `scripts/gss-sspi-gate.sh` |
| kproplog.c:316-420 `print_update`, 473-561 (`main`: `-R`, `-e N`, `-h`, `-v`) | `kproplog` prints the ulog header (`Kerberos update log (%s)`, `Update log dump :`, `\tLog version # : %u`, `\tLog state : `, `\tEntry block size : %u`, `\tNumber of entries : %u`, `\tFirst serial # : `, `\tLast serial # : `, `\tLast time stamp : %s`, `None` when empty) and, `-v`, each entry's serial, time, size and `kdb_incr_update_t` fields; `-R` resets the ulog | text | (absent; the Rust ulog is read only through the iprop RPC) | no `kproplog` CLI | deferred | none (promotion: a `krb5-kproplog` over `UlogEntry`; oracle MIT `kproplog -v` on the MIT ulog after `scripts/iprop-gate.sh` pulls both ways) |
| kadm5_hook.c:40-96 `k5_kadm5_hook_load`, 142-194 (`k5_kadm5_hook_chpass` / `create` / `modify` / `rename` / `remove` / `alias`); plugins/kadm5_hook/test/main.c | kadm5_hook plugins: every chpass, create, modify, rename, remove and alias is offered to each module at `KADM5_HOOK_STAGE_PRECOMMIT` (a module error aborts the operation with that code, nothing is written) and again at `POSTCOMMIT` after the kdb write | module code | (absent; the KDC-side plugin traits live in `krb5-kdc` `plugins.rs`, no kadm5 registry) | no kadm5_hook stage | deferred | none (promotion: the decided `kadm5_hook` trait registry; oracle MIT `kadmind` with the `test` hook module from the image, `scripts/kadmin-gate.sh`) |
| kadmin/server/auth.c:63-120 `auth_init`, 185-200 (the per-verb loop: `call_module` per handle), 307 `auth_end`; ovsec_kadmd.c:498; auth_acl.c, auth_self.c, plugins/kadm5_auth/test/main.c | pluggable kadm5_auth: the built-in `acl` and `self` modules and any loaded module answer each verb; one module returning 0 authorizes, `KRB5_PLUGIN_NO_HANDLE` abstains, any other code denies at once; restrictions come from the allowing module | `KADM5_AUTH_*` | krb5-admin/kadm5.rs is_self; krb5-kdc/acl.rs (the `acl` module's decisions, rows above) | the `acl` and `self` decisions are built in; no third-module registry | deferred | none (promotion: the decided `kadm5_auth` trait registry; oracle MIT `kadmind` with the `test` auth module from the image, `scripts/kadmin-gate.sh`; the `acl` and `self` outcomes are already live in `scripts/kadmin-gate.sh`) |
| server_init.c:87-160 (`kadm5_init_with_password` / `_anonymous` / `_with_creds` / `_with_skey` → `kadm5_init`), 158-260 (`CHECK_VERSIONS` `KADM5_OLD_SERVER_API_VERSION..KADM5_NEW_SERVER_API_VERSION`, `kadm5_get_config_params`, `kdb_init_master` 252, `kdb_init_hist` 258), 348 `kadm5_init_iprop` | local handle setup: struct/API version words checked first (`KADM5_BAD_STRUCT_VERSION` / `KADM5_BAD_API_VERSION`), config params from `[kdcdefaults]`+`[realms]`, master key and history key loaded before the first verb; the ACL is `auth_init` in kadmind, not the library | `KADM5_BAD_*_VERSION`, `KADM5_MISSING_KRB5_CONF_PARAMS` | krb5-admin/kadm5.rs encode_init_res (the RPC INIT reply and API version word, row `server_stubs.c` above); krb5-kdc/acl.rs | version words and the ACL/history/master key paths are exact where already rowed; the remaining `kadm5_init_*` local-handle variants are not a surface (the Rust kadmind has one process-wide handle) | deferred | none (promotion: none planned; oracle `scripts/kadmin-local-gate.sh` and `scripts/kadmin-gate.sh` INIT cells) |
| kdb5_util.c:1-420 (`cmd_table`: `create`, `destroy`, `stash`, `dump`, `load`, `ark`, `add_mkey`, `use_mkey`, `list_mkeys`, `update_princ_encryption`, `purge_mkeys`, `tabdump`), kdb5_create.c, kdb5_destroy.c, kdb5_stash.c, kadm5_create.c, tabdump.c, tdumputil.c | `kdb5_util` verbs and texts (`Loading random data`, `Initializing database '%s' for realm '%s',\nmaster key name '%s'`, `-s` stash, `destroy` `-f`, `tabdump` tables) | text | krb5-kdb.rs cmd_create; krb5-kdb.rs cmd_load; krb5-kdb.rs cmd_dump; krb5-kdb.rs cmd_stash | `create`/`load`/`dump`/`stash` only, own texts; `dump`/`load` bytes are exact both ways (`dump.c` rows above); master-key rollover is the standing non-goal | deferred | none (promotion: verb texts; oracle MIT `kdb5_util` in `scripts/kdb-dump-gate.sh`) |
| kprop_util.c:27-99 `sockaddr2krbaddr`; kpropd_rpc.c:34-61 (`iprop_get_updates_1`, `iprop_full_resync_1` client stubs) | kprop/kpropd helper: `sockaddr2krbaddr` maps the peer `AF_INET`/`AF_INET6` to `ADDRTYPE_INET`/`INET6`; kpropd's iprop client calls `IPROP_GET_UPDATES` / `IPROP_FULL_RESYNC_EXT` through the generated RPC stubs | n/a | krb5-iprop-pull.rs main (the Rust iprop puller) | the Rust kpropd does not run an iprop client (the puller is a separate binary); no address is compared on the kprop path (`kpropd.c:1202` row above) | deferred | none (promotion: kpropd `-S`-less iprop mode; oracle `scripts/iprop-gate.sh` MIT ↔ Rust serial deltas both ways) |
| disp_status.c:157-195 `krb5_gss_display_status`; disp_major_status.c:60-80 (the `routine_error` table), 206-301 `g_display_major_status` | `gss_display_status` texts: major codes are the fixed routine-error table (`A token was invalid`, `A token had an invalid signature`, `The context has expired`, `No credentials were supplied`, `Miscellaneous failure`, …) with calling-error and supplementary words, minors are `krb5` / `com_err` mechanism messages (`A token had an invalid Message Integrity Check`, `Invalid token was supplied`) | text | krb5-gss/lib.rs Error | the Rust `Error` variants carry their own text; the wire-affecting codes are exact | deferred | none (promotion: a `display_status` table; oracle `scripts/gss-gate.sh:535-538` mutation cells settle MIT `gss_display_status` texts live against the Rust helpers' verdicts) |
| acquire_cred.c:1-1370, cred_store.c, store_cred.c, import_cred.c, export_cred.c, lucid_context.c, iakerb.c, export_sec_context.c / import_sec_context.c / ser_sctx.c | credential store (`gss_acquire_cred_from` `ccache` / `client_keytab` / `keytab` / `password` / `rcache` keys), `gss_store_cred_into`, `gss_export_cred` / `gss_import_cred`, `gss_krb5_export_lucid_sec_context` (v1 struct), IAKERB (`gss_mech_iakerb` AS/TGS proxying inside GSS tokens), exported context tokens | `GSS_S_NO_CRED` / `GSS_S_UNAVAILABLE` / `GSS_S_DEFECTIVE_TOKEN` | krb5-gss/lib.rs export_sec_context; krb5-gss/lib.rs import_sec_context (own format); the helpers take `--ccache PATH` and a keytab on argv | no cred-store keys, no lucid export, no IAKERB (`iakerb.c` is the deferred row: the Rust acceptor treats `GSS_EXTS_FINISHED` as IAKERB-only and does not speak IAKERB); the exported-context token is Rust-private, not MIT `ser_sctx.c` | deferred | none (promotion: cred-store + lucid are the promoted parity items, IAKERB stays deferred; oracle `scripts/gss-gate.sh` MIT helpers with cred-store and lucid modes once they exist; `export_import_wrap_still_works` for the Rust-private token) |
| lib/rpc/auth_gssapi.c, auth_gss.c, clnt_tcp.c, xdr_rec.c (the client side); kadmin/cli/kadmin.c `kadmin_startup` (remote) | the remote `kadmin` client: AUTH_GSSAPI / RPCSEC_GSS client handshake, record-marking TCP transport, `kadmin -p/-k/-c/-w/-O/-N/-s host:port`, the client-side texts (`kadmin: … while initializing kadmin interface`) | text | (absent; the Rust admin CLI is `krb5-kadmin-local` over the kdb, and `krb5-kpasswd` speaks kpasswd only) | no Rust remote `kadmin`; the Rust kadmind is exercised by MIT `kadmin` | deferred | none (promotion: the promoted remote `kadmin` client, last in C; oracle `scripts/kadmin-gate.sh` MIT `kadmin` → Rust kadmind and, once the client exists, Rust `kadmin` → MIT kadmind as the reverse leg) |
| pac.c:478-579 | `verify_pac_checksums` over the received PAC; zero server+privsvr in a copy; privsvr over the server buffer minus the 4-byte type (RODC trailer kept); missing buffer ENOENT; server verify does not abort before privsvr | HEADER_PAC 41 `MODIFIED` / 60 `GENERIC` | krb5-kdc/ad.rs verify_pac_checksums; krb5-kdc/ad.rs verify_pac_sig; krb5-types/pac.rs received_zeroed | `HEADER_PAC` 41 `MODIFIED` / 60 `GENERIC` | exact | `accept_rodc_trailer_privsvr_covers_server_buffer_minus_type`; `accept_missing_privsvr_buffer_is_generic_60`; `accept_t_pac_saved_pac_verifies`; `accept_wrong_server_key_still_checks_privsvr` |
| pac.c:640-673 | ticket checksum over the recoded EncTicketPart with PAC ad-data a single 0x00, then `verify_pac_checksums` with `expect_full` | HEADER_PAC | krb5-kdc/ad.rs verify_pac_signatures; krb5-kdc/ad.rs ticket_checksum_der | `HEADER_PAC` | exact | `issued_pac_self_verifies_all_four_signatures`; `accept_t_pac_saved_pac_verifies` |
| server_stubs.c: add/delete ACL denial | add/delete ACL denial | `KADM5_AUTH_ADD` / `KADM5_AUTH_DELETE` | krb5-admin/kadm5.rs dispatch_kadm5_ticket | create path `AUTH_ADD`; some denials still `AUTH_GET` | deviation | W1-C; `acl_target_pattern_scopes_add_and_delete` (create is AUTH_ADD) |
| kadm_rpc_svc.c:80-88,324-331 | kadm5 acceptor: AUTH_GSSAPI or `check_rpcsec_auth` (2 comps, realm, `kadmin`, not `history`) else `svcerr_weakauth` | RPC AUTH_TOOWEAK | kadm5.rs check_rpcsec_auth via acceptor_realm_ok; kadm5.rs kadm5_rpcsec_ok | kiprop/history/1-comp weakauth; realm-qualified (`ticket_realm == store_realm`), bound at `accept_sec_context` and re-checked at the gate | exact | `check_rpcsec_auth_rejects_kiprop_history_and_one_component` (calls `check_rpcsec_auth` / `check_iprop_rpcsec_auth`); `crates/krb5-admin/tests/sp01_acceptor.rs`; `scripts/kadmin-gate.sh` kiprop `--service` both legs |
| server_stubs.c:28-32; ovsec_kadmd.c:468-477 | CHANGEPW_SERVICE compares the full realm-qualified acceptor name (`cmp_gss_names` = `gss_compare_name`) vs `kadmin/changepw@<realm>`; AUTH_GSSAPI names built with `params.realm`; `gss_oldchangepw_name` unset | n/a (authz predicate) | kadm5.rs changepw_acceptor; kadm5.rs check_auth_gssapi_names via acceptor_realm_ok | realm-qualified; realm bound at accept, unreachable via foreign ticket (INIT-fail first) | exact | `changepw_acceptor_requires_store_realm`; `kadm5_auth_gssapi_ok_requires_store_realm` (foreign-realm cell not realizable — `working/logs/audit-polish-0902/sub-plan-01/notes-acceptor-realm.md`) |
| ipropd_svc.c:481-516,542-548 | iprop program: flavor must be RPCSEC_GSS; acceptor 2 comps, `kiprop`, realm; else `svcerr_weakauth` | RPC AUTH_TOOWEAK | kadm5.rs handle_auth_gssapi; kadm5.rs check_iprop_rpcsec_auth; krb5-admin/kadm5.rs rpc_reply_weakauth | AUTH_GSSAPI INIT is auth-layer SUCCESS; DATA without context AUTH_FAILED; established AUTH_GSSAPI DATA and a `kadmin/admin` RPCSEC acceptor are AUTH_TOOWEAK; `kiprop` RPCSEC dispatches | exact | `auth_gssapi_on_iprop_init_is_success`; `auth_gssapi_on_iprop_data_is_auth_failed`; `kiprop_acceptor_requires_store_realm`; `scripts/kadmin-gate.sh` iprop program cells (kiprop `iprop-valid` SUCCESS, kadmin-acceptor `iprop-valid` AUTH_TOOWEAK, `iprop-auth-gssapi` AUTH_TOOWEAK, AUTH_NONE AUTH_TOOWEAK) both legs, MIT on `iprop_port` |
| ovsec_kadmd.c:155-169; alt_prof.c:699-700 | KADM 2112 on `kadmind_port`; IPROP 100423 registered only when `iprop_enable`, with an extra listener on `iprop_port`; gssrpc's program registry is process-wide, so an enabled IPROP also answers on `kadmind_port` | PROG_UNAVAIL on 749 while iprop is disabled; AUTH_TOOWEAK (dispatched) once enabled | kadm5.rs handle_rpc | 100423 always on the kadmind port; no `iprop_enable` knob and no separate `iprop_port` listener (`settle-mit-iprop-port.log` shows MIT's 2121) | deviation | `scripts/kadmin-gate.sh` MIT AUTH_NONE IPROP on `$KADMIND_PORT` and on `$MIT_IPROP_PORT` both AUTH_TOOWEAK; the sub-plan-02 gate log holds the iprop-disabled PROG_UNAVAIL |
| ipropd_svc.c:147-192,312-320; iprop.x:208-211; kpropd.c:918-921 | `iprop_acl_check` denies GET_UPDATES and FULL_RESYNC with `UPDATE_PERM_DENIED`; `kdb_fullresync_result_t` `{lastentry, ret}`; kpropd logs `get_updates permission denied` | UPDATE_PERM_DENIED | kadm5.rs dispatch_iprop; kadm5.rs encode_fullresync_status | same XDR; kiprop without `p` is denied at GET_UPDATES | exact | `iprop_fullresync_deny_is_fullresync_result`; `scripts/iprop-gate.sh` `fullresync_status=5` both legs and MIT kpropd `get_updates permission denied` vs the Rust master (`settle-mit-kpropd-deny.log`) |
| svc_auth_gssapi.c:616-623 | AUTH_GSSAPI DESTROY is answered in the auth layer (`no_dispatch`, `destroy_client`) before any program dispatch | accepted 0, context dropped | kadm5.rs handle_auth_gssapi (DESTROY before the iprop weakauth) | same | exact | `auth_gssapi_destroy_on_iprop_is_auth_layer` |
| ovsec_kadmd.c:472 setup_kdb_keytab; ipropd_svc.c:501-504 | kadmind accepts with `GSS_C_NO_NAME` against the whole KDB, then gates by acceptor name | any DB principal is an acceptor | krb5-kadmind.rs acceptor_keys | the four documented kadm5 principals (`kadmin/admin`, `kadmin/changepw`, `kadmin/history`, `kiprop/<host>`); other service tickets fail INIT (`AUTH_REJECTEDCRED`) where MIT would INIT then weakauth | deviation | `scripts/kadmin-gate.sh` `kadmin/history` probe AUTH_TOOWEAK both legs |
| parse.c:62-102; unparse.c:85-134; x-deltat.y:146,149-171,225-227; t_deltat.c:46-126; server_stubs.c:262-303,471-485,574-595,680-706; auth_acl.c:286-290 | parse/unparse quoting; `string_to_deltat` leftover unknown is EOF and trailing whitespace is `tok_WS` (only the `opt_s: ws` slot after `d`/`h`/`m` absorbs it); CREATE/DELETE/RENAME `rec_out == NULL` authorise first; GET and MODIFY `rec_out` lookup UNK before ACL (and before mask on modify); unknown ACL op `'%c' in %s` | `KADM5_UNK_PRINC` / `KADM5_AUTH_ADD` | name.rs parse_name; deltat.rs parse; kadm5.rs dispatch_kadm5_ticket; acl.rs parse_line | `42x`→42; `3dd` invalid; `42 ` / `1s ` / `1d5s ` invalid and `1d ` valid; authorised `user@OTHER.REALM` created; denied add is AUTH_ADD; missing GET/MODIFY/SETKEY/PURGE/EXTRACT/SET_STRING is UNK | exact | `create_foreign_realm_authorises_first`; `create_foreign_realm_then_getprinc`; `getprinc_foreign_realm_is_unk_princ`; `stub_setup_unk_before_acl_on_modify_setkey_purge_extract_setstr`; `deltat_trailing_whitespace_is_tok_ws`; `scripts/kadmin-gate.sh` addprinc OTHER.REALM / 42x / aZ / 3dd both legs; `scripts/kadmin-local-gate.sh:224-256` `-maxlife "1d "` accepted and `"42 "` refused on both legs |
| server_stubs.c:392-400,925,1027,1081,1216,851-869; misc.c:60-121 | `check_self_keychange` / `check_min_life` / `clamp_self_keepold` 5 for chpass, chrand and setkey; chpass lockdown→ACL→self | `KADM5_PASS_TOOSOON` | kadm5.rs kadm5_code; store.rs check_min_life | min_life self; admin ignores it and resets `last_pwd_change`; keepold 5 | exact | `policy_min_max_life_round_trip_and_min_life`; `self_keepold_clamps_to_five`; `chrand3_self_keepold_clamps_to_five`; `setkey_self_keepold_clamps_to_five`; `scripts/kadmin-gate.sh:1225-1250` / `:1958-1983` admin cpw success text then self cpw `PASS_TOOSOON`; `:1269-1291` / `:1995-2010` `cpw -randkey -keepold` ×6 as self → 5 kvnos on both legs; `scripts/kpasswd-gate.sh` minlife |
| svr_principal.c:1695-1710 | `kadm5_setkey_principal_4` with keepold copies the old keys after the new ones but never advances `n_new_key_data`, so `kdb->n_key_data` counts only the new keys and every old key is dropped (same in upstream master) | 1 key at the newest kvno | store.rs set_keys_in / cap_key_versions | the stub's clamp is a version count like chpass/chrand: self keeps 5, admin unbounded | deviation | `setkey_self_keepold_clamps_to_five`; `setkey4_keepold_retains_history`; `scripts/kadmin-gate.sh:1269-1291` (Rust `setkey -keepold` ×6 as self → 5) / `:2011-2020` (MIT → 1 key `vno 7`, pinned) |
| svr_policy.c:83-109,292-322 | create: DUP before the non-printable-name check, unset lifetimes zeroed, `BAD_MIN_PASS_LIFE` only with both mask bits and `pw_max_life != 0`; modify validates merged `pw_max_life` / length / class / history | `BAD_POLICY` / `BAD_MASK` / `BAD_LENGTH` / `BAD_CLASS` / `BAD_HISTORY` / `BAD_MIN_PASS_LIFE` | kadm5.rs CREATE_POLICY; kadm5.rs policy_name_err; kadm5.rs policy_floor_err; kadm5.rs create_policy_local; kadm5.rs modify_policy_local; krb5-admin/lib.rs add_policy_ent; krb5-admin/lib.rs modify_policy_ent | same codes/texts on both the wire and the local path (kadmin.local routes through the same validators, MIT order min>max → length → classes → history) | exact | `create_policy_checks_min_over_max_before_length_like_mit`; `create_policy_class_and_history_texts_match_mit`; `modify_missing_policy_is_policy_does_not_exist`; `create_policy_dup_before_floors`; `scripts/kadmin-gate.sh:1292-1301` / `:2021-2027` unmasked `pw_max_life`; `scripts/kadmin-local-gate.sh` policy-order/text cells diffed on both legs |
| server_stubs.c:1495-1530; svr_principal.c:1937 | purgekeys has no lockdown check | 0 | kadm5.rs PURGEKEYS | allowed on locked-down target | exact | `purgekeys_locked_down_target_is_allowed`; `scripts/kadmin-gate.sh:1302-1313` / `:2028-2038` purgekeys on a lockdown target through the RPC client on both legs |
| server_kdb.c:140-188 | `kdb_get_hist_key` creates `kadmin/history` on first use (`create_hist`: mask PRINCIPAL\|MAX_LIFE\|ATTRIBUTES, `max_life = KRB5_KDB_DISALLOW_ALL_TIX` = 64 s, `attributes = 0`, one key of `params.enctype`, re-randomized to kvno 2); not at realm creation, not at `addprinc -policy` | `getprinc kadmin/history` absent before the first policy chpass, then `Maximum ticket life: 0 days 00:01:04`, `Attributes:` empty, `Key: vno 2` | krb5-kdc/store.rs ensure_history_principal; krb5-kdc/store.rs history_key | same shape, created by `set_password_keepold_n_in` when the principal has a policy | exact | `loading_the_mit_history_dump_enforces_mit_reuse_and_keeps_the_record`; `scripts/kadmin-gate.sh` `kadmin/history does not exist before the first policy chpass` then `kadmin/history getprinc shape: Rust vs MIT` (both legs, `diff`); `scripts/kdb-dump-gate.sh` half A/B `getprinc kadmin/history` shape diffs |
| adb_xdr.c:18-100; svr_principal.c:485-500,600-640,770-790 | `KRB5_TL_KADM_DATA` is the XDR `osa_princ_ent_rec` (version `0x12345c01`, nullstring policy, `aux_attributes` with `KADM5_POLICY`, `old_key_next`, `admin_history_kvno`, `old_keys[]` of `krb5_key_data`); the policy binding lives there (`get_principal` reports it only with the bit) | `Policy:` on `getprinc` after a dump/load in either direction | krb5-kdc/osa.rs OsaPrincEnt; krb5-kdc/store.rs KadmData; krb5-kdc/store.rs refresh_kadm_tl; krb5-kdc/kdb_dump.rs merge_kadm_tl | same record, maintained on every policy/history change (`kdb_put_entry`); the private 0x4B02/0x4B04 types are read for older Rust dumps and retired on write | exact | `mit_kadm_data_decodes_and_its_history_decrypts_under_the_history_key`, `osa::tests`; `scripts/kdb-dump-gate.sh` half A `Policy: hp` on the Rust load of a MIT dump and half B `Policy: hpb` on the MIT load of a Rust dump |
| svr_principal.c:926-1160,1262-1340 | chpass with a bound policy: `kdb_get_hist_key`, `create_history_entry` (the replaced keys of the top kvno re-encrypted under the history key), `check_pw_reuse` against the current keys and `old_keys`, `add_to_history` ring of `pw_history_num - 1` entries reset when the history kvno changed | `KADM5_PASS_REUSE` `Cannot reuse password` for any of the remembered passwords | krb5-kdc/store.rs record_history; krb5-kdc/store.rs resolve_history; krb5-kdc/osa.rs history_entry; krb5-kdc/osa.rs decrypt_entry | same entries under the same key; the reuse check compares derived keys like `check_pw_reuse` | exact | `password_history_matches_mit_window`, `loading_the_mit_history_dump_enforces_mit_reuse_and_keeps_the_record`; `scripts/kdb-dump-gate.sh` half A (Rust refuses MIT's old password) and half B (MIT refuses Rust's old password) |
| kdb_convert.c:340-360; iprop_xdr.c:225-250; kdb_log.c:401-450 `ulog_replay` | `ulog_conv_2logentry` carries policy/history only inside `AT_TL_DATA` (`KRB5_TL_KADM_DATA`) and excludes the 8 derived attrs (`kdb_convert.c:343-350`); a changed history rides as `AT_PW_HIST` + `AT_PW_HIST_KVNO`, decrypted with the replica's own `kadmin/history` | replica refuses the primary's remembered passwords | kadm5.rs encode_keydata_raw; kadm5.rs decode_keydata_raw | the encoder additionally emits `AT_PW_POLICY`/`AT_PW_HIST_KVNO`/`AT_PW_HIST` and keeps TL 1/2 in `AT_TL_DATA`; a WIRE divergence only — MIT's kpropd tolerates the extra attrs (`default:break` in `ulog_conv_2dbentry`) and the Rust decoder reads history from `AT_PW_HIST`, so replay works both ways | deviation (R2-D1: byte-parity would need a coordinated encoder+decoder change for nil behavioural gain; reopen on a strict byte-parity requirement) | `iprop_kdbe_round_trips_string_attrs_history_policy_lockout` (codec); `scripts/iprop-gate.sh` `password history propagates`: after a policy chpass on the Rust master, MIT kpropd's replica shows `Policy: ihp`, holds `kadmin/history`, and its own `kadmin.local cpw` refuses the old password with `Cannot reuse password` |
| kdb5.c:2441-2508 `krb5_db_create_policy` / `put_policy` / `delete_policy`; kdb5.c:1007,1049 | only principal puts and deletes call `ulog_add_update`; a policy change never enters the ulog, so `kpropd -A` replicas get policies by full resync only and `ulog_replay` (`kdb_log.c:401-450`) parses every entry as a principal | a `policy:` entry in GET_UPDATES makes MIT kpropd fail `ulog_replay` (`Malformed representation of principal`) and drop the whole batch | krb5-kdc/store.rs iprop_get | the local `policy:` ulog markers advance the serial but are not shipped; policies reach a replica by full resync like MIT | exact | `iprop_get_ships_principals_only_like_ulog_get_entries`; `scripts/iprop-gate.sh` (the history policy is created before the first-contact dump; the later principal/chpass batch replays on MIT kpropd) |
| dump.c:416-462 `dump_r1_11_policy`; :873 `process_r1_11_policy` | the `policy` dump line carries `pw_attributes` / `pw_max_life` / `pw_max_renewable_life` and policy tl_data (`osa_policy_ent` fields 11-15) alongside the length/class/history fields | those fields round-trip through a dump | krb5-kdc/store.rs NamedPolicy | the three attributes and policy tl_data are always 0/empty — no kadmin verb sets them and they are not wired to issuance, so a kadmin-produced dump carries them zero and the round-trip preserves that; only a dump another tool wrote with non-zero attributes via the C API is zeroed | deviation (R2-D1/A7: adding the vestigial fields churns ~19 NamedPolicy literals for nil behavioural gain; reopen if a real source of non-zero policy attributes appears) | `osa::tests` policy round-trip; `scripts/kdb-dump-gate.sh` policy line |
| kadmin.c:1480-1580 (`kadmin_getprinc`, `strdate`, `strdur`), lib/kadm5/str_conv.c:97-125 (`outflags`), lib/krb5/krb/str_conv.c:62-114 (`krb5_salttype_to_string`) | `getprinc` prints the whole record; a deleted bound policy is `Policy: <name> [does not exist]` (`kadmin.c:1543-1546`); an unnamed `Attributes:` bit is `0x%08lx` over all 32 bits (`lib/kadm5/str_conv.c:214,238`); salt types are only normal/norealm/onlyrealm/special (`salttype_table`), others `<Salt type 0x..>` | identical text on both legs except the two documented fields | krb5-kadmin-local.rs print_getprinc; krb5-kadmin-local.rs flags_to_string; krb5-kadmin-local.rs salttype_name | the `[does not exist]` suffix, the `0x%08lx` 32-bit flag format and the salt-name set now match MIT; two fields still differ by design — the modifier is the kadmind principal (`kadmin/admin@REALM`) where MIT writes the caller (`root/admin@REALM`), and the zone prints `UTC` at offset zero (no zone database) | deviation | `scripts/kadmin-local-gate.sh` getprinc-dead-policy cell (`Policy: gonep [does not exist]` both legs) + the `getprinc` shape diffs; `scripts/kdb-dump-gate.sh` half A/B |
| kadmin.c:1281-1355 (`kadmin_addprinc`), :926,963 (`kadmin_cpw`) | `No policy specified for %s; defaulting to no policy` (stderr), `Principal "%s" created.`, `add_principal: <err> while creating "%s".`; `Password for "%s" changed.` / `Key for "%s" randomized.`, `change_password: <err> while changing password for "%s".` with the `kadm_err.et` texts | identical text on both legs | krb5-kadmin-local.rs kadm_err_text | same texts; the session continues after a failed verb like `com_err` | exact | `scripts/kdb-dump-gate.sh` half A/B (`created.`, `changed.`, `Cannot reuse password while changing password for`) |
| osconf.hin:90 `DEFAULT_KDC_ENCTYPE`; kdc.conf `master_key_type` | a realm created without `master_key_type` gets an aes256-cts-hmac-sha1-96 master key (settled live: MIT `getprinc K/M` = `aes256-cts-hmac-sha1-96`); `params.enctype` (the history key's type) follows it | `Key: vno 1, aes256-cts-hmac-sha1-96` when unset | krb5-kdc/persist.rs persist_master_etype; krb5-kdc/persist.rs master_etype_or_default | both create paths honour `master_key_type` when set, but Rust defaults to the stronger aes256-cts-hmac-sha384-192 when unset where MIT defaults to aes256-cts-hmac-sha1-96 | deviation | `scripts/kdb-dump-gate.sh` (both realms set `master_key_type`; the unset default is a `docs/security.md` deviation) |
| kdb_cpw.c:116-137 | keepold 1 = unbounded; self clamp 5 | n/a | kadm5.rs clamp_self_keepold; store.rs cap_key_versions | self == 5; non-self keepold=1 > 5 | exact | `self_keepold_clamps_to_five`; `non_self_keepold_is_unbounded` |
| svc_auth_gss.c:459-544 | `_svcauth_gss` AUTH_ERROR: version/svc `AUTH_BADCRED`; INIT non-NULLPROC / unknown proc `AUTH_FAILED`/`AUTH_REJECTEDCRED`; DATA & DESTROY validate (bad header MIC) `CREDPROBLEM`; `gc_seq > MAXSEQ` / window replay `CTXPROBLEM`; DESTROY-then-DATA `CREDPROBLEM` | AUTH_ERROR 1/2/13/14 | kadm5.rs handle_rpcsec_gss | same replies, connection kept | exact | `rpcsec_bad_version_is_auth_badcred`; `rpcsec_bad_mic_is_credproblem`; `rpcsec_init_non_nullproc_is_auth_failed`; `rpcsec_unknown_gc_proc_is_rejectedcred`; `rpcsec_seq_over_maxseq_is_ctxproblem`; `rpcsec_destroy_then_data_is_credproblem`; `scripts/kadmin-gate.sh` reject machine corrupt-verf/maxseq/destroy-then-data both legs |
| svc_auth_gss.c:385-440 | `_svcauth_gss` selects `gd` by connection (`xp_auth`); `gc_handle` is never compared, so a DATA with a wrong handle but a valid header MIC dispatches | accepted 0 SUCCESS | kadm5.rs handle_rpcsec_gss (no handle compare) | handle ignored; header MIC authenticates | exact | `rpcsec_wrong_handle_data_is_dispatched`; `scripts/kadmin-gate.sh` reject machine wrong-handle both legs |
| kadm_rpc_svc.c:258-261; svc_auth_gss.c:546 | established context + undecodable args → `svc_getargs` fail → `svcerr_decode` `GARBAGE_ARGS` | accepted 4 | kadm5.rs rpcsec_dispatch (GarbageArgs) | same | exact | `rpcsec_unwrap_fail_is_garbage_args_with_verf`; `rpcsec_integrity_bad_checksum_is_garbage_args`; `scripts/kadmin-gate.sh` reject machine garbage-args both legs |
| svc.c:290-300; kadm_rpc_svc.c:263-268 | `svcerr_systemerr` (accept_stat `SYSTEM_ERR` 5) only when `svc_sendreply` fails after the stub ran; a stub's own failure travels as `ret->code` inside a normal reply | SYSTEM_ERR 5 | kadm5.rs:649 rpcsec dispatch | any dispatcher error other than `GARBAGE_ARGS`/`PROC_UNAVAIL` is answered `SYSTEM_ERR` with the connection's verifier; no send-failure path (a write error closes the connection) | deviation | R1: the arm is a defensive catch-all — `dispatch_kadm5_ticket` wraps every stub error into `Ok(generic_ret(…, kadm5_code))` and the XDR reader returns `GarbageArgs` on any underflow (`kadm5.rs:3398`), so `kadm5_or_iprop` returns only `Ok`/`GarbageArgs`/`ProcUnavail`; the `SYSTEM_ERR` arm is structurally unreachable without a production failure hook, so no unit is added (recorded, not stretched) |
| authgss_prot.c:130-260 | RPCSEC_GSS DATA body per `gd->sec.svc`: NONE plain args; INTEGRITY `databody_integ` opaque + `checksum` opaque (`gss_get_mic`/`gss_verify_mic` over `seq‖args`); PRIVACY `gss_wrap` opaque; inner seq must equal `gc_seq` | XDR opaque(s) | kadm5.rs handle_rpcsec_gss DATA arm; kadm5.rs rpcsec_dispatch reply | NONE/INTEGRITY/PRIVACY; bad integrity checksum or seq is GARBAGE_ARGS | exact | `rpcsec_integrity_request_is_databody_plus_mic`; `rpcsec_integrity_reply_is_databody_plus_mic`; `rpcsec_none_service_body_is_plain`; `rpcsec_integrity_bad_checksum_is_garbage_args`; `scripts/kadmin-gate.sh` integrity listprincs + tampered checksum both legs |
| recvauth.c:168; krb5_err.et:75-88 | kprop `e_text` is `error_message()` + NUL | 32 `Ticket expired\0` | kprop.rs recvauth_protocol_text | MIT com_err strings | exact | `recvauth_tkt_expired_is_mit_error_message`; `kpropd_expired_ticket_is_32_ticket_expired`; `scripts/kprop-gate.sh` expired AP-REQ both legs |
| kpropd.c:1298-1348 `authorized_principal` | `kpropd.acl` is re-`fopen`ed per connection; a line authorizes when it starts with the unparsed client (`strncmp(name, buf, strlen(name))`) and the next byte is NUL or `isspace`; after skipping whitespace an empty remainder authorizes, a non-empty one only if `krb5_string_to_enctype` accepts it (name/alias, `strcasecmp`, never a number) and it equals `ticket->enc_part.enctype`, else the line is skipped; no globs, no leading whitespace, `#` is not a comment (it just never matches); unopenable file = nobody | `Rejected connection from unauthorized principal %s` syslog, `exit(1)` | krb5-admin/kprop.rs kpropd_authorized_principal; krb5-admin/kprop.rs kpropd_acl_string_to_enctype; krb5-kpropd.rs kpropd_acl | same match (before C2 the lines went through the kadm5 glob matcher with the enctype token ignored, so `*@REALM` authorized everyone and an enctype restriction was not enforced); `KpropUnauthorized` prints the MIT text; the ACL path comes from `KRB5_KPROP_ACL` where MIT takes `-a` or its compiled default (CLI surface) | exact | `c2_kpropd_acl_exact_line_authorizes_and_wildcards_never_do`; `c2_kpropd_acl_enctype_suffix_must_name_the_ticket_enctype`; `c2_kpropd_acl_is_a_prefix_match_ended_by_whitespace_or_eol`; `scripts/prop-acl-gate.sh:313-329` 17 `acl-*` cells MIT kprop vs MIT kpropd and vs Rust kpropd (`acl_case` dies on a differing verdict or refusal count) |
| kpropd.c:526-546; kprop.c:1103-1120 `xmit_database` | `authorized_principal` runs after `kerberos_authenticate` (recvauth complete, AP-REP sent); a refused peer gets no KRB-ERROR, kpropd `exit(1)`s and kprop, whose size SAFE landed, dies on the first PRIV block | kprop `Broken pipe while sending database block starting at 0` | krb5-admin/kprop.rs kpropd_recvauth | ACL check moved after the AP-REP write; the connection drops on `KpropUnauthorized` (before C2 the empty recvauth ack was sent and the AP-REP withheld, so MIT kprop failed `while authenticating to server`) | exact | `c2_kpropd_refuses_after_the_ap_rep_so_kprop_fails_on_the_dump_not_sendauth`; `scripts/prop-acl-gate.sh:147,187,275-311` MIT kprop's verdict line and both kpropds' `Rejected connection…` counts compared per cell |
| kpropd.c:1202-1204,1236-1242; kprop.c:266-276,296; privsafe.c:314-375 `k5_privsafe_check_addrs`; rd_req_dec.c (caddrs only with a remote address) | kpropd sets only a local address (the peer sockaddr, `k5_addr_directional_accept` fallback) and no remote address so a NAT'd kprop is accepted; kprop sets only its `sender_addr`, so its SAFE/PRIV carry an s-address and no r-address; the ticket `caddrs` are not checked without a remote address and the missing r-address is not checked either | n/a (no address check on either side) | krb5-admin/kprop.rs kpropd_recvauth (`addresses: None`); krb5-protocol/safe_priv.rs unwrap_krb_priv_chained / verify_krb_safe_checksum (no address parameters) | no address is enforced on the kprop path; kpropd's SAFE ack carries no s-address where MIT's carries the peer's (MIT kprop, remote NULL, does not compare it) | exact (effect) | `scripts/prop-acl-gate.sh:313-329` MIT kprop from the container address to the Rust kpropd on `0.0.0.0:754`; `scripts/kprop-gate.sh` MIT kprop → Rust kpropd; `scripts/kprop-reverse-gate.sh` Rust kprop → MIT kpropd (static: no address parameter exists on the unwrap path) |
| net-server.c:1101-1105 | empty drop silent (no `while dispatching` when code 0) | none | kdc/listen.rs log_dispatch_drop | debug without suffix | exact | `log_dispatch_drop_udp_is_not_tcp` |
| do_as_req.c:225-226,630-649; kdc_util.c:462-473 `select_session_keytype` / `get_first_current_key`; lib/kdb/keytab.c:153-159 | both directions `kinit`+`kvno` with `session_enctypes=rc4-hmac`; skey `arcfour-hmac`; the ticket key is the first key of the highest kvno regardless of the session etype (MIT's KDB keytab decrypts a local TGT with that key only) | skey arcfour both legs; tkt etype equal | krb5-kdc/issue.rs select_session_keytype; krb5-kdc/store.rs first_current_key | skey `arcfour-hmac`; tkt etype = first current krbtgt key on both legs (`cross-kdc-gate.sh`) | exact | `scripts/rc4-session-gate.sh` `skey=arcfour-hmac`; `scripts/cross-kdc-gate.sh` `TGT etype differs` die on one dump; `first_current_key_ignores_the_session_etype` |
| svr_principal.c:310-333,565-588,650-682; kadm_rpc_xdr.c:349; kdb5.c:893-945,970-975,2304; kdb_db2.c:817-822; server_stubs.c:296-301,621-638 | `kadm5_create_principal_3` / `kadm5_modify_principal`: stub GET (`rec_out`) then ACL then lockdown then `KADM5_BAD_MASK`; `tl_data_type` as int16 then `< 256` → `KADM5_BAD_TL_TYPE`; non-zero `fail_auth_count` → `KADM5_BAD_SERVER_PARAMS`; `KEY_DATA`+`n_key_data!=0` → `KADM5_BAD_MASK`; `KRB5_TL_DB_ARGS` (0x7fff) appended in-memory then stripped at put; DB2 rejects leftover `db_args` with EINVAL 22 | `KADM5_BAD_MASK` / `KADM5_BAD_TL_TYPE` / `KADM5_BAD_SERVER_PARAMS` / 22 | krb5-admin/kadm5.rs dispatch_kadm5_ticket; krb5-kdc/store.rs strip_db_args; merge_tl_data_in | same codes before any store write; put/load/iprop reject `0x7fff`; modify rec_out is UNK before ACL, then lockdown, then mask; modify may still persist more than once (documented) | exact (guards/mask/int16/put-strip); deviation (multi-put) | `modify_one_db_args_is_einval_and_unchanged`; `ro_policy_and_policy_clr_is_auth_modify`; `stub_setup_unk_before_acl_on_modify_setkey_purge_extract_setstr`; `create_key_data_mask_with_one_key_is_bad_mask`; `load_dump_with_tl_32767_names_princ_and_arg`; `iprop_db_args_entry_is_absent_after_apply`; `merge_tl_db_args_leaves_entry_and_file_unchanged`; `scripts/kadmin-gate.sh` kadm5_r12_db_args + `kadm5_modify_raw`; `scripts/kdb-dump-gate.sh` `golden_has_tl_32767` |
| svr_principal.c:614-625,1336 | `modify_principal` with `KADM5_POLICY` and no explicit `pw_expiration` sets it to `last_pwd_change + pw_max_life` (0 without a max life); chpass sets `now + pw_max_life` | `Password expiration date` | krb5-kdc/store.rs apply_pw_max_life_in | same bases | exact | `scripts/kadmin-gate.sh:418-421` / `:1482-1501` backdate `user` to 1000000000 (Rust `krb5-kdb setlastpwd`, MIT `kdb5_util dump`/tl-data 1/`load`); `:1950-1955` `Last password change` and `Password expiration date` identical on both legs after `modprinc -policy life`; `:1226-1228` / `:1959-1961` admin cpw moves it off 2001 |
| util/ss/parse.c:60-150 | `ss_parse`: tokens split on space/tab, `"` toggles quoting inside or at the start of a token, `""` inside quotes is a literal quote, an open quote is `Unbalanced quotes in command line` | argv | krb5-kadmin-local.rs ss_parse | same | exact | `ss_parse_quotes_like_parse_c`; `quoted_interval_keeps_trailing_whitespace`; `scripts/kadmin-local-gate.sh:224-256` quoted `-maxlife` on both legs |
| kadmin.c:666-711,1753-1776 | `delete_principal` / `delete_policy`: `-force` or the prompt `Are you sure you want to delete the {principal,policy} "%s"? (yes/no): `, only a `yes` line deletes, otherwise `Principal "%s" not deleted` / `Policy "%s" not deleted.`; a deleted principal prints `Principal "%s" deleted.` and the ACL reminder | texts | krb5-kadmin-local.rs force_arg / confirm_delete | same texts, the principal unparsed with its realm | exact | `delete_prompts_unless_forced`; `scripts/kadmin-local-gate.sh:258-289` EOF reply keeps, `yes` deletes, on both legs |
| ss_wrapper.c:66-76; kadmin.c:89-99 | `kadmin.local -q` exits 0 after a failed verb (`error()` sets `exit_status` only in script mode) | rc 0 | krb5-kadmin-local.rs run; krb5-kadmin-local.rs addmodpol_usage; krb5-kadmin-local.rs main | addpol/modpol/alias print `com_err` and exit 0 like MIT; other verbs still exit 1 | stricter-documented | `scripts/kadmin-local-gate.sh` `addpol -maxlife "42 "` and the policy-order/alias-dup cells exit 0 with the MIT text on both legs; `docs/security.md` kadmin.local paragraph |
| kadmin.c:778-820 `kadmin_addalias`; kadmin_ct.ct:42 | `alias`/`add_alias` verb: `usage: add_alias alias_principal target_principal`; success `Principal "%s" aliased to "%s".`; failure `add_alias: %s while aliasing principal "%s" to "%s"` | texts + `KADM5_DUP` / `KADM5_ALIAS_REALM` | krb5-kadmin-local.rs run; krb5-admin/lib.rs create_alias | same texts | exact | `kadmin_local_alias_creates_a_stub_for_the_target`; `scripts/kadmin-local-gate.sh` alias verb diffed on both legs (success/dup/realm/usage) |
| kadmin.c:155-171 `parse_interval`/`parse_date` | `krb5_string_to_deltat` first, else the date error `Invalid date specification "%s".` + the addmodpol usage block | text | krb5-admin/lib.rs parse_pol_interval; krb5-kadmin-local.rs addmodpol_usage | same text and usage block | exact | `quoted_interval_keeps_trailing_whitespace`; `scripts/kadmin-local-gate.sh` `-maxlife "42 "` message diffed on both legs |
| getdate.y:1-900 (`get_date`) | natural-language dates (`1 day ago`, `yesterday`) and the `Interval specification "%s" is in the past.` branch | text | krb5-admin/lib.rs parse_pol_interval (deltat only) | natural-language input is `Invalid date specification` where MIT parses it | deferred | none (promotion: a getdate.y port; MIT `kadmin.local -maxlife "1 day ago"` is the oracle) |
| alt_prof.c:509-510; ovsec_kadmd.c:497-500 | default `acl_file` is `KDC_DIR/kadm5.acl`; empty string → NULL → `acl_init` no-handle (refuse to start if the default file is missing) | start fail / self-only | krb5-kdc/lib.rs default_acl_path; krb5-kdc/lib.rs acl_for_store | default path; empty/`None` is self-only (`Acl::none`) | exact | `acl_for_store` tests in `issue_acl_ap.rs`; `scripts/kadmin-gate.sh` missing ACL refuse |
| ipropd_svc.c:258,508-516 | `ipropx_resync`: RPCSEC_GSS only; acceptor 2 comps, `kiprop`, realm | RPC `AUTH_TOOWEAK` | krb5-admin/kadm5.rs check_iprop_rpcsec_auth; krb5-admin/kadm5.rs dispatch_iprop; krb5-admin/kadm5.rs rpc_reply_weakauth | same gate; `kiprop` RPCSEC dispatches | exact | `kiprop_acceptor_requires_store_realm`; `scripts/kadmin-gate.sh` iprop program cells |
| server_misc.c:146-158 | `kadm5_get_privs` returns `*privs = ~0` | all-ones | krb5-admin/kadm5.rs GET_PRIVS; krb5-admin/kadm5.rs dispatch_kadm5_ticket | `GET_PRIVS` returns `~0` | exact | `get_privs_is_all_ones` |
| svr_iters.c:29-115 `glob_to_regexp` | `listprincs`/`listpols` filter is an anchored glob (`?`=one, `*`=run, `[...]`=class, `\x`=literal); a principal glob with no `@` gets an implicit `@*`; a trailing `\` is `EINVAL` | matched names | krb5-admin/kadm5.rs glob_is_match; krb5-admin/kadm5.rs glob_expand; krb5-admin/lib.rs list_ids_glob; krb5-admin/lib.rs list_policies_glob | same matches (RPC and kadmin.local); `*1` matches a1/a11/xa1 not a10 | exact | `get_princs_glob_matches_svr_iters`; `glob_matches_like_svr_iters`; `scripts/kadmin-local-gate.sh` listprincs/listpols glob diffs on both legs; `scripts/kadmin-gate.sh:721`/`:1730-1732` the same `listprincs`/`listpols` patterns through MIT `kadmin` against the Rust kadmind and MIT's kadmind, outputs diffed (incl. MIT's `Invalid argument while retrieving list.` for a trailing backslash) |
| clients/klist/klist.c:588-602 `etype_string` | a `DEPRECATED:` prefix on a deprecated enctype (des3/arcfour) in `klist -e` | display | krb5-crypto/etype.rs is_deprecated; krb5-client/krb5-klist.rs etype_display | `DEPRECATED:arcfour-hmac` on the `Etype (skey, tkt)` line | exact | `deprecated_set_matches_mit`; `scripts/rc4-session-gate.sh` `DEPRECATED:arcfour-hmac` on both legs |
| server_stubs.c:402-459 `log_unauth` / `log_done` | `Unauthorized request: %s, %s, client=%s, service=%s, addr=%s` / `Request: %s, %s, %s, client=…` per stub | syslog text | krb5-admin/kadm5.rs `kadm5_log_op` in `rpcsec_dispatch` (`service` from `GssContext.acceptor`, `addr` from `stream.peer_addr`) | `Unauthorized request:` / `Request:` per op with client/service/addr | exact | `scripts/kadmin-gate.sh` Rust kadmind logs the `extra` create success and the changepw `listprincs` denial; `kadm5.rs::kadm5_log_op_fields_match_mit_stubs` |
| kadm_rpc.h `CREATE_ALIAS` 27; server_stubs.c:1727-1758; svr_principal.c:2051-2087; auth_acl.c:723-734 | `create_alias_2_svc` (CHANGEPW deny, `acl_addalias`, no lockdown) / `kadm5_create_alias` (realm compare, DUP resolves, target may be absent) / `acl_addalias` (ADD on alias w/o restrictions AND MODIFY on target) | proc 27; `KADM5_AUTH_INSUFFICIENT` / `KADM5_ALIAS_REALM` / `KADM5_DUP` | krb5-admin/kadm5.rs CREATE_ALIAS 2517; store.rs create_alias_in 1180; acl.rs check_addalias 290 | same codes | exact | `m3a_alias.rs` acl matrix + codes; `scripts/kadmin-gate.sh:369-484` `alias_cells` both legs (`:674` / `:1683`) |
| svc.c:342,361; svc_auth_gssapi.c:495-497 | AUTH_GSSAPI accepted/mismatch replies carry `FLAVOR_NONE` + empty verifier (`rpc_reply_clear`); RPCSEC accepted/mismatch use `xp_verf` | AUTH_GSSAPI verf none | krb5-admin/kadm5.rs rpc_reply_clear; krb5-admin/kadm5.rs rpc_reply_accepted | AUTH_GSSAPI INIT is `FLAVOR_NONE`; RPCSEC DATA carries `xp_verf` | deviation | `auth_gssapi_on_iprop_init_is_success`; RPCSEC `rpcsec_unknown_program_data_carries_xp_verf` |
| svc_auth_gssapi.c:326-341 | GSSAPI_INIT arg version: 1/2 → reply version 1 (compat warning); 3/4 echoed; other `AUTH_BADCRED` | AUTH_BADCRED on unknown | krb5-admin/kadm5.rs handle_auth_gssapi | `arg_ver` echoed; no v1/v2 downgrade; unknown version not `AUTH_BADCRED` | deviation | proposed: AUTH_GSSAPI init-arg version cell |
| kdb_default.c:111-215,356-380 `krb5_def_store_mkey_list` / `krb5_db_def_fetch_mkey` | the master-key stash is a FILE keytab with one `K/M@REALM` entry (etype + kvno embedded); fetch tries the keytab (`krb5_db_def_fetch_mkey_keytab`) then the raw stash (`krb5_db_def_fetch_mkey_stash`) | n/a | krb5-kdc/persist.rs stash_keytab_key; krb5-kdc/persist.rs master_for_save; krb5-kdc/persist.rs existing_stash_key | keytab format written; keytab-first read (etype from the file, no blind trial), legacy raw rewritten on the next save | exact | `stash_is_keytab_format_and_reads_back_the_master`; `legacy_raw_stash_loads_then_is_rewritten_as_keytab`; `scripts/kdb-dump-gate.sh` MIT `klist -k` reads the Rust `K/M@KERBER.TEST` keytab stash + `krb5-kdb stash` |
| alt_prof.c:633-660 `supported_enctypes` (`kadm5_get_config_params` keysalts) | key generation order and salts for `kdb5_util create` / `addprinc` without `-e` follow the realm's `supported_enctypes` | n/a | krb5-kdc/store.rs password_etypes; store.rs bootstrap_with_kdc_conf | profile order when set; empty profile keeps `randkey_etypes()` 18,17,20,19 | exact | `a4_19b.rs` `a4_19_bootstrap_honours_supported_enctypes_order`; `scripts/kdb-dump-gate.sh` `MIT_keyord` / `RUST_keyord` |
| cc_file.c:87,384-470 read_header; ccmarshal.c:287-315,430-487 k5_unmarshal_cred / k5_marshal_cred; ccmarshal.c:375-425 k5_marshal_princ | FILE v4 layout: `0x0504`, header tag block (`FCC_TAG_DELTATIME` 1), 32-bit name type + component count without the realm, 32-bit times, `is_skey`, addresses, authdata, second ticket, `X-CACHECONF:` config entries | n/a (byte layout) | krb5-protocol/ccache.rs parse; krb5-protocol/ccache.rs to_bytes; krb5-protocol/ccmarshal.rs CcacheCred | byte-identical `parse → to_bytes` on MIT-written caches | exact | `parse_to_bytes_keeps_header_skey_addrs_authdata_second_and_config`; `mit_addr_u2u_golden_is_identity`; `scripts/ccache-gate.sh` FILE identity of the MIT `kinit` cache and of the committed `kinit -a` + u2u golden; `scripts/client-differential-gate.sh` `MIT_klist_Cfea_both_caches` |
| cc_file.c:384-430 read_header; init_ctx.c:290-293 `ccache_type` | FILE versions 1–4 are read (`0x0501`–`0x0504`); the written version follows `[libdefaults] ccache_type` (default 4) | n/a | krb5-protocol/ccache.rs parse (`0x0504` only) | a v1–v3 cache is `not FILE ccache v4`; v4 is always written | deviation | B3 grade: MIT writes v3 only under `ccache_type = 3`, which no gate sets; proposed: `ccache-gate.sh` MIT `kinit` with `ccache_type = 3` read by Rust `klist` |
| ccbase.c:202-280 krb5_cc_resolve; cc_file.c:663 fcc_resolve; cc_memory.c:316 krb5_mcc_resolve; cc_dir.c:332-372 dcc_resolve; cc_kcm.c:741 kcm_resolve | `TYPE:residual` dispatch: no prefix is FILE; `MEMORY:`; `DIR:dir` (primary subsidiary, created as `tkt` when the `primary` file is missing) and `DIR::file`; `KCM:`; any other type | `KRB5_CC_UNKNOWN_TYPE` | krb5-config/lib.rs resolve_ccspec; krb5-config/lib.rs CcSpec | `Unknown credential cache type` for `KEYRING:` and other types; same four types | exact | `ccache_path_rejects_keyring`; `scripts/ccache-gate.sh` R8 unknown type (Rust `klist`) and MEMORY consumes MIT FILE; `scripts/kcm-gate.sh` `KEYRING:` still unknown |
| cc_file.c:470-500 fcc_initialize; cc_file.c:553-660 fcc_destroy; cc_file.c:872-875 | new FILE cache `O_CREAT\|O_EXCL` then `fchmod` 0600; destroy opens the name `O_RDWR` (a symlink is followed), zero-fills, then `unlink`s | n/a | krb5-protocol/secret_file.rs write_secret_file; krb5-protocol/secret_file.rs destroy_secret_file; krb5-client/lib.rs destroy_ccache | exclusive 0600 temp file + `rename` for every write; destroy opens `O_NOFOLLOW\|O_NONBLOCK` and refuses a symlink or FIFO, leaving the target intact | stricter-documented | `open_refuses_symlink_with_nofollow`; `scripts/client-gate.sh` `kdestroy` refuses a symlink and leaves the target intact; `docs/security.md` 0600 secret files + `kdestroy` rows |
| cc_file.c:1034-1110 delete_cred; cc_file.c:1139-1165 fcc_remove_cred | `krb5_cc_remove_cred` re-marshals the matching entry in place with `endtime` 0 and `authtime` -1 (a config entry's realm becomes `X-RMED-CONF:`) so every reader skips it without rewriting the file | n/a | krb5-protocol/ccache.rs remove_cred; krb5-protocol/ccmarshal.rs tombstone | same in-place tombstone; `list` skips tombstones and config entries | exact | `remove_cred_tombstones_ticket_and_config`; `scripts/ccache-gate.sh` MIT `remove_cred` on a Rust FILE and Rust `remove_cred` on a MIT FILE, `klist` both ways |
| cc_dir.c:42-83,146-200 read_primary_file / write_primary_file; cc_dir.c:221-240 verify_dir; cc_dir.c:720 dcc_switch_to | `DIR:` collection: `tkt*` subsidiaries, the `primary` file names the current one (`krb5_cc_switch`); a missing directory is created `S_IRWXU` on any resolve; a non-directory is `KRB5_CC_FORMAT` | `KRB5_FCC_NOFILE` / `KRB5_CC_FORMAT` | krb5-protocol/ccache_dir.rs dir_cache_path; krb5-protocol/ccache_dir.rs dir_cache_path_for_store; krb5-protocol/ccache_dir.rs dir_switch | same `primary` / `tkt*` layout; the directory is created only on a store — a read (`klist`) of a missing directory fails without creating it | stricter-documented | `dir_switch_residual_must_be_subsidiary`; `scripts/ccache-gate.sh` DIR list of a missing path does not create, DIR collection MIT `kinit` ×2 then MIT `kswitch -p` and Rust `kswitch -c DIR::` read both ways; `docs/security.md` DIR row |
| cccursor.c:184-230 krb5_cc_cache_match; kswitch.c:85-125 | `kswitch -p princ` walks the collection for the cache whose default principal matches, else `KRB5_CC_NOTFOUND`; `kswitch -c name` switches by name; `while switching to credential cache` exit 1 | `KRB5_CC_NOTFOUND` | krb5-client/krb5-kswitch.rs run; krb5-client/krb5-kswitch.rs switch_principal; krb5-protocol/kcm.rs kcm_switch_principal | DIR and KCM collections by name or principal; a FILE default is refused | exact | `scripts/ccache-gate.sh` DIR collection (MIT `kswitch -p` read by Rust `klist`, Rust `kswitch -c DIR::` read by MIT `klist`); `scripts/kcm-gate.sh` two-principal collection, `kswitch -c KCM:0` then `kswitch -p extra@KERBER.TEST` |
| ccselect.c:124-191 krb5_cc_select; ccselect_realm.c:41-90; cc_retr.c:152-192 krb5int_cc_creds_match_request | per-server cache choice inside a collection (k5identity → realm → hostname modules) and `KRB5_TC_MATCH_*` retrieval of an already-cached service ticket | `KRB5_CC_NOTFOUND` → default cache | (absent; `kvno` uses the primary cache and always asks the KDC) | primary cache only | deferred | none (promotion: `ccselect_realm` for `kvno` inside a DIR collection; `scripts/ccache-gate.sh` DIR collection is the oracle; the `get_creds.c:135-152` row covers retrieval) |
| cc_kcm.c:165-175,300-320,688-1290; kcm.h:36-116 | KCM protocol 2.0 framing over the `kcm_socket` unix socket (`DEFAULT_KCM_SOCKET_PATH`); ops GEN_NEW 3, INITIALIZE 4, DESTROY 5, STORE 6, GET_PRINCIPAL 8, GET_CRED_UUID_LIST 9, GET_CRED_BY_UUID 10, GET_CACHE_UUID_LIST 18, GET_CACHE_BY_UUID 19, GET_DEFAULT_CACHE 20, SET_DEFAULT_CACHE 21, MIT GET_CRED_LIST 13001 with the UUID-list fallback | n/a | krb5-protocol/kcm.rs kcm_load; krb5-protocol/kcm.rs kcm_store; krb5-protocol/kcm.rs kcm_switch; krb5-protocol/kcm.rs kcm_socket_path | same opcodes and marshalling against sssd-kcm; the socket comes from the KCM_SOCKET env (extra), then `[libdefaults] kcm_socket`, then the default path | exact | `kcm_socket_env_overrides_conf_then_default`; `scripts/kcm-gate.sh` Rust `kinit -c KCM:` read by MIT `klist`, MIT `kinit -c KCM:` read by Rust `klist`, restart persist, `kdestroy`; `scripts/kcm-opcode-gate.sh` |
| kt_file.c:70-73; kt_file.c:285-390 krb5_ktfile_get_entry; kt_file.c:890-1100 krb5_ktfileint_internal_read_entry; kt_file.c:1127-1200 krb5_ktfileint_write_entry; kt_memory.c:244 krb5_mkt_resolve | keytab `0x0502` (v1 `0x0501` in host order): 32-bit record size (negative is a hole), 16-bit component count without the realm, realm and components, 32-bit name type (v2 only), 32-bit timestamp, 8-bit vno, 16-bit etype + key, trailing 32-bit vno when present; `get_entry` kvno 0 takes the highest, etype `IGNORE_ENCTYPE` or a similar-enctype match | `KRB5_KT_NOTFOUND` / `KRB5_KT_KVNONOTFOUND` | krb5-protocol/keytab.rs parse; krb5-protocol/keytab.rs to_bytes; krb5-protocol/keytab.rs keytab_init_creds_keys | v1/v2 read, the file's version written back; holes skipped; unknown etypes kept as unparsed records; highest kvno for `kinit -k` | exact | `scripts/ktutil-gate.sh` MIT `ktadd` then Rust `ktutil list`, MIT unknown-etype keytab listed by Rust `ktutil`, Rust-written keytab MIT `kinit -k`; `scripts/client-differential-gate.sh` `MIT_klist_ke_rust_keytab` / `MIT_kinit_kt_highest_kvno`; `scripts/kdb-dump-gate.sh` MIT `klist -k` of the Rust stash |
| sendto_kdc.c:79-80,447-530 k5_sendto_kdc; sendto_kdc.c:807-880 | UDP first when the message is ≤ `udp_preference_limit` (default `DEFAULT_UDP_PREF_LIMIT` 1465, clamp `HARD_UDP_LIMIT` 32700), TCP first otherwise, then the other transport; `KRB_ERR_RESPONSE_TOO_BIG` re-sends over TCP; per-server `kdc_timeout` / `max_retries` pacing | `KRB5_KDC_UNREACH` | krb5-protocol/transport.rs exchange_one; krb5-config/lib.rs udp_preference_limit | UDP (0.5 s / 1 s / 2 s retransmits) then TCP under the limit; TCP only above it; error 52 falls to TCP; datagrams are source-checked | exact (strategy + 52); deferred (`kdc_timeout` / `max_retries` pacing and UDP after TCP above the limit — `scripts/knobs-gate.sh` is the oracle) | `scripts/client-differential-gate.sh` `MIT_no_kdc` / `RUST_no_kdc` (`kdc = 127.0.0.1:1` fails on both); `scripts/knobs-gate.sh` MIT `kinit` pacing is unchanged by `kdc_timeout` / `max_retries` |
| locate_kdc.c:539-600 prof_locate_server; locate_kdc.c:756-800 dns_locate_server_srv; locate_kdc.c:808-890 locate_server / k5_locate_kdc; dnssrv.c:80-110,259 | `[realms] kdc =` list (`host[:port]`, default 88) first; when the profile has none and `dns_lookup_kdc`, SRV `_kerberos._udp` / `_tcp.REALM` (plus `_kerberos-master`, `_kerberos-adm`, `_kpasswd`) ordered by priority, URI records under `dns_uri_lookup`; a realm with neither is `KRB5_REALM_UNKNOWN` (`KRB5_REALM_CANT_RESOLVE` for an empty realm) | `KRB5_REALM_UNKNOWN` | krb5-config/lib.rs kdcs_for; krb5-config/lib.rs lookup_srv_kdc; krb5-client/krb5-kinit.rs kdc_addr | profile list first, then `_kerberos._udp` SRV when `dns_lookup_kdc`; no `_tcp`, master or URI records | exact (profile list + `_kerberos._udp` SRV); deferred (`_tcp` / `_kerberos-master` / URI — a DNS fixture is the oracle) | every client gate resolves `kdc = 127.0.0.1:88` from the conf (`scripts/client-gate.sh`); `scripts/client-differential-gate.sh` `MIT_no_kdc` / `RUST_no_kdc` |
| hostrealm.c:361-400 krb5_get_host_realm; hostrealm_profile.c:45-80 profile_host_realm; hostrealm_domain.c:45 domain_fallback_realm; hostrealm_dns.c:63 dns_fallback_realm | `[domain_realm]`: the host, then each suffix with and without its leading dot (`a.b.c`, `.b.c`, `b.c`, `.c`, `c`); numeric addresses are skipped; no match yields the referral (empty) realm; fallbacks `dns_lookup_realm` TXT and `realm_try_domains` | `KRB5_PLUGIN_NO_HANDLE` → `KRB5_REFERRAL_REALM` | krb5-config/lib.rs host_to_realm; krb5-config/lib.rs realm_for_host | same suffix walk (lower-cased, trailing dot stripped, numeric skipped); no match asks the client realm's KDC (which answers with the same referral) | exact (profile walk); deferred (TXT / `realm_try_domains` fallbacks — the `get_creds.c:517-542` row) | `scripts/knobs-gate.sh` `[domain_realm]` longest-suffix + conf `proxiable`; `scripts/client-gate.sh` `host/testhost.kerber.test` |
| sn2princ.c:89-160 expand_hostname; sn2princ.c:271-330 k5_canonprinc; sn2princ.c:331-380 krb5_sname_to_principal | `host/<hostname>` NT-SRV-HST: an empty hostname is the local name, lower-cased, trailing dot removed; `dns_canonicalize_hostname` true does a forward (and `rdns` reverse) lookup, `fallback` retries with the canonical name after a 7 on the raw name | 7 on the raw name first | (absent; `kvno` / `vfy_increds` send the caller's spelling) | no canonicalization | deferred | none (promotion: `dns_canonicalize_hostname` + `rdns`; MIT `kvno host/<short-name>` beside Rust through `scripts/client-differential-gate.sh` `kvno_plain` is the oracle — today both sides send `host/testhost.kerber.test` verbatim) |
| ccdefname.c:216-300 get_from_os / krb5_cc_default_name; ccdefault.c:41 krb5_cc_default; osconf.hin DEFCCNAME | default cache name: `KRB5CCNAME` env, else `[libdefaults] default_ccache_name` with `%{uid}` / `%{euid}` / `%{USERID}` / `%{username}` / `%{TEMP}` / `%{null}` expansion (`k5_expand_path_tokens`), else `FILE:/tmp/krb5cc_%{uid}`; an unknown token fails | n/a (name) | krb5-config/lib.rs resolve_ccspec; krb5-config/lib.rs expand_ccache_params | `-c` flag, then `KRB5CCNAME`, then the conf name, then the builtin; unknown `%{token}` is an error | exact | `ccache_path_default_is_uid_file`; `ccache_path_flag_beats_positional`; `scripts/knobs-gate.sh` `default_ccache_name` env > conf > builtin; `scripts/client-gate.sh` default ccache is `/tmp/krb5cc_<uid>` |
| init_os_ctx.c:245-310,340-410 os_init_paths / add_kdc_config_file; osconf.hin:53-54,81-82 | profile files: `KRB5_CONFIG` colon list (`secure_getenv`, ignored when setuid) else `DEFAULT_PROFILE_PATH` `/etc/krb5.conf`; the KDC context adds `KRB5_KDC_PROFILE` else `KDC_DIR/kdc.conf`; `include` / `includedir` inside the files; an unreadable default is `KRB5_CONFIG_CANTOPEN` | `KRB5_CONFIG_CANTOPEN` / `KRB5_CONFIG_BADFORMAT` | krb5-config/lib.rs krb5_conf_paths; krb5-config/lib.rs kdc_conf_path; krb5-config/lib.rs env_kdc_config | same list and default; `KRB5_KDC_CONF` also honoured (extra); no setuid guard (no setuid binaries) | exact | `scripts/config-include-gate.sh`; every gate sets `KRB5_CONFIG`; `scripts/knobs-gate.sh` |
| changepw.c:61-90 locate_kpasswd | kpasswd server: `kpasswd_server` else `admin_server` on port 464 (`DEFAULT_KPASSWD_PORT`), else `_kpasswd` SRV | `KRB5_REALM_UNKNOWN` | (absent; `krb5-kpasswd <kdc-host> <user@REALM>` takes the server on argv) | explicit host only | deferred | none (promotion: `kpasswd_server` / `admin_server` locate in `krb5-kpasswd.rs`; `scripts/rust-kpasswd-mit-gate.sh` against the MIT kadmind is the oracle) |
| changepw.c:205-270 change_set_password; kpasswd.c:13-14,124-149 | TCP first (`NO_UDP`) then UDP (`ONLY_UDP`) because retransmits look like replays; a 5-minute `kadmin/changepw` ticket; `Enter new password` / `Enter it again` prompts; `Password changed.` / `Password change rejected: %s` result lines | n/a (client) | krb5-admin/krb5-kpasswd.rs run; krb5-admin/krb5-kpasswd.rs is_chpw_result_line | TCP 464 then UDP; same result lines | exact | `scripts/rust-kpasswd-mit-gate.sh`; `scripts/client-differential-gate.sh` `MIT_kpasswd_soft_rejected` / `RUST_kpasswd_soft_rejected` / `MIT_kpasswd_setpw_denied` / `RUST_kpasswd_setpw_denied`; `scripts/kpasswd-gate.sh` |
| kinit.c:140-178 usage; kinit.c:222-410 parse_options | option letters `-V -l -s -r -f -F -p -P -n -a -A -C -E -v -R -k -i -t -T -c -I -S -X` (`-I` at most once); `-k` default keytab; `-R` renew; `-v` validate; `-s` postdate; `-C` canonicalize; `-E` enterprise; `-n` anonymous; usage exit 2 | exit 2 | krb5-client/cli.rs parse_kinit; krb5-client/cli.rs getopt | the same letters `-l -s -r -f -F -p -P -n -a -A -C -E -v -R -k -t -T -c -S -X` with the same meaning; an unknown option is usage exit 2; `-I -i -V` absent | exact (implemented letters, usage exit); deferred (`-I` / `-i` / `-V`, `warn_pw_expiry`) | `scripts/client-differential-gate.sh` flows `-k`, `-R`, `-v`, `-C`, `-s`, `-n`, `-E`, FAST `-T`, `-X`; `MIT_kinit_validate` / `MIT_kinit_canonicalize` / `MIT_kinit_postdated`; `scripts/knobs-gate.sh` |
| klist.c:206-260 main; klist.c:453-465 do_ccache; klist.c:533-600 check_ccache; klist.c:606-680 flags_string; klist.c:264-357 do_keytab | `-c -f -e -s -C -a -A -l -k -K -t -i -n`; `-s` exits 0 only with a current local TGT (or, TGT-less, a current non-config credential); `Ticket cache:` / `Default principal:` / `Valid starting` / `Expires` / `Service principal` / `renew until` / `Flags:` / `Etype (skey, tkt):` layout; a missing cache exits 1 | exit 0 / 1 | krb5-client/cli.rs check_ccache; krb5-client/cli.rs parse_klist; krb5-client/krb5-klist.rs klist_status | `-c -f -e -s` with the same `check_ccache` rule and layout; `-C -a -A -l -k -K -t -i -n` absent | exact (`-s` rule, layout, flags / etype); deferred (`-C -a -A -l -k -K -t -i -n` — MIT `klist` over the Rust cache and keytab in `scripts/client-differential-gate.sh` `MIT_klist_Cfea_both_caches` / `MIT_klist_ke_rust_keytab` is the oracle) | `format_cred_prints_renew_until_when_set`; `ticket_server_only_when_sname_differs`; `scripts/client-gate.sh` MIT and Rust `klist -s` agree on the same FILE, Rust `klist -c -f -e` of a MIT FILE, MIT vs Rust `klist` Ticket server text-diff; `scripts/rc4-session-gate.sh` `DEPRECATED:arcfour-hmac` |
| kvno.c:40-51 usage; kvno.c:150-170; kvno.c:293-390 kvno; kvno.c:453-585 do_v5_kvno | `kvno [-c ccache] [-e etype] [-k keytab] [-S sname] [-U user [-P]] [--u2u ccache] [-F cert] [-I] [-q]`; `-P` without `-U` / `-I` / `-F` is `Option -P (constrained delegation) requires`; `--u2u` excludes `-I -U -F`; success prints `%s: kvno = %d`; any failure makes the exit 1 after every name | exit 1 | krb5-client/cli.rs parse_kvno; krb5-client/krb5-kvno.rs run | `-c`, `-U`, `-P`, `--u2u`; `-P` alone is refused with `requires`; the `kvno = N` line; `-e -k -S -F -I -q` absent (gate-only `--body-realm` / `--disable-transited-check` / `--renew` extras) | exact (`-U` / `-P` / `--u2u` + the `-P` guard); deferred (`-e -k -S -F -I -q` — MIT `kvno` beside Rust in `scripts/client-differential-gate.sh` is the oracle) | `scripts/client-differential-gate.sh` `kvno -P without -U is refused`, `kvno_plain` / `kvno_s4u` / `kvno_u2u` flows, `MIT_kvno_P_tgs_padata`; `scripts/s4u-mit-gate.sh` |
| kdestroy.c:58-82,126-200 | `kdestroy [-A] [-q] [-c cache] [-p princ]`; `-A` destroys every cache in the collection; without `-A` a collection with other caches prints `Other credential caches present, use -A to destroy all`; failure `while destroying cache` exit 1; usage exit 2 | exit 1 / 2 | krb5-client/cli.rs parse_kdestroy; krb5-client/krb5-kdestroy.rs main; krb5-client/lib.rs destroy_ccache | `-c` only; FILE unlink without following, DIR subsidiary, KCM destroy op; exit 1 on failure, 2 on usage; `-A -q -p` absent | exact (`-c`, exit codes); deferred (`-A -q -p` — `scripts/kcm-gate.sh` `kdestroy` second cache and `scripts/client-gate.sh` are the oracle) | `scripts/client-gate.sh` Rust `kdestroy` then MIT `klist`; `scripts/kcm-gate.sh` `kdestroy` second cache |
| kinit.c:639-890 k5_kinit; klist.c:107,241-248; kvno.c:354; kdestroy.c:177; kpasswd.c:135-141 | `com_err` message shapes `<prog>: <error text> while <doing X>` (`kinit: Password incorrect while getting initial credentials`, `kvno: … while getting credentials for host/…`, `kdestroy: … while destroying cache`, `kpasswd: Password incorrect while getting initial ticket`); `warn_pw_expiry` `Warning: Your password will expire in` | text | krb5-client/krb5-kinit.rs main; krb5-client/krb5-kvno.rs kvno_err | Rust prints its own `<prog>: <error>` texts; exit codes agree | deferred | none (promotion: the `com_err` table + `while …` tails; `scripts/client-differential-gate.sh` CLI error paths — eight `MIT_<path>_rc` / `RUST_<path>_rc` pairs printed and both non-zero — is the oracle) |
| server_misc.c:44-58,110-135 `passwd_check`; svr_principal.c:364-373,1282; pwqual_empty.c:38-44 | on create and on chpass: `check_against_policy` floors when a policy is bound (an unknown name is no policy, `get_policy`), then the built-in modules in load order `dict`, `empty`, `princ`; `empty` rejects `""` even without a policy; `-randkey` skips; a rejected create leaves no entry | `KADM5_PASS_Q_TOOSHORT`; `kadmin.local` prints the `k5_setmsg` text `Empty passwords are not allowed` | krb5-kdc/store.rs check_new_password; krb5-kdc/store.rs check_password_quality; krb5-admin/kadm5.rs dispatch_kadm5_ticket; krb5-admin/lib.rs check_new_password; krb5-kadmin-local.rs kadm_err_text | same order, code and text on kadm5 create/chpass, `kadmin.local addprinc`/`cpw` and kpasswd; `add_principal: Empty passwords are not allowed while creating "…"` | exact | `c1_kadm5_create_empty_password_is_pass_q_tooshort_and_creates_nothing`; `c1_chpass_runs_empty_and_princ_modules`; `c1_empty_password_is_rejected_without_a_policy`; `c1_addprinc_rejected_password_creates_no_principal`; `scripts/kadmin-local-gate.sh` `pwq-empty-nopolicy`, `pwq-cpw-empty`, `pwq-rejected-not-created` (Rust vs MIT `kadmin.local`, `dl` identical) |
| pwqual_princ.c:32-58 `princ_check` | only with a policy: the realm `strcasecmp` first (plain `KADM5_PASS_Q_DICT` text), then every component (`Password may not match principal name`); without a policy the module returns 0 | `KADM5_PASS_Q_DICT` `Password is in the password dictionary` / `Password may not match principal name` | krb5-kdc/store.rs check_new_password; krb5-admin/kadm5.rs kadm5_code | same order, ASCII case-fold, code 43787544 and texts; no policy → accepted | exact | `c1_kadm5_create_principal_name_password_is_pass_q_dict_only_with_policy`; `c1_principal_component_and_realm_match_are_rejected_only_with_a_policy`; `c1_create_runs_pwqual_modules_before_the_entry_exists`; `scripts/kadmin-local-gate.sh` `pwq-princ-component`, `pwq-realm`, `pwq-nopolicy-name-ok`, `pwq-cpw-princ` (both legs) |
| alt_prof.c:486-513 `dict_file`; pwqual_dict.c:66-69,96-153,215-230 `init_dict` / `dict_check` | `[realms] dict_file` (realm stanza only, never `[kdcdefaults]`) read at `kadm5_init`: every `\n`-terminated line is a word, `qsort`/`bsearch` with `strcasecmp`; ENOENT logs `continuing without one`, any other error fails init; checked only with a policy | `KADM5_PASS_Q_DICT` `Password is in the password dictionary` | krb5-config/lib.rs parse_kdc_realm_line; krb5-kdc/store.rs parse_dict_words; krb5-kdc/store.rs apply_kdc_conf | same word split, ASCII case-fold exact match, realm-only relation; ENOENT continues, other read errors fail `apply_kdc_conf` | exact | `c1_dict_file_is_a_realm_relation_only`; `c1_dict_file_words_are_rejected_case_insensitively_only_with_a_policy`; `c1_dict_file_from_the_realm_stanza_rejects_words_case_insensitively`; `scripts/kadmin-local-gate.sh` `pwq-dict-word`, `pwq-dict-nopolicy-ok`, `pwq-dict-substring-ok` (`dict_file = /tmp/dict.txt` added to the shared `kdc.conf`; both legs) |
| kadmin.c:1294-1298; svr_principal.c:369,455-470 | `addprinc -randkey` (1.8+) sends a NULL `passwd`: the server skips `passwd_check` and keys the entry with `krb5_dbe_crk`; `-nokey` sets `KADM5_KEY_DATA` with `n_key_data = 0` and creates a keyless entry; a password string goes through `krb5_dbe_cpw` | 0; keys are random | krb5-admin/kadm5.rs parse_create; krb5-kdc/store.rs insert_new_randkey | NULL `passwd` is a random key of `supported_enctypes` with no quality check (before C1 the NULL was read as `""` and the entry was keyed from the empty password); `-nokey` also lands on a random key | exact (randkey); deviation (`-nokey` keyed instead of keyless) | `c1_kadm5_create_null_password_is_a_random_key_not_the_empty_password`; `scripts/kadmin-gate.sh:950-967,1884-1898` `kadmin addprinc -randkey rkempty` then a blank-password `kinit` is `Password incorrect` and `addprinc -pw ""` is `Password is too short`, Rust and MIT kadmind; `:938-944` MIT `kadmin addprinc -randkey renamefrom` against the Rust kadmind then `renprinc`; `scripts/iprop-gate.sh:140-142` `addprinc -randkey kiprop/…` then `ktadd` + kpropd auth; `scripts/chaos-gate.sh` prod realm `addprinc -randkey` hosts (proposed: `-nokey` cell — MIT `getprinc` `Number of keys: 0`) |
| svr_principal.c:376-420,455-470 `kadm5_create_principal_3` field application; alt_prof.c:573-632 `params.max_life`/`max_rlife`/`expiration`/`flags` | after `passwd_check`: `attributes` = entry when `KADM5_ATTRIBUTES` else `params.flags` (`default_principal_flags`, `KRB5_KDB_DEF_FLAGS` 0); `max_life` / `max_renewable_life` = entry when masked else `params.max_life` (kdc.conf `max_life`, 1 day) / `params.max_rlife` (0); `expiration` = entry when masked else `params.expiration` (`default_principal_expiration`, a `krb5_string_to_timestamp` form, else 0); `pw_expiration` = entry when masked, else `now + pw_max_life` when the (existing) policy has one, else 0; `KADM5_KVNO` keys the password at that kvno (`krb5_dbe_cpw`) or rewrites the random keys' kvno; `KADM5_POLICY` binds the name in `adb.policy` even when `get_policy` found none | `getprinc` shows every field; 0 | krb5-kdc/store.rs create_principal_3_in, default_create_attributes, apply_kdc_conf; krb5-admin/kadm5.rs parse_create, parse_principal_ent_rest; krb5-kdc/acl.rs default_principal_flags; krb5-types/timestamp.rs string_to_timestamp | the RPC record's `attributes`/`max_life`/`max_renewable_life`/`princ_expire_time`/`pw_expiration`/`kvno`/`policy` are read and applied under the mask (before Z1.1 `parse_create` skipped every scalar: the entry took the realm defaults and `-kvno`/`-expire`/`+flags` were dropped); defaults are `[realms] default_principal_flags` when written (flagspec tokens over 0, exactly MIT) else the `requires_preauth` knob's bit on a password-keyed create and 0 on a random-key create (MIT's default is 0 for both — `docs/security.md`), kdc.conf `max_life`/`max_renewable_life`, `default_principal_expiration` (the `str_conv.c:152-165` format table, local time; unparsable = 0) else 0, `now + pw_max_life`; the local `kadmin.local` verbs go through the same function with an empty mask and apply their options after | exact (masked fields, `pw_expiration`, kvno); stricter-documented (`params.flags` default carries `REQUIRES_PRE_AUTH` on password-keyed creates when `default_principal_flags` is unset; random-key creates are 0 like MIT) | `z1_kadm5_create_applies_every_masked_field`; `z1_kadm5_create_without_mask_bits_takes_realm_defaults`; `z1_default_principal_flags_is_params_flags_for_a_create`; `z1_create_without_max_life_takes_params_max_life`; `z1_create_without_expire_takes_default_principal_expiration`; `krb5-types timestamp::tests`; `scripts/kadmin-gate.sh` `z11_leg` (both kadminds restarted under `z11_profile`: kdc.conf + `default_principal_flags = +disallow_svr` + `default_principal_expiration = 20300102030405`) `addprinc -pw x z1def` → `getprinc` `Expiration date: Wed Jan 02 03:04:05 UTC 2030` and `Attributes: DISALLOW_SVR` on both legs; `addprinc -pw x -maxlife 1h -maxrenewlife 0 -expire … -pwexpire … -kvno 7 +disallow_all_tix +requires_preauth z1u` → `getprinc z1u` shape byte-identical Rust vs MIT kadmind (`z11-rust.txt`/`z11-mit.txt` diff), `addpol -maxlife 30d z1pw` + `addprinc -policy z1pw` → `Password expiration date` = now + 30 d both |
| kadmin/server/auth.c:205-272 `impose_restrictions`, :274-303 `auth_restrict`; server_stubs.c:337-343 `stub_auth_restrict`, :477-479, :518-520, :629-631; auth_acl.c:167-244 `parse_restrictions` | the ACL line's restrictions rewrite the create/modify *request* (`ent`, `*mask`) before `kadm5_create_principal`/`kadm5_modify_principal` and its mask validation: flags `\|= require` then `&= forbid` on the request's attributes and set `KADM5_ATTRIBUTES`; `-clearpolicy` clears `KADM5_POLICY` and sets `KADM5_POLICY_CLR`; `-policy P` replaces a different requested policy and sets `KADM5_POLICY` (so P's floors and the pwqual modules run in the create); `-expire`/`-pwexpire`/`-maxlife`/`-maxrenewlife` set the bit and take the cap when the bit was absent or the value exceeds it — an in-mask value below the cap (an explicit 0 included) is kept | `getprinc` after `addprinc`/`modprinc` by the restricted actor; `Password is too short` under a `-policy` restriction | krb5-kdc/acl.rs Restrictions::impose; krb5-kdc/store.rs AdminEnt, apply_acl_restrictions; krb5-admin/kadm5.rs impose_request_restrictions | `impose` runs on the parsed `AdminEnt` in the CREATE and MODIFY arms after the ACL check and before `create_princ_mask_err`/`modify_princ_mask_err` (before Z1.1 the restrictions were applied to the stored entry *after* the write: an in-mask 0 was raised to the cap, a stored value below the cap was never raised to it, and a `-policy` restriction bypassed `passwd_check` entirely); the local verbs impose on an empty-mask request (every restriction = its cap) | exact | `z1_acl_policy_restriction_is_enforced_on_create`; `z1_acl_maxrenewlife_keeps_an_in_mask_zero_and_lowers_above_cap`; `z1_acl_maxlife_absent_from_modify_mask_takes_the_cap`; `z1_acl_attribute_restriction_composes_with_the_request_attributes`; `z1_acl_expire_keeps_a_masked_value_below_the_cap`; `z1_impose_acl_restrictions_on_an_empty_request_takes_every_cap`; `scripts/kadmin-gate.sh` `z11_leg` both kadminds restarted with `z1pol … -policy shortpol` / `z1rl … -maxrenewlife 1d` / `z1ml … -maxlife 1h`: `addprinc -pw abc` → `add_principal: Password is too short while creating "z1short@KERBER.TEST".` and `getprinc` → `Principal does not exist`; `-maxrenewlife 0` → `0 days 00:00:00` and `-maxrenewlife 30d` → `1 day 00:00:00`; `addprinc -maxlife 30m` then `modprinc +requires_preauth` → `Maximum ticket life: 0 days 01:00:00` — identical on both legs |

## B1 — lib/krb5/krb (client FAST reply processing)

The client half of W1-B. Until W1-Z Z1.2 this section had no rows and the
client was graded only in `docs/security.md`; the rows here are the ones
the section re-audit found laxer than MIT (`working/audit-w1z-0913.md`
§3.2) and the fix that made them exact. Oracle for the live cells: the
MIT 1.22.2 KDC behind `scripts/lib/kdc-rewrite-proxy.py`, MIT `kinit -T`
and Rust `krb5-kinit --fast` side by side (`scripts/mit-fast-kdc-gate.sh`
Z1.2 cells).

| MIT file:line | check (condition) | MIT status + wire code | Rust site | Rust e_text + code | verdict | proof |
| --- | --- | --- | --- | --- | --- | --- |
| fast.c:517-560 `krb5int_fast_process_response` (`:548-558` the swap) | under an armor key the KDC-REP must carry PA-FX-FAST whose finished checksum over the ticket verifies (`:541-547`); then `resp->client` ← `finished->client`, `resp->padata` ← the decrypted inner padata, the strengthen key is taken — the outer cname / crealm / padata are unauthenticated and never read again | `KRB5_KDCREP_MODIFIED` "Ticket modified in KDC reply" on a bad checksum; `KRB5_ERR_FAST_REQUIRED` "FAST response missing finish message in KDC reply" without a finished message | krb5-protocol/as_ex.rs finish_fast_as; krb5-protocol/tgs.rs tgs_fast_reply_key | after `verify_fast_finished` the reply's `crealm` / `cname` / `padata` are the finished message's and the inner list before `finish_as_rep` compares or returns them; the TGS reply's `crealm` / `cname` likewise before `process_tgs_reply`'s compare; a missing finished message is `FAST response missing finish message in KDC reply` | exact | `z1_fast_as_rep_client_is_the_finished_client_under_canonicalize`; `z1_fast_as_rep_outer_cname_is_ignored_without_canonicalize`; `z1_fast_as_rep_finished_cname_mismatch_is_kdcrep_modified`; `scripts/mit-fast-kdc-gate.sh` Z1.2 (i): MIT `kinit -T` and Rust `krb5-kinit --fast` behind `kdc-rewrite-proxy.py as-rep-cname mitm` in front of the MIT KDC both succeed and `klist` shows `Default principal: user@KERBER.TEST` (before Z1.2 the Rust client compared and returned the outer cname) |
| get_in_tkt.c:236-241 `verify_as_reply` client compare | unless `canon_req` (CANONICALIZE, NT-ENTERPRISE, anonymous) the reply client must equal the request client — the compare runs after the FAST swap, so under armor it is the finished client that is compared and, under `canon_req`, returned as the canonical name | `KRB5_KDCREP_MODIFIED` | krb5-protocol/as_ex.rs finish_as_rep | `AS-REP cname mismatch` (`ReplyMismatch`) on the finished client; under canonicalize the outcome `cname` is the finished client, never the outer one | exact | `z1_fast_as_rep_client_is_the_finished_client_under_canonicalize`; `z1_fast_as_rep_finished_cname_mismatch_is_kdcrep_modified`; gate cell (i) above |
| fast.c:428-511 `krb5int_fast_process_error` (`:445-458` envelope failure, `:459-495` inner error); gc_via_tkt.c:190-194 the TGS call | under an armor key the e_data must decode as a padata sequence whose PA-FX-FAST decrypts with the request nonce; when it does not the outer error is the answer with `*retry = 0` and no `out_padata` (no cookie, no method data — "the KDC does not understand FAST"); an envelope without FX-ERROR is `KRB5KDC_ERR_PREAUTH_FAILED`; otherwise the inner error replaces the outer, the inner padata is the method data and `retry` is set only when that list has more than the FX-ERROR entry and includes an FX-COOKIE; the TGS path runs the same and reports the inner error | outer error fatal / `KRB5KDC_ERR_PREAUTH_FAILED` "Expecting FX_ERROR pa-data inside FAST container" | krb5-protocol/as_ex.rs fast_error_material; krb5-protocol/as_ex.rs continue_fast; krb5-protocol/tgs.rs tgs_once | the outer error is returned as received with `cookie: None`, `retry: false` (no second AS-REQ); `PREAUTH_FAILED` 24 without FX-ERROR; only `PREAUTH_REQUIRED` with `retry` continues the loop; the TGS error is the inner FX-ERROR's code and e_text when the envelope unwraps, else the outer | exact | `z1_fast_error_without_fx_fast_is_the_fatal_outer_error`; `z1_fast_error_with_a_corrupt_fx_fast_is_the_fatal_outer_error`; `z1_fast_error_without_inner_fx_error_is_preauth_failed`; `z1_fast_tgs_error_is_the_inner_fx_error`; `scripts/mit-fast-kdc-gate.sh` Z1.2 (ii): both clients behind `kdc-rewrite-proxy.py strip-fx-fast` stop at the first 25 (`Additional pre-authentication required` / `KRB-ERROR 25`) with exactly one `req#` each (before Z1.2 the Rust client took the outer cookie and retried) |
