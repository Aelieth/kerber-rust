[sssd]
services = nss
domains = files

[nss]

[kcm]
# Secrets DB under /var/lib/sss/secrets (libsss_secrets).
# TGT renewal stays off: this image is a KCM socket oracle, not a KDC.
tgt_renewal = false
